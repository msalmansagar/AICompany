# Shell scripts and git hooks must stay LF, or a CRLF checkout on Windows
# (core.autocrlf=true here) breaks the shebang with "bad interpreter".
*.sh            text eol=lf
.githooks/*     text eol=lf
