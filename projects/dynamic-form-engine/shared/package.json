{
  "name": "@qdb/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types and engines for Dynamic Form Engine",
  "main": "src/index.ts",
  "types": "src/server.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "json-rules-engine": "^6.4.2",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
