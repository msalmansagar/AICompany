'use client';

/**
 * Hook alias that reads active entity state from ActiveEntityContext.
 * Exported here so the import path for consumers is stable.
 */
export { useActiveEntity } from '../contexts/ActiveEntityContext';
