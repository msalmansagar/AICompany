/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
var pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad;
/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./index.ts"
/*!******************!*\
  !*** ./index.ts ***!
  \******************/
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
eval("{\n\n/**\n * index.ts  — PCF entry point\n * EmailEditorControl: StandardControl<IInputs, IOutputs>\n *\n * Lifecycle:\n *   init()        — capture context, build MetadataService, read initial value\n *   updateView()  — return React element tree (virtual control)\n *   getOutputs()  — return current template string to save pipeline\n *   destroy()     — dispose service\n */\nvar __createBinding = this && this.__createBinding || (Object.create ? function (o, m, k, k2) {\n  if (k2 === undefined) k2 = k;\n  var desc = Object.getOwnPropertyDescriptor(m, k);\n  if (!desc || (\"get\" in desc ? !m.__esModule : desc.writable || desc.configurable)) {\n    desc = {\n      enumerable: true,\n      get: function get() {\n        return m[k];\n      }\n    };\n  }\n  Object.defineProperty(o, k2, desc);\n} : function (o, m, k, k2) {\n  if (k2 === undefined) k2 = k;\n  o[k2] = m[k];\n});\nvar __setModuleDefault = this && this.__setModuleDefault || (Object.create ? function (o, v) {\n  Object.defineProperty(o, \"default\", {\n    enumerable: true,\n    value: v\n  });\n} : function (o, v) {\n  o[\"default\"] = v;\n});\nvar __importStar = this && this.__importStar || function () {\n  var _ownKeys = function ownKeys(o) {\n    _ownKeys = Object.getOwnPropertyNames || function (o) {\n      var ar = [];\n      for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;\n      return ar;\n    };\n    return _ownKeys(o);\n  };\n  return function (mod) {\n    if (mod && mod.__esModule) return mod;\n    var result = {};\n    if (mod != null) for (var k = _ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== \"default\") __createBinding(result, mod, k[i]);\n    __setModuleDefault(result, mod);\n    return result;\n  };\n}();\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.EmailEditorControl = void 0;\nvar React = __importStar(__webpack_require__(/*! react */ \"./node_modules/react/index.js\"));\nvar EmailEditorComponent_1 = __webpack_require__(/*! ./src/components/EmailEditorComponent */ \"./src/components/EmailEditorComponent.tsx\");\nvar MetadataService_1 = __webpack_require__(/*! ./src/services/MetadataService */ \"./src/services/MetadataService.ts\");\nvar MetadataCache_1 = __webpack_require__(/*! ./src/services/MetadataCache */ \"./src/services/MetadataCache.ts\");\nvar Logger_1 = __webpack_require__(/*! ./src/services/Logger */ \"./src/services/Logger.ts\");\nclass EmailEditorControl {\n  constructor() {\n    this.currentValue = '';\n  }\n  /** Called once when the control is first attached to the form. */\n  init(context, notifyOutputChanged, _state) {\n    var _a, _b;\n    this.notifyOutputChanged = notifyOutputChanged;\n    this.currentValue = (_a = context.parameters.templateValue.raw) !== null && _a !== void 0 ? _a : '';\n    this.logger = new Logger_1.Logger('EmailEditorControl');\n    var clientUrl = this.resolveClientUrl(context);\n    var cache = new MetadataCache_1.MetadataCache(window.sessionStorage, clientUrl);\n    this.metadataService = new MetadataService_1.MetadataService({\n      webAPI: context.webAPI,\n      cache,\n      logger: this.logger,\n      clientUrl\n    });\n    this.logger.info('init', {\n      rootEntity: (_b = context.parameters.rootEntityLogicalName.raw) !== null && _b !== void 0 ? _b : '',\n      clientUrl\n    });\n  }\n  /** Called whenever the bound field value or context properties change. */\n  updateView(context) {\n    var _a, _b, _c, _d;\n    var incoming = (_a = context.parameters.templateValue.raw) !== null && _a !== void 0 ? _a : '';\n    if (incoming !== this.currentValue) {\n      this.currentValue = incoming;\n    }\n    return React.createElement(EmailEditorComponent_1.EmailEditorComponent, {\n      value: this.currentValue,\n      rootEntityLogicalName: (_b = context.parameters.rootEntityLogicalName.raw) !== null && _b !== void 0 ? _b : '',\n      sampleRecordId: (_c = context.parameters.sampleRecordId.raw) !== null && _c !== void 0 ? _c : null,\n      maxRelationshipDepth: (_d = context.parameters.maxRelationshipDepth.raw) !== null && _d !== void 0 ? _d : 3,\n      metadataService: this.metadataService,\n      logger: this.logger,\n      onChange: next => {\n        if (next === this.currentValue) return;\n        this.currentValue = next;\n        this.notifyOutputChanged();\n      }\n    });\n  }\n  /** Returns the current template value back to the CRM field. */\n  getOutputs() {\n    return {\n      templateValue: this.currentValue\n    };\n  }\n  /** Called when the control is removed from the form. */\n  destroy() {\n    this.metadataService.dispose();\n    this.logger.info('destroy', {});\n  }\n  // ── Private ───────────────────────────────────────────────────────────────\n  /**\n   * Resolves the CRM client URL from the PCF context.\n   * Falls through to window.location.origin for test harness / dev mode.\n   */\n  resolveClientUrl(context) {\n    var _a;\n    try {\n      // PCF context exposes page.getClientUrl() in UCI\n      var page = context.page;\n      if (page === null || page === void 0 ? void 0 : page.getClientUrl) return page.getClientUrl().replace(/\\/$/, '');\n    } catch (_b) {\n      // Expected outside UCI — fall through\n    }\n    try {\n      // Try parent Xrm for legacy web resource embedding\n      var xrm = window.parent.Xrm;\n      if ((_a = xrm === null || xrm === void 0 ? void 0 : xrm.Utility) === null || _a === void 0 ? void 0 : _a.getGlobalContext) {\n        return xrm.Utility.getGlobalContext().getClientUrl().replace(/\\/$/, '');\n      }\n    } catch (_c) {\n      // Not in CRM context\n    }\n    return window.location.origin.replace(/\\/$/, '');\n  }\n}\nexports.EmailEditorControl = EmailEditorControl;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./index.ts?\n}");

/***/ },

/***/ "./src/components/EditorSurface.tsx"
/*!******************************************!*\
  !*** ./src/components/EditorSurface.tsx ***!
  \******************************************/
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
eval("{\n\n/**\n * EditorSurface.tsx\n * The contenteditable editing surface. Responsible for:\n *  - Rendering the current template value\n *  - Detecting \".\" keypress and firing onDotTyped with the trigger Range\n *  - Notifying parent of content changes via onChange\n *  - Forwarding the ref so the parent can access the DOM node\n */\nvar __createBinding = this && this.__createBinding || (Object.create ? function (o, m, k, k2) {\n  if (k2 === undefined) k2 = k;\n  var desc = Object.getOwnPropertyDescriptor(m, k);\n  if (!desc || (\"get\" in desc ? !m.__esModule : desc.writable || desc.configurable)) {\n    desc = {\n      enumerable: true,\n      get: function get() {\n        return m[k];\n      }\n    };\n  }\n  Object.defineProperty(o, k2, desc);\n} : function (o, m, k, k2) {\n  if (k2 === undefined) k2 = k;\n  o[k2] = m[k];\n});\nvar __setModuleDefault = this && this.__setModuleDefault || (Object.create ? function (o, v) {\n  Object.defineProperty(o, \"default\", {\n    enumerable: true,\n    value: v\n  });\n} : function (o, v) {\n  o[\"default\"] = v;\n});\nvar __importStar = this && this.__importStar || function () {\n  var _ownKeys = function ownKeys(o) {\n    _ownKeys = Object.getOwnPropertyNames || function (o) {\n      var ar = [];\n      for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;\n      return ar;\n    };\n    return _ownKeys(o);\n  };\n  return function (mod) {\n    if (mod && mod.__esModule) return mod;\n    var result = {};\n    if (mod != null) for (var k = _ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== \"default\") __createBinding(result, mod, k[i]);\n    __setModuleDefault(result, mod);\n    return result;\n  };\n}();\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.EditorSurface = void 0;\nvar React = __importStar(__webpack_require__(/*! react */ \"./node_modules/react/index.js\"));\nvar react_1 = __webpack_require__(/*! react */ \"./node_modules/react/index.js\");\n/**\n * We use useImperativeHandle to expose the raw HTMLDivElement ref\n * so the parent (EmailEditorComponent) can feed it to useEditorCursor.\n */\nexports.EditorSurface = (0, react_1.forwardRef)((props, ref) => {\n  var innerRef = (0, react_1.useRef)(null);\n  (0, react_1.useImperativeHandle)(ref, () => innerRef.current);\n  // Sync external value → DOM only when it differs, to avoid caret reset\n  (0, react_1.useEffect)(() => {\n    var el = innerRef.current;\n    if (!el) return;\n    if (el.innerText !== props.value) {\n      el.innerText = props.value;\n    }\n  }, [props.value]);\n  var handleInput = () => {\n    var el = innerRef.current;\n    if (!el) return;\n    props.onChange(el.innerText);\n  };\n  var handleKeyUp = event => {\n    if (event.key === 'Escape') {\n      props.onEscape();\n      return;\n    }\n    if (event.key === '.') {\n      var sel = window.getSelection();\n      if (!sel || sel.rangeCount === 0) return;\n      var current = sel.getRangeAt(0);\n      if (current.startOffset === 0) return;\n      var triggerRange = current.cloneRange();\n      triggerRange.setStart(triggerRange.endContainer, triggerRange.endOffset - 1);\n      triggerRange.setEnd(triggerRange.endContainer, triggerRange.endOffset);\n      props.onDotTyped(triggerRange);\n    }\n  };\n  return React.createElement(\"div\", {\n    ref: innerRef,\n    contentEditable: true,\n    suppressContentEditableWarning: true,\n    spellCheck: true,\n    role: \"textbox\",\n    \"aria-multiline\": \"true\",\n    \"aria-label\": \"Email template editor\",\n    style: {\n      minHeight: '200px',\n      padding: '8px 12px',\n      fontFamily: 'Segoe UI, sans-serif',\n      fontSize: '14px',\n      lineHeight: '1.6',\n      whiteSpace: 'pre-wrap',\n      wordBreak: 'break-word',\n      outline: 'none',\n      border: '1px solid #c8c6c4',\n      borderRadius: '2px',\n      backgroundColor: '#ffffff',\n      color: '#323130'\n    },\n    onInput: handleInput,\n    onKeyUp: handleKeyUp\n  });\n});\nexports.EditorSurface.displayName = 'EditorSurface';\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/components/EditorSurface.tsx?\n}");

/***/ },

/***/ "./src/components/EmailEditorComponent.tsx"
/*!*************************************************!*\
  !*** ./src/components/EmailEditorComponent.tsx ***!
  \*************************************************/
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
eval("{\n\n/**\n * EmailEditorComponent.tsx\n * Root React component for the Email Editor PCF control.\n * Owns the popup state machine and wires together:\n *   EditorSurface ↔ FieldSelectorPopup ↔ PreviewPane\n */\nvar __createBinding = this && this.__createBinding || (Object.create ? function (o, m, k, k2) {\n  if (k2 === undefined) k2 = k;\n  var desc = Object.getOwnPropertyDescriptor(m, k);\n  if (!desc || (\"get\" in desc ? !m.__esModule : desc.writable || desc.configurable)) {\n    desc = {\n      enumerable: true,\n      get: function get() {\n        return m[k];\n      }\n    };\n  }\n  Object.defineProperty(o, k2, desc);\n} : function (o, m, k, k2) {\n  if (k2 === undefined) k2 = k;\n  o[k2] = m[k];\n});\nvar __setModuleDefault = this && this.__setModuleDefault || (Object.create ? function (o, v) {\n  Object.defineProperty(o, \"default\", {\n    enumerable: true,\n    value: v\n  });\n} : function (o, v) {\n  o[\"default\"] = v;\n});\nvar __importStar = this && this.__importStar || function () {\n  var _ownKeys = function ownKeys(o) {\n    _ownKeys = Object.getOwnPropertyNames || function (o) {\n      var ar = [];\n      for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;\n      return ar;\n    };\n    return _ownKeys(o);\n  };\n  return function (mod) {\n    if (mod && mod.__esModule) return mod;\n    var result = {};\n    if (mod != null) for (var k = _ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== \"default\") __createBinding(result, mod, k[i]);\n    __setModuleDefault(result, mod);\n    return result;\n  };\n}();\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.EmailEditorComponent = void 0;\nvar React = __importStar(__webpack_require__(/*! react */ \"./node_modules/react/index.js\"));\nvar react_1 = __webpack_require__(/*! react */ \"./node_modules/react/index.js\");\nvar EditorSurface_1 = __webpack_require__(/*! ./EditorSurface */ \"./src/components/EditorSurface.tsx\");\nvar FieldSelectorPopup_1 = __webpack_require__(/*! ./FieldSelectorPopup */ \"./src/components/FieldSelectorPopup.tsx\");\nvar PreviewPane_1 = __webpack_require__(/*! ./PreviewPane */ \"./src/components/PreviewPane.tsx\");\nvar useEditorCursor_1 = __webpack_require__(/*! ../hooks/useEditorCursor */ \"./src/hooks/useEditorCursor.ts\");\nvar TokenEngine_1 = __webpack_require__(/*! ../token/TokenEngine */ \"./src/token/TokenEngine.ts\");\nvar EditorProps_1 = __webpack_require__(/*! ../types/EditorProps */ \"./src/types/EditorProps.ts\");\nvar EmailEditorComponent = props => {\n  var {\n    value,\n    rootEntityLogicalName,\n    sampleRecordId,\n    maxRelationshipDepth,\n    metadataService,\n    logger,\n    onChange\n  } = props;\n  var surfaceRef = (0, react_1.useRef)(null);\n  var [popup, setPopup] = (0, react_1.useState)(EditorProps_1.CLOSED_POPUP);\n  var cursor = (0, useEditorCursor_1.useEditorCursor)(surfaceRef);\n  var engine = (0, react_1.useMemo)(() => new TokenEngine_1.TokenEngine(logger), [logger]);\n  // ── Popup open ──────────────────────────────────────────────────────────\n  var openPopup = (0, react_1.useCallback)(triggerRange => {\n    var rect = triggerRange.getBoundingClientRect();\n    setPopup({\n      open: true,\n      anchor: {\n        x: rect.left,\n        y: rect.bottom + 4\n      },\n      entityPath: [rootEntityLogicalName],\n      triggerRange,\n      mode: 'field'\n    });\n  }, [rootEntityLogicalName]);\n  var closePopup = (0, react_1.useCallback)(() => setPopup(EditorProps_1.CLOSED_POPUP), []);\n  // ── Dot typed in editor ─────────────────────────────────────────────────\n  var handleDotTyped = (0, react_1.useCallback)(triggerRange => {\n    cursor.saveSelection();\n    openPopup(triggerRange);\n  }, [cursor, openPopup]);\n  // ── Field selected from popup ───────────────────────────────────────────\n  var handleFieldSelected = (0, react_1.useCallback)(tokenSource => {\n    var _a, _b;\n    if (!popup.triggerRange) return;\n    // Replace the \".\" that triggered the popup with the full token\n    cursor.replaceRangeWithText(popup.triggerRange, tokenSource);\n    var next = (_b = (_a = surfaceRef.current) === null || _a === void 0 ? void 0 : _a.innerText) !== null && _b !== void 0 ? _b : '';\n    onChange(next);\n    closePopup();\n  }, [popup.triggerRange, cursor, onChange, closePopup]);\n  // ── Editor content change ───────────────────────────────────────────────\n  var handleSurfaceChange = (0, react_1.useCallback)(next => onChange(next), [onChange]);\n  // ── Popup drill navigation ──────────────────────────────────────────────\n  var handleDrillDown = (0, react_1.useCallback)(entityLogicalName => {\n    setPopup(prev => Object.assign(Object.assign({}, prev), {\n      entityPath: [...prev.entityPath, entityLogicalName]\n    }));\n  }, []);\n  var handleDrillUp = (0, react_1.useCallback)(() => {\n    setPopup(prev => {\n      if (prev.entityPath.length <= 1) return prev;\n      return Object.assign(Object.assign({}, prev), {\n        entityPath: prev.entityPath.slice(0, -1)\n      });\n    });\n  }, []);\n  // ── Validation banner ───────────────────────────────────────────────────\n  var parseErrors = (0, react_1.useMemo)(() => engine.validate(value), [engine, value]);\n  // ── Render ──────────────────────────────────────────────────────────────\n  return React.createElement(\"div\", {\n    style: {\n      display: 'flex',\n      flexDirection: 'column',\n      gap: '4px',\n      width: '100%',\n      fontFamily: 'Segoe UI, sans-serif',\n      position: 'relative'\n    }\n  }, React.createElement(\"div\", {\n    style: {\n      display: 'flex',\n      gap: '8px',\n      alignItems: 'center',\n      flexWrap: 'wrap'\n    }\n  }, React.createElement(\"span\", {\n    style: {\n      fontSize: '11px',\n      color: '#605e5c'\n    }\n  }, \"Type \", React.createElement(\"strong\", null, \".\"), \" to insert a dynamic field token\"), parseErrors.length > 0 && React.createElement(\"span\", {\n    style: {\n      fontSize: '11px',\n      color: '#a4262c',\n      backgroundColor: '#fde7e9',\n      padding: '2px 8px',\n      borderRadius: '2px'\n    }\n  }, parseErrors.length, \" template error\", parseErrors.length > 1 ? 's' : '')), React.createElement(EditorSurface_1.EditorSurface, {\n    ref: surfaceRef,\n    value: value,\n    onChange: handleSurfaceChange,\n    onDotTyped: handleDotTyped,\n    onEscape: closePopup\n  }), popup.open && popup.anchor && React.createElement(FieldSelectorPopup_1.FieldSelectorPopup, {\n    anchor: popup.anchor,\n    entityPath: popup.entityPath,\n    metadataService: metadataService,\n    mode: popup.mode,\n    maxDepth: maxRelationshipDepth,\n    onDrillDown: handleDrillDown,\n    onDrillUp: handleDrillUp,\n    onSelect: handleFieldSelected,\n    onClose: closePopup\n  }), React.createElement(PreviewPane_1.PreviewPane, {\n    template: value,\n    rootEntity: rootEntityLogicalName,\n    sampleRecordId: sampleRecordId,\n    engine: engine,\n    metadataService: metadataService\n  }));\n};\nexports.EmailEditorComponent = EmailEditorComponent;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/components/EmailEditorComponent.tsx?\n}");

/***/ },

/***/ "./src/components/FieldSelectorPopup.tsx"
/*!***********************************************!*\
  !*** ./src/components/FieldSelectorPopup.tsx ***!
  \***********************************************/
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
eval("{\n\n/**\n * FieldSelectorPopup.tsx\n * Anchored popup that shows entity attributes, N:1 lookups, and 1:N children.\n * Supports keyboard navigation (↑↓ move, → drill-in, ← drill-out, Enter insert, Esc close).\n * Lazy-loads metadata for each entity level.\n */\nvar __createBinding = this && this.__createBinding || (Object.create ? function (o, m, k, k2) {\n  if (k2 === undefined) k2 = k;\n  var desc = Object.getOwnPropertyDescriptor(m, k);\n  if (!desc || (\"get\" in desc ? !m.__esModule : desc.writable || desc.configurable)) {\n    desc = {\n      enumerable: true,\n      get: function get() {\n        return m[k];\n      }\n    };\n  }\n  Object.defineProperty(o, k2, desc);\n} : function (o, m, k, k2) {\n  if (k2 === undefined) k2 = k;\n  o[k2] = m[k];\n});\nvar __setModuleDefault = this && this.__setModuleDefault || (Object.create ? function (o, v) {\n  Object.defineProperty(o, \"default\", {\n    enumerable: true,\n    value: v\n  });\n} : function (o, v) {\n  o[\"default\"] = v;\n});\nvar __importStar = this && this.__importStar || function () {\n  var _ownKeys = function ownKeys(o) {\n    _ownKeys = Object.getOwnPropertyNames || function (o) {\n      var ar = [];\n      for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;\n      return ar;\n    };\n    return _ownKeys(o);\n  };\n  return function (mod) {\n    if (mod && mod.__esModule) return mod;\n    var result = {};\n    if (mod != null) for (var k = _ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== \"default\") __createBinding(result, mod, k[i]);\n    __setModuleDefault(result, mod);\n    return result;\n  };\n}();\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.FieldSelectorPopup = void 0;\nvar React = __importStar(__webpack_require__(/*! react */ \"./node_modules/react/index.js\"));\nvar react_1 = __webpack_require__(/*! react */ \"./node_modules/react/index.js\");\nvar useMetadata_1 = __webpack_require__(/*! ../hooks/useMetadata */ \"./src/hooks/useMetadata.ts\");\nvar useKeyboardNav_1 = __webpack_require__(/*! ../hooks/useKeyboardNav */ \"./src/hooks/useKeyboardNav.ts\");\nfunction getDisplayName(obj) {\n  var _a, _b;\n  if ('DisplayName' in obj && ((_b = (_a = obj.DisplayName) === null || _a === void 0 ? void 0 : _a.UserLocalizedLabel) === null || _b === void 0 ? void 0 : _b.Label)) {\n    return obj.DisplayName.UserLocalizedLabel.Label;\n  }\n  if ('SchemaName' in obj) return obj.SchemaName;\n  return '';\n}\nvar FieldSelectorPopup = props => {\n  var _a;\n  var {\n    anchor,\n    entityPath,\n    metadataService,\n    mode,\n    maxDepth,\n    onDrillDown,\n    onDrillUp,\n    onSelect: _onSelect,\n    onClose\n  } = props;\n  var currentEntity = (_a = entityPath[entityPath.length - 1]) !== null && _a !== void 0 ? _a : '';\n  var {\n    bundle,\n    loading,\n    error\n  } = (0, useMetadata_1.useMetadata)(currentEntity, metadataService);\n  var [filter, setFilter] = (0, react_1.useState)('');\n  var filterRef = (0, react_1.useRef)(null);\n  // Reset filter when entity changes\n  (0, react_1.useEffect)(() => {\n    setFilter('');\n  }, [currentEntity]);\n  // Focus the filter input when popup opens\n  (0, react_1.useEffect)(() => {\n    var _a;\n    (_a = filterRef.current) === null || _a === void 0 ? void 0 : _a.focus();\n  }, []);\n  var rows = (0, react_1.useMemo)(() => {\n    if (!bundle) return [];\n    var result = [];\n    var needle = filter.trim().toLowerCase();\n    var matches = (displayName, logicalName) => !needle || displayName.toLowerCase().includes(needle) || logicalName.toLowerCase().includes(needle);\n    // Attributes (skip in \"each\" mode — each is for collections only)\n    if (mode !== 'each') {\n      for (var attr of bundle.attributes) {\n        var dn = getDisplayName(attr);\n        if (matches(dn, attr.LogicalName)) {\n          result.push({\n            kind: 'attribute',\n            logicalName: attr.LogicalName,\n            displayName: dn || attr.LogicalName,\n            typeLabel: attr.AttributeType\n          });\n        }\n      }\n      // N:1 (parent lookups — drill into them for nested paths)\n      for (var rel of bundle.manyToOne) {\n        var _dn = rel.ReferencingEntityNavigationPropertyName;\n        if (matches(_dn, rel.ReferencingAttribute)) {\n          result.push({\n            kind: 'manyToOne',\n            logicalName: rel.ReferencingAttribute,\n            displayName: _dn,\n            typeLabel: \"\\u2192 \".concat(rel.ReferencedEntity),\n            navigationTarget: rel.ReferencedEntity\n          });\n        }\n      }\n    }\n    // 1:N (child collections — primary for #each mode)\n    for (var _rel of bundle.oneToMany) {\n      var _dn2 = _rel.ReferencedEntityNavigationPropertyName;\n      if (matches(_dn2, _rel.ReferencingEntity)) {\n        result.push({\n          kind: 'oneToMany',\n          logicalName: _rel.ReferencedEntityNavigationPropertyName,\n          displayName: _dn2,\n          typeLabel: \"1:N \\u2192 \".concat(_rel.ReferencingEntity),\n          navigationTarget: _rel.ReferencingEntity\n        });\n      }\n    }\n    return result;\n  }, [bundle, filter, mode]);\n  var buildToken = row => {\n    // Build the full path: skip root entity label, concat remaining segments\n    var pathSegments = entityPath.slice(1).concat(row.logicalName);\n    var path = pathSegments.join('.');\n    if (row.kind === 'oneToMany') {\n      return \"{{#each \".concat(path, \"}}\\n\\n{{/each}}\");\n    }\n    if (row.kind === 'manyToOne') {\n      return \"{{\".concat(path, \" | display}}\");\n    }\n    return \"{{\".concat(path, \"}}\");\n  };\n  var canDrillIn = row => !!row.navigationTarget && entityPath.length < maxDepth;\n  var {\n    selectedIndex,\n    setSelectedIndex,\n    handleKeyDown\n  } = (0, useKeyboardNav_1.useKeyboardNav)({\n    items: rows,\n    onSelect: row => _onSelect(buildToken(row)),\n    onDrillIn: row => {\n      if (row.navigationTarget) onDrillDown(row.navigationTarget);\n    },\n    onDrillOut: onDrillUp,\n    onClose,\n    canDrillIn\n  });\n  var popupStyle = {\n    position: 'fixed',\n    left: anchor.x,\n    top: anchor.y,\n    zIndex: 9999,\n    width: '340px',\n    maxHeight: '360px',\n    overflowY: 'auto',\n    backgroundColor: '#ffffff',\n    border: '1px solid #c8c6c4',\n    borderRadius: '2px',\n    boxShadow: '0 4px 16px rgba(0,0,0,0.18)',\n    fontFamily: 'Segoe UI, sans-serif',\n    fontSize: '13px'\n  };\n  var headerStyle = {\n    padding: '6px 10px',\n    borderBottom: '1px solid #edebe9',\n    backgroundColor: '#f3f2f1'\n  };\n  var breadcrumbStyle = {\n    fontSize: '11px',\n    color: '#605e5c',\n    marginBottom: '4px'\n  };\n  var rowBaseStyle = {\n    display: 'flex',\n    justifyContent: 'space-between',\n    alignItems: 'center',\n    padding: '5px 10px',\n    cursor: 'pointer',\n    borderBottom: '1px solid #f3f2f1',\n    color: '#323130'\n  };\n  var typeLabelStyle = {\n    fontSize: '11px',\n    color: '#605e5c',\n    marginLeft: '8px',\n    whiteSpace: 'nowrap'\n  };\n  return React.createElement(\"div\", {\n    role: \"listbox\",\n    \"aria-label\": \"Field selector\",\n    style: popupStyle,\n    onKeyDown: handleKeyDown,\n    tabIndex: -1\n  }, React.createElement(\"div\", {\n    style: headerStyle\n  }, entityPath.length > 1 && React.createElement(\"div\", {\n    style: breadcrumbStyle\n  }, entityPath.join(' › ')), React.createElement(\"input\", {\n    ref: filterRef,\n    type: \"text\",\n    placeholder: \"Search fields\\u2026\",\n    value: filter,\n    onChange: e => {\n      setFilter(e.target.value);\n      setSelectedIndex(0);\n    },\n    onKeyDown: handleKeyDown,\n    style: {\n      width: '100%',\n      border: '1px solid #c8c6c4',\n      borderRadius: '2px',\n      padding: '3px 6px',\n      fontSize: '13px',\n      outline: 'none',\n      boxSizing: 'border-box'\n    },\n    \"aria-label\": \"Filter fields\"\n  })), loading && React.createElement(\"div\", {\n    style: {\n      padding: '10px',\n      color: '#605e5c'\n    }\n  }, \"Loading metadata\\u2026\"), error && React.createElement(\"div\", {\n    style: {\n      padding: '10px',\n      color: '#a4262c'\n    }\n  }, \"Failed to load metadata. Check console for details.\"), !loading && !error && rows.length === 0 && React.createElement(\"div\", {\n    style: {\n      padding: '10px',\n      color: '#605e5c'\n    }\n  }, \"No fields match.\"), !loading && !error && rows.map((row, i) => {\n    var isSelected = i === selectedIndex;\n    return React.createElement(\"div\", {\n      key: \"\".concat(row.kind, \":\").concat(row.logicalName),\n      role: \"option\",\n      \"aria-selected\": isSelected,\n      style: Object.assign(Object.assign({}, rowBaseStyle), {\n        backgroundColor: isSelected ? '#deecf9' : 'transparent'\n      }),\n      onMouseEnter: () => setSelectedIndex(i),\n      onClick: () => _onSelect(buildToken(row))\n    }, React.createElement(\"span\", {\n      style: {\n        overflow: 'hidden',\n        textOverflow: 'ellipsis',\n        whiteSpace: 'nowrap'\n      }\n    }, row.displayName, canDrillIn(row) && React.createElement(\"span\", {\n      style: {\n        marginLeft: '4px',\n        color: '#0078d4'\n      }\n    }, \"\\u203A\")), React.createElement(\"span\", {\n      style: typeLabelStyle\n    }, row.typeLabel));\n  }));\n};\nexports.FieldSelectorPopup = FieldSelectorPopup;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/components/FieldSelectorPopup.tsx?\n}");

/***/ },

/***/ "./src/components/PreviewPane.tsx"
/*!****************************************!*\
  !*** ./src/components/PreviewPane.tsx ***!
  \****************************************/
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
eval("{\n\n/**\n * PreviewPane.tsx\n * Renders the template against a sample record fetched from Dataverse.\n * Displays resolved token values inline — errors shown as highlighted spans.\n */\nvar __createBinding = this && this.__createBinding || (Object.create ? function (o, m, k, k2) {\n  if (k2 === undefined) k2 = k;\n  var desc = Object.getOwnPropertyDescriptor(m, k);\n  if (!desc || (\"get\" in desc ? !m.__esModule : desc.writable || desc.configurable)) {\n    desc = {\n      enumerable: true,\n      get: function get() {\n        return m[k];\n      }\n    };\n  }\n  Object.defineProperty(o, k2, desc);\n} : function (o, m, k, k2) {\n  if (k2 === undefined) k2 = k;\n  o[k2] = m[k];\n});\nvar __setModuleDefault = this && this.__setModuleDefault || (Object.create ? function (o, v) {\n  Object.defineProperty(o, \"default\", {\n    enumerable: true,\n    value: v\n  });\n} : function (o, v) {\n  o[\"default\"] = v;\n});\nvar __importStar = this && this.__importStar || function () {\n  var _ownKeys = function ownKeys(o) {\n    _ownKeys = Object.getOwnPropertyNames || function (o) {\n      var ar = [];\n      for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;\n      return ar;\n    };\n    return _ownKeys(o);\n  };\n  return function (mod) {\n    if (mod && mod.__esModule) return mod;\n    var result = {};\n    if (mod != null) for (var k = _ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== \"default\") __createBinding(result, mod, k[i]);\n    __setModuleDefault(result, mod);\n    return result;\n  };\n}();\nvar __awaiter = this && this.__awaiter || function (thisArg, _arguments, P, generator) {\n  function adopt(value) {\n    return value instanceof P ? value : new P(function (resolve) {\n      resolve(value);\n    });\n  }\n  return new (P || (P = Promise))(function (resolve, reject) {\n    function fulfilled(value) {\n      try {\n        step(generator.next(value));\n      } catch (e) {\n        reject(e);\n      }\n    }\n    function rejected(value) {\n      try {\n        step(generator[\"throw\"](value));\n      } catch (e) {\n        reject(e);\n      }\n    }\n    function step(result) {\n      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);\n    }\n    step((generator = generator.apply(thisArg, _arguments || [])).next());\n  });\n};\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.PreviewPane = void 0;\nvar React = __importStar(__webpack_require__(/*! react */ \"./node_modules/react/index.js\"));\nvar react_1 = __webpack_require__(/*! react */ \"./node_modules/react/index.js\");\nvar PreviewPane = props => {\n  var {\n    template,\n    rootEntity,\n    sampleRecordId,\n    engine,\n    metadataService\n  } = props;\n  var [preview, setPreview] = (0, react_1.useState)({\n    status: 'idle'\n  });\n  var [expanded, setExpanded] = (0, react_1.useState)(false);\n  (0, react_1.useEffect)(() => {\n    if (!sampleRecordId || !rootEntity || !template.trim()) {\n      setPreview({\n        status: 'idle'\n      });\n      return;\n    }\n    var cancelled = false;\n    setPreview({\n      status: 'loading'\n    });\n    (() => __awaiter(void 0, void 0, void 0, function* () {\n      try {\n        var entity = yield metadataService.getEntity(rootEntity);\n        var record = yield metadataService.fetchRecord(entity.EntitySetName, sampleRecordId, [], []);\n        if (cancelled) return;\n        var context = {\n          root: record,\n          locale: navigator.language || 'en',\n          currency: 'USD'\n        };\n        var rendered = engine.renderSource(template, context);\n        setPreview({\n          status: 'ready',\n          html: rendered\n        });\n      } catch (err) {\n        if (!cancelled) {\n          setPreview({\n            status: 'error',\n            message: err instanceof Error ? err.message : 'preview_failed'\n          });\n        }\n      }\n    }))();\n    return () => {\n      cancelled = true;\n    };\n  }, [template, rootEntity, sampleRecordId, engine, metadataService]);\n  if (!sampleRecordId) return null;\n  var containerStyle = {\n    marginTop: '8px',\n    border: '1px solid #edebe9',\n    borderRadius: '2px',\n    fontFamily: 'Segoe UI, sans-serif',\n    fontSize: '13px'\n  };\n  var headerStyle = {\n    display: 'flex',\n    justifyContent: 'space-between',\n    alignItems: 'center',\n    padding: '6px 10px',\n    backgroundColor: '#f3f2f1',\n    cursor: 'pointer',\n    userSelect: 'none'\n  };\n  var bodyStyle = {\n    padding: '10px 12px',\n    whiteSpace: 'pre-wrap',\n    wordBreak: 'break-word',\n    color: '#323130',\n    maxHeight: '300px',\n    overflowY: 'auto'\n  };\n  return React.createElement(\"div\", {\n    style: containerStyle\n  }, React.createElement(\"div\", {\n    style: headerStyle,\n    onClick: () => setExpanded(e => !e),\n    role: \"button\",\n    \"aria-expanded\": expanded,\n    tabIndex: 0,\n    onKeyDown: e => {\n      if (e.key === 'Enter' || e.key === ' ') setExpanded(x => !x);\n    }\n  }, React.createElement(\"span\", null, \"Preview \", expanded ? '▲' : '▼'), preview.status === 'loading' && React.createElement(\"span\", {\n    style: {\n      color: '#605e5c'\n    }\n  }, \"Loading\\u2026\"), preview.status === 'error' && React.createElement(\"span\", {\n    style: {\n      color: '#a4262c'\n    }\n  }, \"Error\")), expanded && React.createElement(\"div\", {\n    style: bodyStyle\n  }, preview.status === 'idle' && React.createElement(\"span\", {\n    style: {\n      color: '#605e5c'\n    }\n  }, \"No sample record configured. Set the sampleRecordId property.\"), preview.status === 'loading' && React.createElement(\"span\", {\n    style: {\n      color: '#605e5c'\n    }\n  }, \"Fetching sample record\\u2026\"), preview.status === 'error' && React.createElement(\"span\", {\n    style: {\n      color: '#a4262c'\n    }\n  }, preview.message), preview.status === 'ready' && (\n  /* Preview output is already HTML-escaped by the TokenRenderer.\n     We set dangerouslySetInnerHTML here intentionally for rendered preview only.\n     The template source is stored as plain text in the bound field — not rendered in UCI. */\n  React.createElement(\"div\", {\n    dangerouslySetInnerHTML: {\n      __html: preview.html\n    }\n  }))));\n};\nexports.PreviewPane = PreviewPane;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/components/PreviewPane.tsx?\n}");

/***/ },

/***/ "./src/hooks/useEditorCursor.ts"
/*!**************************************!*\
  !*** ./src/hooks/useEditorCursor.ts ***!
  \**************************************/
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
eval("{\n\n/**\n * useEditorCursor.ts\n * Custom hook encapsulating all Selection / Range API operations\n * for the contenteditable editor surface.\n */\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.useEditorCursor = useEditorCursor;\nvar react_1 = __webpack_require__(/*! react */ \"./node_modules/react/index.js\");\nfunction useEditorCursor(surfaceRef) {\n  var savedRange = (0, react_1.useRef)(null);\n  var saveSelection = (0, react_1.useCallback)(() => {\n    var sel = window.getSelection();\n    if (!sel || sel.rangeCount === 0) return;\n    savedRange.current = sel.getRangeAt(0).cloneRange();\n  }, []);\n  var restoreSelection = (0, react_1.useCallback)(() => {\n    var range = savedRange.current;\n    if (!range) return;\n    var sel = window.getSelection();\n    if (!sel) return;\n    sel.removeAllRanges();\n    sel.addRange(range);\n  }, []);\n  var insertTextAtCursor = (0, react_1.useCallback)(text => {\n    var sel = window.getSelection();\n    if (!sel || sel.rangeCount === 0) return;\n    if (!surfaceRef.current) return;\n    var range = sel.getRangeAt(0);\n    if (!surfaceRef.current.contains(range.startContainer)) return;\n    range.deleteContents();\n    var node = document.createTextNode(text);\n    range.insertNode(node);\n    var after = document.createRange();\n    after.setStartAfter(node);\n    after.setEndAfter(node);\n    sel.removeAllRanges();\n    sel.addRange(after);\n  }, [surfaceRef]);\n  var replaceRangeWithText = (0, react_1.useCallback)((range, text) => {\n    range.deleteContents();\n    var node = document.createTextNode(text);\n    range.insertNode(node);\n    var sel = window.getSelection();\n    if (!sel) return;\n    var after = document.createRange();\n    after.setStartAfter(node);\n    after.setEndAfter(node);\n    sel.removeAllRanges();\n    sel.addRange(after);\n  }, []);\n  var getTriggerRangeForDot = (0, react_1.useCallback)(() => {\n    var _a;\n    var sel = window.getSelection();\n    if (!sel || sel.rangeCount === 0) return null;\n    var original = sel.getRangeAt(0);\n    if (original.startOffset === 0) return null;\n    var range = original.cloneRange();\n    range.setStart(range.endContainer, range.endOffset - 1);\n    range.setEnd(range.endContainer, range.endOffset);\n    // Confirm the character at this range is actually \".\"\n    var fragment = range.cloneContents();\n    var char = (_a = fragment.textContent) !== null && _a !== void 0 ? _a : '';\n    if (char !== '.') return null;\n    return range;\n  }, []);\n  return {\n    saveSelection,\n    restoreSelection,\n    insertTextAtCursor,\n    replaceRangeWithText,\n    getTriggerRangeForDot\n  };\n}\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/hooks/useEditorCursor.ts?\n}");

/***/ },

/***/ "./src/hooks/useKeyboardNav.ts"
/*!*************************************!*\
  !*** ./src/hooks/useKeyboardNav.ts ***!
  \*************************************/
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
eval("{\n\n/**\n * useKeyboardNav.ts\n * Reusable keyboard navigation hook for the field selector popup.\n * Handles ArrowUp / ArrowDown selection, Enter confirm, Escape close,\n * ArrowRight drill-in, ArrowLeft drill-out.\n */\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.useKeyboardNav = useKeyboardNav;\nvar react_1 = __webpack_require__(/*! react */ \"./node_modules/react/index.js\");\nfunction useKeyboardNav(options) {\n  var [selectedIndex, setSelectedIndex] = (0, react_1.useState)(0);\n  var handleKeyDown = (0, react_1.useCallback)(event => {\n    var {\n      items,\n      onSelect,\n      onDrillIn,\n      onDrillOut,\n      onClose,\n      canDrillIn\n    } = options;\n    switch (event.key) {\n      case 'ArrowDown':\n        {\n          event.preventDefault();\n          setSelectedIndex(i => Math.min(i + 1, items.length - 1));\n          break;\n        }\n      case 'ArrowUp':\n        {\n          event.preventDefault();\n          setSelectedIndex(i => Math.max(i - 1, 0));\n          break;\n        }\n      case 'Enter':\n      case 'Tab':\n        {\n          event.preventDefault();\n          var item = items[selectedIndex];\n          if (item !== undefined) onSelect(item);\n          break;\n        }\n      case 'ArrowRight':\n        {\n          event.preventDefault();\n          var _item = items[selectedIndex];\n          if (_item !== undefined && canDrillIn(_item)) onDrillIn(_item);\n          break;\n        }\n      case 'ArrowLeft':\n        {\n          event.preventDefault();\n          onDrillOut();\n          break;\n        }\n      case 'Escape':\n        {\n          event.preventDefault();\n          onClose();\n          break;\n        }\n    }\n  }, [options, selectedIndex]);\n  return {\n    selectedIndex,\n    setSelectedIndex,\n    handleKeyDown\n  };\n}\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/hooks/useKeyboardNav.ts?\n}");

/***/ },

/***/ "./src/hooks/useMetadata.ts"
/*!**********************************!*\
  !*** ./src/hooks/useMetadata.ts ***!
  \**********************************/
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
eval("{\n\n/**\n * useMetadata.ts\n * React hook that wraps MetadataService for use in components.\n * Handles loading state and errors so components stay clean.\n */\nvar __awaiter = this && this.__awaiter || function (thisArg, _arguments, P, generator) {\n  function adopt(value) {\n    return value instanceof P ? value : new P(function (resolve) {\n      resolve(value);\n    });\n  }\n  return new (P || (P = Promise))(function (resolve, reject) {\n    function fulfilled(value) {\n      try {\n        step(generator.next(value));\n      } catch (e) {\n        reject(e);\n      }\n    }\n    function rejected(value) {\n      try {\n        step(generator[\"throw\"](value));\n      } catch (e) {\n        reject(e);\n      }\n    }\n    function step(result) {\n      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);\n    }\n    step((generator = generator.apply(thisArg, _arguments || [])).next());\n  });\n};\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.useMetadata = useMetadata;\nvar react_1 = __webpack_require__(/*! react */ \"./node_modules/react/index.js\");\nfunction useMetadata(entityLogicalName, service) {\n  var [bundle, setBundle] = (0, react_1.useState)(null);\n  var [loading, setLoading] = (0, react_1.useState)(false);\n  var [error, setError] = (0, react_1.useState)(null);\n  var cancelRef = (0, react_1.useRef)(false);\n  var load = (0, react_1.useCallback)(logicalName => __awaiter(this, void 0, void 0, function* () {\n    cancelRef.current = false;\n    setLoading(true);\n    setError(null);\n    setBundle(null);\n    try {\n      var [attributes, oneToMany, manyToOne] = yield Promise.all([service.getAttributes(logicalName), service.getOneToMany(logicalName), service.getManyToOne(logicalName)]);\n      if (!cancelRef.current) {\n        setBundle({\n          attributes,\n          oneToMany,\n          manyToOne\n        });\n      }\n    } catch (err) {\n      if (!cancelRef.current) {\n        setError(err instanceof Error ? err.message : 'metadata_load_failed');\n      }\n    } finally {\n      if (!cancelRef.current) {\n        setLoading(false);\n      }\n    }\n  }), [service]);\n  (0, react_1.useEffect)(() => {\n    if (!entityLogicalName) return;\n    load(entityLogicalName);\n    return () => {\n      cancelRef.current = true;\n    };\n  }, [entityLogicalName, load]);\n  return {\n    bundle,\n    loading,\n    error\n  };\n}\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/hooks/useMetadata.ts?\n}");

/***/ },

/***/ "./src/services/Logger.ts"
/*!********************************!*\
  !*** ./src/services/Logger.ts ***!
  \********************************/
(__unused_webpack_module, exports) {

"use strict";
eval("{\n\n/**\n * Logger.ts\n * Structured logger wrapper. In production (UCI context) delegates to\n * context.reporting; in tests / preview host uses console.\n * No console.log in committed code — structured entries only.\n */\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.Logger = void 0;\nfunction defaultSink(entry) {\n  var _a, _b;\n  var msg = \"[\".concat(entry.level.toUpperCase(), \"] \").concat(entry.component, \"::\").concat(entry.event);\n  if (entry.level === 'error') {\n    console.error(msg, (_a = entry.data) !== null && _a !== void 0 ? _a : {});\n  } else if (entry.level === 'warn') {\n    console.warn(msg, (_b = entry.data) !== null && _b !== void 0 ? _b : {});\n  }\n  // info logs are suppressed in default sink — wire a real sink in production\n}\nclass Logger {\n  constructor(component) {\n    var sink = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : defaultSink;\n    this.component = component;\n    this.sink = sink;\n  }\n  info(event, data) {\n    this.sink({\n      level: 'info',\n      component: this.component,\n      event,\n      data\n    });\n  }\n  warn(event, data) {\n    this.sink({\n      level: 'warn',\n      component: this.component,\n      event,\n      data\n    });\n  }\n  error(event, data) {\n    this.sink({\n      level: 'error',\n      component: this.component,\n      event,\n      data\n    });\n  }\n  /** Returns a child logger scoped to a sub-component */\n  child(subComponent) {\n    return new Logger(\"\".concat(this.component, \"::\").concat(subComponent), this.sink);\n  }\n}\nexports.Logger = Logger;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/services/Logger.ts?\n}");

/***/ },

/***/ "./src/services/MetadataCache.ts"
/*!***************************************!*\
  !*** ./src/services/MetadataCache.ts ***!
  \***************************************/
(__unused_webpack_module, exports) {

"use strict";
eval("{\n\n/**\n * MetadataCache.ts\n * Two-tier cache: in-memory LRU (hot) + sessionStorage (warm, 30-min TTL).\n * Keyed by \"maqsad.emaileditor.v1.<orgHash>.<key>\" to isolate across envs.\n */\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.MetadataCache = void 0;\nvar SESSION_KEY_PREFIX = 'maqsad.emaileditor.v1';\nvar DEFAULT_TTL_MS = 30 * 60 * 1000;\nvar MAX_MEMORY_ENTRIES = 200;\nclass MetadataCache {\n  constructor(storage) {\n    var orgUrl = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '';\n    this.storage = storage;\n    this.memory = new Map();\n    this.orgHash = this.hashString(orgUrl);\n  }\n  get(key) {\n    var now = Date.now();\n    // Memory layer first\n    var memEntry = this.memory.get(key);\n    if (memEntry) {\n      if (memEntry.expiresAt > now) return memEntry.value;\n      this.memory.delete(key);\n    }\n    // Session storage fallback\n    var sessionValue = this.readFromStorage(key);\n    if (sessionValue !== null) {\n      this.writeToMemory(key, sessionValue, DEFAULT_TTL_MS);\n      return sessionValue;\n    }\n    return null;\n  }\n  set(key, value) {\n    var ttlMs = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : DEFAULT_TTL_MS;\n    this.writeToMemory(key, value, ttlMs);\n    this.writeToStorage(key, value, ttlMs);\n  }\n  invalidate(key) {\n    this.memory.delete(key);\n    try {\n      this.storage.removeItem(this.storageKey(key));\n    } catch (_a) {\n      // Storage may be unavailable in some embedded contexts\n    }\n  }\n  clear() {\n    this.memory.clear();\n    // Only clear keys belonging to this instance\n    var prefix = this.storageKey('');\n    var keysToRemove = [];\n    for (var i = 0; i < this.storage.length; i++) {\n      var k = this.storage.key(i);\n      if (k && k.startsWith(prefix)) keysToRemove.push(k);\n    }\n    keysToRemove.forEach(k => this.storage.removeItem(k));\n  }\n  // ── Private ───────────────────────────────────────────────────────────────\n  writeToMemory(key, value, ttlMs) {\n    if (this.memory.size >= MAX_MEMORY_ENTRIES) {\n      // Evict the oldest entry (Map preserves insertion order)\n      var firstKey = this.memory.keys().next().value;\n      if (firstKey !== undefined) this.memory.delete(firstKey);\n    }\n    this.memory.set(key, {\n      value,\n      expiresAt: Date.now() + ttlMs\n    });\n  }\n  writeToStorage(key, value, ttlMs) {\n    try {\n      var entry = {\n        value,\n        expiresAt: Date.now() + ttlMs\n      };\n      this.storage.setItem(this.storageKey(key), JSON.stringify(entry));\n    } catch (_a) {\n      // Quota exceeded or storage unavailable — memory cache is still warm\n    }\n  }\n  readFromStorage(key) {\n    try {\n      var raw = this.storage.getItem(this.storageKey(key));\n      if (!raw) return null;\n      var entry = JSON.parse(raw);\n      if (entry.expiresAt <= Date.now()) {\n        this.storage.removeItem(this.storageKey(key));\n        return null;\n      }\n      return entry.value;\n    } catch (_a) {\n      return null;\n    }\n  }\n  storageKey(key) {\n    return \"\".concat(SESSION_KEY_PREFIX, \".\").concat(this.orgHash, \".\").concat(key);\n  }\n  hashString(str) {\n    var hash = 0;\n    for (var i = 0; i < str.length; i++) {\n      hash = (hash << 5) - hash + str.charCodeAt(i) | 0;\n    }\n    return Math.abs(hash).toString(36);\n  }\n}\nexports.MetadataCache = MetadataCache;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/services/MetadataCache.ts?\n}");

/***/ },

/***/ "./src/services/MetadataService.ts"
/*!*****************************************!*\
  !*** ./src/services/MetadataService.ts ***!
  \*****************************************/
(__unused_webpack_module, exports) {

"use strict";
eval("{\n\n/**\n * MetadataService.ts\n * Loads entity, attribute, and relationship metadata from the Dataverse\n * Web API v9.2. Uses direct fetch (not context.webAPI) because EntityDefinitions\n * traversal is not exposed through the PCF WebApi interface.\n *\n * In-flight deduplication prevents parallel popup opens from firing duplicate XHRs.\n */\nvar __awaiter = this && this.__awaiter || function (thisArg, _arguments, P, generator) {\n  function adopt(value) {\n    return value instanceof P ? value : new P(function (resolve) {\n      resolve(value);\n    });\n  }\n  return new (P || (P = Promise))(function (resolve, reject) {\n    function fulfilled(value) {\n      try {\n        step(generator.next(value));\n      } catch (e) {\n        reject(e);\n      }\n    }\n    function rejected(value) {\n      try {\n        step(generator[\"throw\"](value));\n      } catch (e) {\n        reject(e);\n      }\n    }\n    function step(result) {\n      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);\n    }\n    step((generator = generator.apply(thisArg, _arguments || [])).next());\n  });\n};\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.MetadataService = exports.MetadataError = void 0;\nvar API_VERSION = 'v9.2';\nvar CACHE_TTL_MS = 30 * 60 * 1000;\nclass MetadataError extends Error {\n  constructor(code, httpStatus, url) {\n    super(code);\n    this.httpStatus = httpStatus;\n    this.url = url;\n    this.name = 'MetadataError';\n  }\n}\nexports.MetadataError = MetadataError;\nclass MetadataService {\n  constructor(options) {\n    this.inflight = new Map();\n    this.options = options;\n    this.log = options.logger.child('MetadataService');\n  }\n  getEntity(logicalName) {\n    return __awaiter(this, void 0, void 0, function* () {\n      var key = \"entity:\".concat(logicalName);\n      var cached = this.options.cache.get(key);\n      if (cached) return cached;\n      return this.dedupe(key, () => __awaiter(this, void 0, void 0, function* () {\n        var url = this.buildUrl(\"EntityDefinitions(LogicalName='\".concat(encodeURIComponent(logicalName), \"')\") + \"?$select=LogicalName,SchemaName,DisplayName,PrimaryIdAttribute,\" + \"PrimaryNameAttribute,ObjectTypeCode,EntitySetName\");\n        var data = yield this.fetchJson(url);\n        this.options.cache.set(key, data, CACHE_TTL_MS);\n        return data;\n      }));\n    });\n  }\n  getAttributes(logicalName) {\n    return __awaiter(this, void 0, void 0, function* () {\n      var key = \"attrs:\".concat(logicalName);\n      var cached = this.options.cache.get(key);\n      if (cached) return cached;\n      return this.dedupe(key, () => __awaiter(this, void 0, void 0, function* () {\n        var url = this.buildUrl(\"EntityDefinitions(LogicalName='\".concat(encodeURIComponent(logicalName), \"')/Attributes\") + \"?$select=LogicalName,SchemaName,DisplayName,AttributeType,IsValidForRead,IsCustomAttribute\" + \"&$filter=IsValidForRead eq true\");\n        var data = yield this.fetchJson(url);\n        this.options.cache.set(key, data.value, CACHE_TTL_MS);\n        return data.value;\n      }));\n    });\n  }\n  getOneToMany(logicalName) {\n    return __awaiter(this, void 0, void 0, function* () {\n      var key = \"1n:\".concat(logicalName);\n      var cached = this.options.cache.get(key);\n      if (cached) return cached;\n      return this.dedupe(key, () => __awaiter(this, void 0, void 0, function* () {\n        var url = this.buildUrl(\"EntityDefinitions(LogicalName='\".concat(encodeURIComponent(logicalName), \"')/OneToManyRelationships\") + \"?$select=SchemaName,ReferencedEntity,ReferencedAttribute,ReferencingEntity,\" + \"ReferencingAttribute,ReferencedEntityNavigationPropertyName,\" + \"ReferencingEntityNavigationPropertyName\");\n        var data = yield this.fetchJson(url);\n        this.options.cache.set(key, data.value, CACHE_TTL_MS);\n        return data.value;\n      }));\n    });\n  }\n  getManyToOne(logicalName) {\n    return __awaiter(this, void 0, void 0, function* () {\n      var key = \"n1:\".concat(logicalName);\n      var cached = this.options.cache.get(key);\n      if (cached) return cached;\n      return this.dedupe(key, () => __awaiter(this, void 0, void 0, function* () {\n        var url = this.buildUrl(\"EntityDefinitions(LogicalName='\".concat(encodeURIComponent(logicalName), \"')/ManyToOneRelationships\") + \"?$select=SchemaName,ReferencedEntity,ReferencedAttribute,ReferencingEntity,\" + \"ReferencingAttribute,ReferencingEntityNavigationPropertyName\");\n        var data = yield this.fetchJson(url);\n        this.options.cache.set(key, data.value, CACHE_TTL_MS);\n        return data.value;\n      }));\n    });\n  }\n  /**\n   * Fetches a single record for the preview pane.\n   * @param entitySetName  Plural entity set name (e.g. \"contacts\")\n   * @param recordId       GUID of the record\n   * @param select         Field logical names to include\n   * @param expand         Navigation properties to expand\n   */\n  fetchRecord(entitySetName, recordId, select, expand) {\n    return __awaiter(this, void 0, void 0, function* () {\n      var params = [];\n      if (select.length > 0) params.push(\"$select=\".concat(select.join(',')));\n      if (expand.length > 0) params.push(\"$expand=\".concat(expand.join(',')));\n      var query = params.length > 0 ? \"?\".concat(params.join('&')) : '';\n      var url = this.buildUrl(\"\".concat(entitySetName, \"(\").concat(recordId, \")\").concat(query));\n      return this.fetchJson(url);\n    });\n  }\n  dispose() {\n    this.inflight.clear();\n  }\n  // ── Private ───────────────────────────────────────────────────────────────\n  buildUrl(path) {\n    var base = this.options.clientUrl.replace(/\\/$/, '');\n    return \"\".concat(base, \"/api/data/\").concat(API_VERSION, \"/\").concat(path);\n  }\n  dedupe(key, loader) {\n    return __awaiter(this, void 0, void 0, function* () {\n      var existing = this.inflight.get(key);\n      if (existing) return existing;\n      var promise = loader().finally(() => this.inflight.delete(key));\n      this.inflight.set(key, promise);\n      return promise;\n    });\n  }\n  fetchJson(url) {\n    return __awaiter(this, void 0, void 0, function* () {\n      var response;\n      try {\n        response = yield fetch(url, {\n          method: 'GET',\n          credentials: 'include',\n          headers: {\n            'Accept': 'application/json',\n            'OData-MaxVersion': '4.0',\n            'OData-Version': '4.0',\n            'Prefer': 'odata.include-annotations=\"*\"'\n          }\n        });\n      } catch (networkError) {\n        this.log.error('metadata_network_error', {\n          url,\n          error: String(networkError)\n        });\n        throw new MetadataError('network_error', 0, url);\n      }\n      if (!response.ok) {\n        var body = yield response.text().catch(() => '');\n        this.log.error('metadata_http_error', {\n          url,\n          status: response.status,\n          body\n        });\n        throw new MetadataError('http_error', response.status, url);\n      }\n      return response.json();\n    });\n  }\n}\nexports.MetadataService = MetadataService;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/services/MetadataService.ts?\n}");

/***/ },

/***/ "./src/token/Formatters.ts"
/*!*********************************!*\
  !*** ./src/token/Formatters.ts ***!
  \*********************************/
(__unused_webpack_module, exports) {

"use strict";
eval("{\n\n/**\n * Formatters.ts\n * Pure formatting functions for token modifier resolution.\n * All use Intl APIs — no eval, no date libraries.\n */\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.Formatters = void 0;\nclass Formatters {\n  /**\n   * Format a value using a format spec string: \"date:dd MMM yyyy\", \"money:en-PK:PKR\", etc.\n   * Returns the formatted string, or the original value stringified if spec is unrecognised.\n   */\n  applyFormat(value, spec, locale, currency) {\n    if (value === null || value === undefined) return '';\n    var colonIdx = spec.indexOf(':');\n    if (colonIdx === -1) return String(value);\n    var specType = spec.slice(0, colonIdx);\n    var specArg = spec.slice(colonIdx + 1);\n    switch (specType) {\n      case 'date':\n        return this.formatDate(value, specArg, locale);\n      case 'money':\n        return this.formatMoney(value, specArg, locale, currency);\n      case 'number':\n        return this.formatNumber(value, specArg, locale);\n      default:\n        return String(value);\n    }\n  }\n  applyCase(value, kind) {\n    switch (kind) {\n      case 'upper':\n        return value.toUpperCase();\n      case 'lower':\n        return value.toLowerCase();\n      case 'title':\n        return value.replace(/\\b\\w/g, ch => ch.toUpperCase());\n    }\n  }\n  escapeHtml(value) {\n    return value.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/\"/g, '&quot;').replace(/'/g, '&#39;');\n  }\n  // ── Private helpers ──────────────────────────────────────────────────────\n  formatDate(value, pattern, locale) {\n    var date = this.toDate(value);\n    if (!date) return String(value);\n    // Map common pattern tokens to Intl options\n    var options = this.patternToIntlOptions(pattern);\n    try {\n      return new Intl.DateTimeFormat(locale || 'en', options).format(date);\n    } catch (_a) {\n      return date.toLocaleDateString();\n    }\n  }\n  formatMoney(value, specArg, defaultLocale, defaultCurrency) {\n    var num = parseFloat(String(value));\n    if (isNaN(num)) return String(value);\n    // specArg format: \"en-PK:PKR\" or just \"PKR\"\n    var parts = specArg.split(':');\n    var locale = parts.length >= 2 ? parts[0] : defaultLocale;\n    var currCode = parts.length >= 2 ? parts[1] : parts[0] || defaultCurrency;\n    try {\n      return new Intl.NumberFormat(locale || 'en', {\n        style: 'currency',\n        currency: currCode || 'USD',\n        minimumFractionDigits: 2\n      }).format(num);\n    } catch (_a) {\n      return num.toFixed(2);\n    }\n  }\n  formatNumber(value, pattern, locale) {\n    var num = parseFloat(String(value));\n    if (isNaN(num)) return String(value);\n    // pattern: \"0.00\" → 2 fraction digits\n    var fractionMatch = pattern.match(/\\.(\\d+)/);\n    var fractionDigits = fractionMatch ? fractionMatch[1].length : 0;\n    try {\n      return new Intl.NumberFormat(locale || 'en', {\n        minimumFractionDigits: fractionDigits,\n        maximumFractionDigits: fractionDigits\n      }).format(num);\n    } catch (_a) {\n      return num.toFixed(fractionDigits);\n    }\n  }\n  toDate(value) {\n    if (value instanceof Date) return isNaN(value.getTime()) ? null : value;\n    if (typeof value === 'string' || typeof value === 'number') {\n      var d = new Date(value);\n      return isNaN(d.getTime()) ? null : d;\n    }\n    return null;\n  }\n  /**\n   * Very lightweight pattern → Intl option mapper.\n   * Supports: yyyy/yy, MMM/MM/M, dd/d, HH/H, mm, ss\n   */\n  patternToIntlOptions(pattern) {\n    var options = {};\n    if (/yyyy/.test(pattern)) options.year = 'numeric';else if (/yy/.test(pattern)) options.year = '2-digit';\n    if (/MMM/.test(pattern)) options.month = 'short';else if (/MMMM/.test(pattern)) options.month = 'long';else if (/MM|M/.test(pattern)) options.month = '2-digit';\n    if (/dd|d/.test(pattern)) options.day = '2-digit';\n    if (/HH|H/.test(pattern)) options.hour = '2-digit';\n    if (/mm/.test(pattern)) options.minute = '2-digit';\n    if (/ss/.test(pattern)) options.second = '2-digit';\n    return options;\n  }\n}\nexports.Formatters = Formatters;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/token/Formatters.ts?\n}");

/***/ },

/***/ "./src/token/TokenEngine.ts"
/*!**********************************!*\
  !*** ./src/token/TokenEngine.ts ***!
  \**********************************/
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
eval("{\n\n/**\n * TokenEngine.ts\n * Public facade over TokenLexer → TokenParser → TokenRenderer.\n * This is the only class callers should import for token operations.\n */\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.TokenEngine = void 0;\nvar Formatters_1 = __webpack_require__(/*! ./Formatters */ \"./src/token/Formatters.ts\");\nvar TokenLexer_1 = __webpack_require__(/*! ./TokenLexer */ \"./src/token/TokenLexer.ts\");\nvar TokenParser_1 = __webpack_require__(/*! ./TokenParser */ \"./src/token/TokenParser.ts\");\nvar TokenRenderer_1 = __webpack_require__(/*! ./TokenRenderer */ \"./src/token/TokenRenderer.ts\");\nclass TokenEngine {\n  constructor(logger) {\n    this.logger = logger;\n    this.lexer = new TokenLexer_1.TokenLexer();\n    this.parser = new TokenParser_1.TokenParser();\n    this.renderer = new TokenRenderer_1.TokenRenderer(new Formatters_1.Formatters(), logger);\n  }\n  /**\n   * Parse a raw template string into an AST.\n   * Returns a ParseResult — never throws on invalid input.\n   */\n  parse(source) {\n    var tokens = this.lexer.lex(source);\n    return this.parser.parse(tokens);\n  }\n  /**\n   * Render a pre-parsed AST against a record context.\n   * Never throws — missing paths resolve to empty string.\n   */\n  render(ast, context) {\n    return this.renderer.render(ast, context);\n  }\n  /**\n   * Convenience: parse + render in one call.\n   * On parse failure, logs warnings and returns the original source unchanged.\n   */\n  renderSource(source, context) {\n    var result = this.parse(source);\n    if (!result.ok) {\n      this.logger.warn('token_parse_failed', {\n        errors: result.errors\n      });\n      return source;\n    }\n    return this.render(result.ast, context);\n  }\n  /**\n   * Validate a template without rendering it.\n   * Returns a list of error messages, or an empty array if valid.\n   */\n  validate(source) {\n    var result = this.parse(source);\n    if (result.ok) return [];\n    return result.errors.map(e => \"Position \".concat(e.position, \": \").concat(e.message));\n  }\n}\nexports.TokenEngine = TokenEngine;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/token/TokenEngine.ts?\n}");

/***/ },

/***/ "./src/token/TokenLexer.ts"
/*!*********************************!*\
  !*** ./src/token/TokenLexer.ts ***!
  \*********************************/
(__unused_webpack_module, exports) {

"use strict";
eval("{\n\n/**\n * TokenLexer.ts\n * Converts a raw template string into a flat token stream.\n * No eval, no Function constructor — pure character scanning.\n */\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.TokenLexer = void 0;\nclass TokenLexer {\n  lex(source) {\n    var tokens = [];\n    var pos = 0;\n    while (pos < source.length) {\n      // Block tags and mustache openers are matched in text mode.\n      // Everything else outside {{ }} is plain TEXT.\n      if (this.matchesAt(source, pos, '{{#each')) {\n        tokens.push({\n          kind: 'BLOCK_EACH',\n          value: '{{#each',\n          position: pos\n        });\n        pos = this.lexInsideMustache(source, pos + 7, tokens);\n      } else if (this.matchesAt(source, pos, '{{#if')) {\n        tokens.push({\n          kind: 'BLOCK_IF',\n          value: '{{#if',\n          position: pos\n        });\n        pos = this.lexInsideMustache(source, pos + 5, tokens);\n      } else if (this.matchesAt(source, pos, '{{else}}')) {\n        tokens.push({\n          kind: 'BLOCK_ELSE',\n          value: '{{else}}',\n          position: pos\n        });\n        pos += 8;\n      } else if (this.matchesAt(source, pos, '{{/each}}')) {\n        tokens.push({\n          kind: 'BLOCK_END_EACH',\n          value: '{{/each}}',\n          position: pos\n        });\n        pos += 9;\n      } else if (this.matchesAt(source, pos, '{{/if}}')) {\n        tokens.push({\n          kind: 'BLOCK_END_IF',\n          value: '{{/if}}',\n          position: pos\n        });\n        pos += 7;\n      } else if (this.matchesAt(source, pos, '{{')) {\n        tokens.push({\n          kind: 'OPEN_MUSTACHE',\n          value: '{{',\n          position: pos\n        });\n        pos = this.lexInsideMustache(source, pos + 2, tokens);\n      } else {\n        // Text mode: accumulate everything until the next {{ (or end of input).\n        var start = pos;\n        while (pos < source.length && !this.matchesAt(source, pos, '{{')) {\n          pos += 1;\n        }\n        if (pos > start) {\n          tokens.push({\n            kind: 'TEXT',\n            value: source.slice(start, pos),\n            position: start\n          });\n        }\n      }\n    }\n    tokens.push({\n      kind: 'EOF',\n      value: '',\n      position: pos\n    });\n    return tokens;\n  }\n  /**\n   * Lexes fine-grained tokens from inside a {{ … }} expression.\n   * Skips whitespace, emits IDENTIFIER / DOT / PIPE / etc., then\n   * emits CLOSE_MUSTACHE on }} and returns the new position.\n   */\n  lexInsideMustache(source, pos, tokens) {\n    var _a;\n    while (pos < source.length) {\n      // Whitespace is insignificant inside mustaches\n      if (/\\s/.test(source[pos])) {\n        pos++;\n        continue;\n      }\n      if (this.matchesAt(source, pos, '}}')) {\n        tokens.push({\n          kind: 'CLOSE_MUSTACHE',\n          value: '}}',\n          position: pos\n        });\n        return pos + 2;\n      }\n      if (source[pos] === '|') {\n        tokens.push({\n          kind: 'PIPE',\n          value: '|',\n          position: pos\n        });\n        pos++;\n      } else if (source[pos] === '.') {\n        tokens.push({\n          kind: 'DOT',\n          value: '.',\n          position: pos\n        });\n        pos++;\n      } else if (source[pos] === ':') {\n        tokens.push({\n          kind: 'COLON',\n          value: ':',\n          position: pos\n        });\n        pos++;\n      } else if (this.isCompareOpStart(source, pos)) {\n        var {\n          value,\n          length\n        } = this.readCompareOp(source, pos);\n        tokens.push({\n          kind: 'COMPARE_OP',\n          value,\n          position: pos\n        });\n        pos += length;\n      } else if (source[pos] === '\"' || source[pos] === \"'\") {\n        var {\n          value: _value,\n          length: _length\n        } = this.readStringLiteral(source, pos);\n        tokens.push({\n          kind: 'STRING_LITERAL',\n          value: _value,\n          position: pos\n        });\n        pos += _length;\n      } else if (this.isDigit(source[pos]) || source[pos] === '-' && this.isDigit((_a = source[pos + 1]) !== null && _a !== void 0 ? _a : '')) {\n        var {\n          value: _value2,\n          length: _length2\n        } = this.readNumber(source, pos);\n        tokens.push({\n          kind: 'NUMBER_LITERAL',\n          value: _value2,\n          position: pos\n        });\n        pos += _length2;\n      } else if (this.isIdentStart(source[pos])) {\n        var {\n          value: _value3,\n          length: _length3\n        } = this.readIdentifier(source, pos);\n        tokens.push({\n          kind: this.classifyIdentifier(_value3),\n          value: _value3,\n          position: pos\n        });\n        pos += _length3;\n      } else {\n        pos++; // skip unrecognised character inside mustache\n      }\n    }\n    // Reached end of input without closing }} — emit no CLOSE_MUSTACHE\n    return pos;\n  }\n  matchesAt(source, pos, needle) {\n    return source.startsWith(needle, pos);\n  }\n  isIdentStart(ch) {\n    return /[a-zA-Z_]/.test(ch);\n  }\n  isDigit(ch) {\n    return /[0-9]/.test(ch);\n  }\n  isCompareOpStart(source, pos) {\n    var ch = source[pos];\n    return ch === '=' || ch === '!' || ch === '<' || ch === '>';\n  }\n  readCompareOp(source, pos) {\n    var twoChar = source.slice(pos, pos + 2);\n    if (twoChar === '==' || twoChar === '!=' || twoChar === '>=' || twoChar === '<=') {\n      return {\n        value: twoChar,\n        length: 2\n      };\n    }\n    return {\n      value: source[pos],\n      length: 1\n    };\n  }\n  readStringLiteral(source, pos) {\n    var quote = source[pos];\n    var i = pos + 1;\n    while (i < source.length && source[i] !== quote) {\n      if (source[i] === '\\\\') i += 1; // skip escaped char\n      i += 1;\n    }\n    var raw = source.slice(pos + 1, i);\n    return {\n      value: raw,\n      length: i - pos + 1\n    };\n  }\n  readNumber(source, pos) {\n    var i = pos;\n    if (source[i] === '-') i += 1;\n    while (i < source.length && (this.isDigit(source[i]) || source[i] === '.')) {\n      i += 1;\n    }\n    return {\n      value: source.slice(pos, i),\n      length: i - pos\n    };\n  }\n  readIdentifier(source, pos) {\n    var i = pos;\n    while (i < source.length && /[a-zA-Z0-9_]/.test(source[i])) {\n      i += 1;\n    }\n    return {\n      value: source.slice(pos, i),\n      length: i - pos\n    };\n  }\n  classifyIdentifier(value) {\n    if (value === 'true' || value === 'false') return 'BOOL_LITERAL';\n    if (value === 'null') return 'NULL_LITERAL';\n    return 'IDENTIFIER';\n  }\n}\nexports.TokenLexer = TokenLexer;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/token/TokenLexer.ts?\n}");

/***/ },

/***/ "./src/token/TokenParser.ts"
/*!**********************************!*\
  !*** ./src/token/TokenParser.ts ***!
  \**********************************/
(__unused_webpack_module, exports) {

"use strict";
eval("{\n\n/**\n * TokenParser.ts\n * Converts a flat LexToken stream (from TokenLexer) into a TemplateAst.\n * Hand-written recursive descent — no eval, no dynamic code.\n */\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.COMPARISON_OPERATORS = exports.TokenParser = void 0;\nvar COMPARISON_OPERATORS = ['==', '!=', '>=', '<=', '>', '<'];\nexports.COMPARISON_OPERATORS = COMPARISON_OPERATORS;\nclass TokenParser {\n  constructor() {\n    this.tokens = [];\n    this.pos = 0;\n    this.errors = [];\n  }\n  parse(tokens) {\n    this.tokens = tokens;\n    this.pos = 0;\n    this.errors = [];\n    var ast = this.parseTemplate('root');\n    if (this.errors.length > 0) {\n      return {\n        ok: false,\n        errors: this.errors\n      };\n    }\n    return {\n      ok: true,\n      ast\n    };\n  }\n  // ── Template (sequence of nodes until a stop token) ─────────────────────\n  parseTemplate(stopAt) {\n    var nodes = [];\n    while (!this.isAtEnd()) {\n      var current = this.peek();\n      if (current.kind === 'EOF') break;\n      if (stopAt === 'else' && current.kind === 'BLOCK_ELSE') break;\n      if (stopAt === 'each-end' && current.kind === 'BLOCK_END_EACH') break;\n      if (stopAt === 'if-end' && (current.kind === 'BLOCK_END_IF' || current.kind === 'BLOCK_ELSE')) break;\n      if (current.kind === 'TEXT') {\n        nodes.push(this.parseText());\n      } else if (current.kind === 'BLOCK_EACH') {\n        nodes.push(this.parseEach());\n      } else if (current.kind === 'BLOCK_IF') {\n        nodes.push(this.parseIf());\n      } else if (current.kind === 'OPEN_MUSTACHE') {\n        nodes.push(this.parseMustache());\n      } else if (current.kind === 'BLOCK_END_EACH' || current.kind === 'BLOCK_END_IF' || current.kind === 'BLOCK_ELSE') {\n        // Unexpected end/else tag — report and skip\n        this.addError(\"Unexpected \".concat(current.kind, \" at position \").concat(current.position), current.position);\n        this.advance();\n      } else {\n        // Skip whitespace-only tokens that leaked past text accumulation\n        this.advance();\n      }\n    }\n    return nodes;\n  }\n  // ── Text node ────────────────────────────────────────────────────────────\n  parseText() {\n    var token = this.consume('TEXT');\n    return {\n      type: 'text',\n      value: token.value\n    };\n  }\n  // ── Mustache: {{ path modifier* }} ───────────────────────────────────────\n  parseMustache() {\n    this.consume('OPEN_MUSTACHE');\n    var path = this.parsePath();\n    var modifiers = [];\n    while (this.peek().kind === 'PIPE') {\n      this.consume('PIPE');\n      var mod = this.parseModifier();\n      if (mod) modifiers.push(mod);\n    }\n    this.consumeOrError('CLOSE_MUSTACHE', 'Expected }} to close mustache');\n    return {\n      type: 'mustache',\n      path,\n      modifiers\n    };\n  }\n  // ── Each block: {{#each path}} body {{/each}} ────────────────────────────\n  parseEach() {\n    var start = this.peek().position;\n    this.consume('BLOCK_EACH');\n    this.skipWhitespace();\n    var path = this.parsePath();\n    this.consumeOrError('CLOSE_MUSTACHE', 'Expected }} after {{#each path');\n    var body = this.parseTemplate('each-end');\n    if (this.peek().kind === 'BLOCK_END_EACH') {\n      this.consume('BLOCK_END_EACH');\n    } else {\n      this.addError('Unclosed {{#each}} block', start);\n    }\n    return {\n      type: 'each',\n      path,\n      body\n    };\n  }\n  // ── If block: {{#if expr}} then {{else}} else {{/if}} ────────────────────\n  parseIf() {\n    var start = this.peek().position;\n    this.consume('BLOCK_IF');\n    this.skipWhitespace();\n    var expr = this.parseIfExpr();\n    this.consumeOrError('CLOSE_MUSTACHE', 'Expected }} after {{#if expr');\n    var thenBranch = this.parseTemplate('if-end');\n    var elseBranch = [];\n    if (this.peek().kind === 'BLOCK_ELSE') {\n      this.consume('BLOCK_ELSE');\n      elseBranch = this.parseTemplate('if-end');\n    }\n    if (this.peek().kind === 'BLOCK_END_IF') {\n      this.consume('BLOCK_END_IF');\n    } else {\n      this.addError('Unclosed {{#if}} block', start);\n    }\n    return {\n      type: 'if',\n      expr,\n      thenBranch,\n      elseBranch\n    };\n  }\n  // ── Expressions ──────────────────────────────────────────────────────────\n  parseIfExpr() {\n    var path = this.parsePath();\n    if (this.peek().kind !== 'COMPARE_OP') {\n      // Truthy check — no operator\n      return {\n        path,\n        operator: null,\n        operand: null\n      };\n    }\n    var opToken = this.consume('COMPARE_OP');\n    var operator = opToken.value;\n    var operand = this.parseLiteral();\n    return {\n      path,\n      operator,\n      operand\n    };\n  }\n  parseLiteral() {\n    var token = this.peek();\n    if (token.kind === 'STRING_LITERAL') {\n      this.advance();\n      return token.value;\n    }\n    if (token.kind === 'NUMBER_LITERAL') {\n      this.advance();\n      return parseFloat(token.value);\n    }\n    if (token.kind === 'BOOL_LITERAL') {\n      this.advance();\n      return token.value === 'true';\n    }\n    if (token.kind === 'NULL_LITERAL') {\n      this.advance();\n      return null;\n    }\n    this.addError(\"Expected literal, got \".concat(token.kind), token.position);\n    return null;\n  }\n  // ── Path: identifier (. identifier)* ────────────────────────────────────\n  parsePath() {\n    var parts = [];\n    var first = this.peek();\n    if (first.kind !== 'IDENTIFIER') {\n      this.addError(\"Expected identifier, got \".concat(first.kind, \" at \").concat(first.position), first.position);\n      return '__invalid__';\n    }\n    parts.push(this.consume('IDENTIFIER').value);\n    while (this.peek().kind === 'DOT') {\n      this.consume('DOT');\n      if (this.peek().kind === 'IDENTIFIER') {\n        parts.push(this.consume('IDENTIFIER').value);\n      } else {\n        this.addError('Expected identifier after dot', this.peek().position);\n        break;\n      }\n    }\n    return parts.join('.');\n  }\n  // ── Modifiers: display | raw | upper | lower | title\n  //              | default:\"value\" | format:spec:\"value\" ──────────────────\n  parseModifier() {\n    var token = this.peek();\n    if (token.kind !== 'IDENTIFIER') {\n      this.addError(\"Expected modifier name, got \".concat(token.kind), token.position);\n      return null;\n    }\n    var name = this.consume('IDENTIFIER').value;\n    switch (name) {\n      case 'display':\n        return {\n          kind: 'display'\n        };\n      case 'raw':\n        return {\n          kind: 'raw'\n        };\n      case 'upper':\n        return {\n          kind: 'upper'\n        };\n      case 'lower':\n        return {\n          kind: 'lower'\n        };\n      case 'title':\n        return {\n          kind: 'title'\n        };\n      case 'default':\n        {\n          this.consumeOrError('COLON', 'Expected : after default');\n          var value = this.peek().kind === 'STRING_LITERAL' ? this.consume('STRING_LITERAL').value : '';\n          return {\n            kind: 'default',\n            value\n          };\n        }\n      case 'format':\n        {\n          this.consumeOrError('COLON', 'Expected : after format');\n          if (this.peek().kind !== 'IDENTIFIER') {\n            this.addError('Expected format spec (date, money, number)', this.peek().position);\n            return null;\n          }\n          var specType = this.consume('IDENTIFIER').value;\n          this.consumeOrError('COLON', \"Expected : after format:\".concat(specType));\n          var specValue = this.peek().kind === 'STRING_LITERAL' ? this.consume('STRING_LITERAL').value : '';\n          return {\n            kind: 'format',\n            spec: \"\".concat(specType, \":\").concat(specValue)\n          };\n        }\n      default:\n        this.addError(\"Unknown modifier: \".concat(name), token.position);\n        return null;\n    }\n  }\n  // ── Helpers ──────────────────────────────────────────────────────────────\n  peek() {\n    var _a;\n    return (_a = this.tokens[this.pos]) !== null && _a !== void 0 ? _a : {\n      kind: 'EOF',\n      value: '',\n      position: -1\n    };\n  }\n  advance() {\n    var token = this.peek();\n    if (!this.isAtEnd()) this.pos += 1;\n    return token;\n  }\n  consume(kind) {\n    var token = this.peek();\n    if (token.kind !== kind) {\n      this.addError(\"Expected \".concat(kind, \", got \").concat(token.kind), token.position);\n    }\n    return this.advance();\n  }\n  consumeOrError(kind, message) {\n    var token = this.peek();\n    if (token.kind !== kind) {\n      this.addError(message, token.position);\n      return;\n    }\n    this.advance();\n  }\n  skipWhitespace() {\n    // The lexer emits whitespace as TEXT tokens inside {{ }} — skip them\n    while (this.peek().kind === 'TEXT' && this.peek().value.trim() === '') {\n      this.advance();\n    }\n  }\n  isAtEnd() {\n    return this.peek().kind === 'EOF';\n  }\n  addError(message, position) {\n    this.errors.push({\n      message,\n      position\n    });\n  }\n}\nexports.TokenParser = TokenParser;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/token/TokenParser.ts?\n}");

/***/ },

/***/ "./src/token/TokenRenderer.ts"
/*!************************************!*\
  !*** ./src/token/TokenRenderer.ts ***!
  \************************************/
(__unused_webpack_module, exports) {

"use strict";
eval("{\n\n/**\n * TokenRenderer.ts\n * Walks a TemplateAst and produces the final rendered string.\n * Resolves token paths against a RenderContext, applies modifiers,\n * handles each/if blocks, and HTML-escapes by default.\n */\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.TokenRenderer = void 0;\nvar OData_FORMATTED_VALUE_SUFFIX = '@OData.Community.Display.V1.FormattedValue';\nclass TokenRenderer {\n  constructor(formatters, logger) {\n    this.formatters = formatters;\n    this.logger = logger;\n  }\n  render(ast, context) {\n    return this.renderNodes(ast, context);\n  }\n  // ── Node dispatch ────────────────────────────────────────────────────────\n  renderNodes(nodes, context) {\n    return nodes.map(node => this.renderNode(node, context)).join('');\n  }\n  renderNode(node, context) {\n    switch (node.type) {\n      case 'text':\n        return node.value;\n      case 'mustache':\n        return this.renderMustache(node, context);\n      case 'each':\n        return this.renderEach(node, context);\n      case 'if':\n        return this.renderIf(node, context);\n    }\n  }\n  // ── Mustache ─────────────────────────────────────────────────────────────\n  renderMustache(node, context) {\n    var hasDisplay = node.modifiers.some(m => m.kind === 'display');\n    var hasRaw = node.modifiers.some(m => m.kind === 'raw');\n    var defaultMod = node.modifiers.find(m => m.kind === 'default');\n    var formatMod = node.modifiers.find(m => m.kind === 'format');\n    var caseMod = node.modifiers.find(m => m.kind === 'upper' || m.kind === 'lower' || m.kind === 'title');\n    var raw = hasDisplay ? this.resolveDisplayValue(node.path, context) : this.resolvePath(node.path, context);\n    if (raw === null || raw === undefined) {\n      raw = defaultMod ? defaultMod.value : '';\n    }\n    var stringValue = formatMod ? this.formatters.applyFormat(raw, formatMod.spec, context.locale, context.currency) : String(raw !== null && raw !== void 0 ? raw : '');\n    if (caseMod) {\n      stringValue = this.formatters.applyCase(stringValue, caseMod.kind);\n    }\n    return hasRaw ? stringValue : this.formatters.escapeHtml(stringValue);\n  }\n  // ── Each ─────────────────────────────────────────────────────────────────\n  renderEach(node, context) {\n    var collection = this.resolvePath(node.path, context);\n    if (!Array.isArray(collection)) {\n      this.logger.warn('each_not_array', {\n        path: node.path\n      });\n      return '';\n    }\n    return collection.map(item => {\n      if (typeof item !== 'object' || item === null) {\n        this.logger.warn('each_item_not_object', {\n          path: node.path\n        });\n        return '';\n      }\n      var itemContext = Object.assign(Object.assign({}, context), {\n        root: item\n      });\n      return this.renderNodes(node.body, itemContext);\n    }).join('');\n  }\n  // ── If ───────────────────────────────────────────────────────────────────\n  renderIf(node, context) {\n    var condition = this.evaluateExpr(node.expr, context);\n    return condition ? this.renderNodes(node.thenBranch, context) : this.renderNodes(node.elseBranch, context);\n  }\n  evaluateExpr(expr, context) {\n    var left = this.resolvePath(expr.path, context);\n    if (expr.operator === null) {\n      return this.isTruthy(left);\n    }\n    var right = expr.operand;\n    switch (expr.operator) {\n      case '==':\n        return this.looseEquals(left, right);\n      case '!=':\n        return !this.looseEquals(left, right);\n      case '>':\n        return this.toNumber(left) > this.toNumber(right);\n      case '>=':\n        return this.toNumber(left) >= this.toNumber(right);\n      case '<':\n        return this.toNumber(left) < this.toNumber(right);\n      case '<=':\n        return this.toNumber(left) <= this.toNumber(right);\n      default:\n        return false;\n    }\n  }\n  // ── Path resolution ───────────────────────────────────────────────────────\n  resolvePath(path, context) {\n    var segments = path.split('.');\n    var current = context.root;\n    for (var segment of segments) {\n      if (current === null || current === undefined) {\n        this.logger.warn('path_null_segment', {\n          path,\n          segment\n        });\n        return null;\n      }\n      if (typeof current !== 'object') {\n        this.logger.warn('path_not_object', {\n          path,\n          segment\n        });\n        return null;\n      }\n      current = current[segment];\n    }\n    return current;\n  }\n  /**\n   * Resolves a lookup field's display value via the OData FormattedValue annotation.\n   * Dataverse returns: _fieldname_value@OData.Community.Display.V1.FormattedValue\n   */\n  resolveDisplayValue(path, context) {\n    var segments = path.split('.');\n    var lastSegment = segments[segments.length - 1];\n    var parentSegments = segments.slice(0, -1);\n    var parentObj = parentSegments.length > 0 ? this.resolvePath(parentSegments.join('.'), context) : context.root;\n    if (typeof parentObj !== 'object' || parentObj === null) return null;\n    var parent = parentObj;\n    // Try OData annotation first\n    var annotationKey = \"_\".concat(lastSegment, \"_value\").concat(OData_FORMATTED_VALUE_SUFFIX);\n    if (annotationKey in parent) return parent[annotationKey];\n    // Try direct formatted value annotation on the field itself\n    var directAnnotation = \"\".concat(lastSegment).concat(OData_FORMATTED_VALUE_SUFFIX);\n    if (directAnnotation in parent) return parent[directAnnotation];\n    // Fallback to raw value\n    return parent[lastSegment];\n  }\n  // ── Comparison helpers ────────────────────────────────────────────────────\n  isTruthy(value) {\n    if (value === null || value === undefined || value === false || value === 0) return false;\n    if (typeof value === 'string') return value.trim().length > 0;\n    return true;\n  }\n  looseEquals(a, b) {\n    if (a === null || a === undefined) return b === null || b === undefined;\n    return String(a).toLowerCase() === String(b).toLowerCase();\n  }\n  toNumber(value) {\n    var n = parseFloat(String(value));\n    return isNaN(n) ? 0 : n;\n  }\n}\nexports.TokenRenderer = TokenRenderer;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/token/TokenRenderer.ts?\n}");

/***/ },

/***/ "./src/types/EditorProps.ts"
/*!**********************************!*\
  !*** ./src/types/EditorProps.ts ***!
  \**********************************/
(__unused_webpack_module, exports) {

"use strict";
eval("{\n\n/**\n * EditorProps.ts\n * Shared prop interfaces for the email editor component tree.\n */\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.CLOSED_POPUP = void 0;\nexports.CLOSED_POPUP = {\n  open: false,\n  anchor: null,\n  entityPath: [],\n  triggerRange: null,\n  mode: 'field'\n};\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./src/types/EditorProps.ts?\n}");

/***/ },

/***/ "./node_modules/object-assign/index.js"
/*!*********************************************!*\
  !*** ./node_modules/object-assign/index.js ***!
  \*********************************************/
(module) {

"use strict";
eval("{/*\nobject-assign\n(c) Sindre Sorhus\n@license MIT\n*/\n\n\n\n/* eslint-disable no-unused-vars */\nvar getOwnPropertySymbols = Object.getOwnPropertySymbols;\nvar hasOwnProperty = Object.prototype.hasOwnProperty;\nvar propIsEnumerable = Object.prototype.propertyIsEnumerable;\nfunction toObject(val) {\n  if (val === null || val === undefined) {\n    throw new TypeError('Object.assign cannot be called with null or undefined');\n  }\n  return Object(val);\n}\nfunction shouldUseNative() {\n  try {\n    if (!Object.assign) {\n      return false;\n    }\n\n    // Detect buggy property enumeration order in older V8 versions.\n\n    // https://bugs.chromium.org/p/v8/issues/detail?id=4118\n    var test1 = new String('abc'); // eslint-disable-line no-new-wrappers\n    test1[5] = 'de';\n    if (Object.getOwnPropertyNames(test1)[0] === '5') {\n      return false;\n    }\n\n    // https://bugs.chromium.org/p/v8/issues/detail?id=3056\n    var test2 = {};\n    for (var i = 0; i < 10; i++) {\n      test2['_' + String.fromCharCode(i)] = i;\n    }\n    var order2 = Object.getOwnPropertyNames(test2).map(function (n) {\n      return test2[n];\n    });\n    if (order2.join('') !== '0123456789') {\n      return false;\n    }\n\n    // https://bugs.chromium.org/p/v8/issues/detail?id=3056\n    var test3 = {};\n    'abcdefghijklmnopqrst'.split('').forEach(function (letter) {\n      test3[letter] = letter;\n    });\n    if (Object.keys(Object.assign({}, test3)).join('') !== 'abcdefghijklmnopqrst') {\n      return false;\n    }\n    return true;\n  } catch (err) {\n    // We don't expect any of the above to throw, but better to be safe.\n    return false;\n  }\n}\nmodule.exports = shouldUseNative() ? Object.assign : function (target, source) {\n  var from;\n  var to = toObject(target);\n  var symbols;\n  for (var s = 1; s < arguments.length; s++) {\n    from = Object(arguments[s]);\n    for (var key in from) {\n      if (hasOwnProperty.call(from, key)) {\n        to[key] = from[key];\n      }\n    }\n    if (getOwnPropertySymbols) {\n      symbols = getOwnPropertySymbols(from);\n      for (var i = 0; i < symbols.length; i++) {\n        if (propIsEnumerable.call(from, symbols[i])) {\n          to[symbols[i]] = from[symbols[i]];\n        }\n      }\n    }\n  }\n  return to;\n};\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./node_modules/object-assign/index.js?\n}");

/***/ },

/***/ "./node_modules/prop-types/checkPropTypes.js"
/*!***************************************************!*\
  !*** ./node_modules/prop-types/checkPropTypes.js ***!
  \***************************************************/
(module, __unused_webpack_exports, __webpack_require__) {

"use strict";
eval("{/**\n * Copyright (c) 2013-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n */\n\n\n\nvar printWarning = function printWarning() {};\nif (true) {\n  var ReactPropTypesSecret = __webpack_require__(/*! ./lib/ReactPropTypesSecret */ \"./node_modules/prop-types/lib/ReactPropTypesSecret.js\");\n  var loggedTypeFailures = {};\n  var has = __webpack_require__(/*! ./lib/has */ \"./node_modules/prop-types/lib/has.js\");\n  printWarning = function printWarning(text) {\n    var message = 'Warning: ' + text;\n    if (typeof console !== 'undefined') {\n      console.error(message);\n    }\n    try {\n      // --- Welcome to debugging React ---\n      // This error was thrown as a convenience so that you can use this stack\n      // to find the callsite that caused this warning to fire.\n      throw new Error(message);\n    } catch (x) {/**/}\n  };\n}\n\n/**\n * Assert that the values match with the type specs.\n * Error messages are memorized and will only be shown once.\n *\n * @param {object} typeSpecs Map of name to a ReactPropType\n * @param {object} values Runtime values that need to be type-checked\n * @param {string} location e.g. \"prop\", \"context\", \"child context\"\n * @param {string} componentName Name of the component for error messages.\n * @param {?Function} getStack Returns the component stack.\n * @private\n */\nfunction checkPropTypes(typeSpecs, values, location, componentName, getStack) {\n  if (true) {\n    for (var typeSpecName in typeSpecs) {\n      if (has(typeSpecs, typeSpecName)) {\n        var error;\n        // Prop type validation may throw. In case they do, we don't want to\n        // fail the render phase where it didn't fail before. So we log it.\n        // After these have been cleaned up, we'll let them throw.\n        try {\n          // This is intentionally an invariant that gets caught. It's the same\n          // behavior as without this statement except with a better message.\n          if (typeof typeSpecs[typeSpecName] !== 'function') {\n            var err = Error((componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' + 'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.' + 'This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.');\n            err.name = 'Invariant Violation';\n            throw err;\n          }\n          error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);\n        } catch (ex) {\n          error = ex;\n        }\n        if (error && !(error instanceof Error)) {\n          printWarning((componentName || 'React class') + ': type specification of ' + location + ' `' + typeSpecName + '` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a ' + typeof error + '. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).');\n        }\n        if (error instanceof Error && !(error.message in loggedTypeFailures)) {\n          // Only monitor this failure once because there tends to be a lot of the\n          // same error.\n          loggedTypeFailures[error.message] = true;\n          var stack = getStack ? getStack() : '';\n          printWarning('Failed ' + location + ' type: ' + error.message + (stack != null ? stack : ''));\n        }\n      }\n    }\n  }\n}\n\n/**\n * Resets warning cache when testing.\n *\n * @private\n */\ncheckPropTypes.resetWarningCache = function () {\n  if (true) {\n    loggedTypeFailures = {};\n  }\n};\nmodule.exports = checkPropTypes;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./node_modules/prop-types/checkPropTypes.js?\n}");

/***/ },

/***/ "./node_modules/prop-types/lib/ReactPropTypesSecret.js"
/*!*************************************************************!*\
  !*** ./node_modules/prop-types/lib/ReactPropTypesSecret.js ***!
  \*************************************************************/
(module) {

"use strict";
eval("{/**\n * Copyright (c) 2013-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n */\n\n\n\nvar ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';\nmodule.exports = ReactPropTypesSecret;\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./node_modules/prop-types/lib/ReactPropTypesSecret.js?\n}");

/***/ },

/***/ "./node_modules/prop-types/lib/has.js"
/*!********************************************!*\
  !*** ./node_modules/prop-types/lib/has.js ***!
  \********************************************/
(module) {

eval("{module.exports = Function.call.bind(Object.prototype.hasOwnProperty);\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./node_modules/prop-types/lib/has.js?\n}");

/***/ },

/***/ "./node_modules/react/cjs/react.development.js"
/*!*****************************************************!*\
  !*** ./node_modules/react/cjs/react.development.js ***!
  \*****************************************************/
(__unused_webpack_module, exports, __webpack_require__) {

"use strict";
eval("{/** @license React v16.14.0\n * react.development.js\n *\n * Copyright (c) Facebook, Inc. and its affiliates.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n */\n\n\n\nif (true) {\n  (function () {\n    'use strict';\n\n    var _assign = __webpack_require__(/*! object-assign */ \"./node_modules/object-assign/index.js\");\n    var checkPropTypes = __webpack_require__(/*! prop-types/checkPropTypes */ \"./node_modules/prop-types/checkPropTypes.js\");\n    var ReactVersion = '16.14.0';\n\n    // The Symbol used to tag the ReactElement-like types. If there is no native Symbol\n    // nor polyfill, then a plain number is used for performance.\n    var hasSymbol = typeof Symbol === 'function' && Symbol.for;\n    var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for('react.element') : 0xeac7;\n    var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for('react.portal') : 0xeaca;\n    var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for('react.fragment') : 0xeacb;\n    var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for('react.strict_mode') : 0xeacc;\n    var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for('react.profiler') : 0xead2;\n    var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for('react.provider') : 0xeacd;\n    var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for('react.context') : 0xeace; // TODO: We don't use AsyncMode or ConcurrentMode anymore. They were temporary\n    var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for('react.concurrent_mode') : 0xeacf;\n    var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for('react.forward_ref') : 0xead0;\n    var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for('react.suspense') : 0xead1;\n    var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for('react.suspense_list') : 0xead8;\n    var REACT_MEMO_TYPE = hasSymbol ? Symbol.for('react.memo') : 0xead3;\n    var REACT_LAZY_TYPE = hasSymbol ? Symbol.for('react.lazy') : 0xead4;\n    var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for('react.block') : 0xead9;\n    var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for('react.fundamental') : 0xead5;\n    var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for('react.responder') : 0xead6;\n    var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for('react.scope') : 0xead7;\n    var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;\n    var FAUX_ITERATOR_SYMBOL = '@@iterator';\n    function getIteratorFn(maybeIterable) {\n      if (maybeIterable === null || typeof maybeIterable !== 'object') {\n        return null;\n      }\n      var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];\n      if (typeof maybeIterator === 'function') {\n        return maybeIterator;\n      }\n      return null;\n    }\n\n    /**\n     * Keeps track of the current dispatcher.\n     */\n    var ReactCurrentDispatcher = {\n      /**\n       * @internal\n       * @type {ReactComponent}\n       */\n      current: null\n    };\n\n    /**\n     * Keeps track of the current batch's configuration such as how long an update\n     * should suspend for if it needs to.\n     */\n    var ReactCurrentBatchConfig = {\n      suspense: null\n    };\n\n    /**\n     * Keeps track of the current owner.\n     *\n     * The current owner is the component who should own any components that are\n     * currently being constructed.\n     */\n    var ReactCurrentOwner = {\n      /**\n       * @internal\n       * @type {ReactComponent}\n       */\n      current: null\n    };\n    var BEFORE_SLASH_RE = /^(.*)[\\\\\\/]/;\n    function describeComponentFrame(name, source, ownerName) {\n      var sourceInfo = '';\n      if (source) {\n        var path = source.fileName;\n        var fileName = path.replace(BEFORE_SLASH_RE, '');\n        {\n          // In DEV, include code for a common special case:\n          // prefer \"folder/index.js\" instead of just \"index.js\".\n          if (/^index\\./.test(fileName)) {\n            var match = path.match(BEFORE_SLASH_RE);\n            if (match) {\n              var pathBeforeSlash = match[1];\n              if (pathBeforeSlash) {\n                var folderName = pathBeforeSlash.replace(BEFORE_SLASH_RE, '');\n                fileName = folderName + '/' + fileName;\n              }\n            }\n          }\n        }\n        sourceInfo = ' (at ' + fileName + ':' + source.lineNumber + ')';\n      } else if (ownerName) {\n        sourceInfo = ' (created by ' + ownerName + ')';\n      }\n      return '\\n    in ' + (name || 'Unknown') + sourceInfo;\n    }\n    var Resolved = 1;\n    function refineResolvedLazyComponent(lazyComponent) {\n      return lazyComponent._status === Resolved ? lazyComponent._result : null;\n    }\n    function getWrappedName(outerType, innerType, wrapperName) {\n      var functionName = innerType.displayName || innerType.name || '';\n      return outerType.displayName || (functionName !== '' ? wrapperName + \"(\" + functionName + \")\" : wrapperName);\n    }\n    function getComponentName(type) {\n      if (type == null) {\n        // Host root, text node or just invalid type.\n        return null;\n      }\n      {\n        if (typeof type.tag === 'number') {\n          error('Received an unexpected object in getComponentName(). ' + 'This is likely a bug in React. Please file an issue.');\n        }\n      }\n      if (typeof type === 'function') {\n        return type.displayName || type.name || null;\n      }\n      if (typeof type === 'string') {\n        return type;\n      }\n      switch (type) {\n        case REACT_FRAGMENT_TYPE:\n          return 'Fragment';\n        case REACT_PORTAL_TYPE:\n          return 'Portal';\n        case REACT_PROFILER_TYPE:\n          return \"Profiler\";\n        case REACT_STRICT_MODE_TYPE:\n          return 'StrictMode';\n        case REACT_SUSPENSE_TYPE:\n          return 'Suspense';\n        case REACT_SUSPENSE_LIST_TYPE:\n          return 'SuspenseList';\n      }\n      if (typeof type === 'object') {\n        switch (type.$$typeof) {\n          case REACT_CONTEXT_TYPE:\n            return 'Context.Consumer';\n          case REACT_PROVIDER_TYPE:\n            return 'Context.Provider';\n          case REACT_FORWARD_REF_TYPE:\n            return getWrappedName(type, type.render, 'ForwardRef');\n          case REACT_MEMO_TYPE:\n            return getComponentName(type.type);\n          case REACT_BLOCK_TYPE:\n            return getComponentName(type.render);\n          case REACT_LAZY_TYPE:\n            {\n              var thenable = type;\n              var resolvedThenable = refineResolvedLazyComponent(thenable);\n              if (resolvedThenable) {\n                return getComponentName(resolvedThenable);\n              }\n              break;\n            }\n        }\n      }\n      return null;\n    }\n    var ReactDebugCurrentFrame = {};\n    var currentlyValidatingElement = null;\n    function setCurrentlyValidatingElement(element) {\n      {\n        currentlyValidatingElement = element;\n      }\n    }\n    {\n      // Stack implementation injected by the current renderer.\n      ReactDebugCurrentFrame.getCurrentStack = null;\n      ReactDebugCurrentFrame.getStackAddendum = function () {\n        var stack = ''; // Add an extra top frame while an element is being validated\n\n        if (currentlyValidatingElement) {\n          var name = getComponentName(currentlyValidatingElement.type);\n          var owner = currentlyValidatingElement._owner;\n          stack += describeComponentFrame(name, currentlyValidatingElement._source, owner && getComponentName(owner.type));\n        } // Delegate to the injected renderer-specific implementation\n\n        var impl = ReactDebugCurrentFrame.getCurrentStack;\n        if (impl) {\n          stack += impl() || '';\n        }\n        return stack;\n      };\n    }\n\n    /**\n     * Used by act() to track whether you're inside an act() scope.\n     */\n    var IsSomeRendererActing = {\n      current: false\n    };\n    var ReactSharedInternals = {\n      ReactCurrentDispatcher: ReactCurrentDispatcher,\n      ReactCurrentBatchConfig: ReactCurrentBatchConfig,\n      ReactCurrentOwner: ReactCurrentOwner,\n      IsSomeRendererActing: IsSomeRendererActing,\n      // Used by renderers to avoid bundling object-assign twice in UMD bundles:\n      assign: _assign\n    };\n    {\n      _assign(ReactSharedInternals, {\n        // These should not be included in production.\n        ReactDebugCurrentFrame: ReactDebugCurrentFrame,\n        // Shim for React DOM 16.0.0 which still destructured (but not used) this.\n        // TODO: remove in React 17.0.\n        ReactComponentTreeHook: {}\n      });\n    }\n\n    // by calls to these methods by a Babel plugin.\n    //\n    // In PROD (or in packages without access to React internals),\n    // they are left as they are instead.\n\n    function warn(format) {\n      {\n        for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {\n          args[_key - 1] = arguments[_key];\n        }\n        printWarning('warn', format, args);\n      }\n    }\n    function error(format) {\n      {\n        for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {\n          args[_key2 - 1] = arguments[_key2];\n        }\n        printWarning('error', format, args);\n      }\n    }\n    function printWarning(level, format, args) {\n      // When changing this logic, you might want to also\n      // update consoleWithStackDev.www.js as well.\n      {\n        var hasExistingStack = args.length > 0 && typeof args[args.length - 1] === 'string' && args[args.length - 1].indexOf('\\n    in') === 0;\n        if (!hasExistingStack) {\n          var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;\n          var stack = ReactDebugCurrentFrame.getStackAddendum();\n          if (stack !== '') {\n            format += '%s';\n            args = args.concat([stack]);\n          }\n        }\n        var argsWithFormat = args.map(function (item) {\n          return '' + item;\n        }); // Careful: RN currently depends on this prefix\n\n        argsWithFormat.unshift('Warning: ' + format); // We intentionally don't use spread (or .apply) directly because it\n        // breaks IE9: https://github.com/facebook/react/issues/13610\n        // eslint-disable-next-line react-internal/no-production-logging\n\n        Function.prototype.apply.call(console[level], console, argsWithFormat);\n        try {\n          // --- Welcome to debugging React ---\n          // This error was thrown as a convenience so that you can use this stack\n          // to find the callsite that caused this warning to fire.\n          var argIndex = 0;\n          var message = 'Warning: ' + format.replace(/%s/g, function () {\n            return args[argIndex++];\n          });\n          throw new Error(message);\n        } catch (x) {}\n      }\n    }\n    var didWarnStateUpdateForUnmountedComponent = {};\n    function warnNoop(publicInstance, callerName) {\n      {\n        var _constructor = publicInstance.constructor;\n        var componentName = _constructor && (_constructor.displayName || _constructor.name) || 'ReactClass';\n        var warningKey = componentName + \".\" + callerName;\n        if (didWarnStateUpdateForUnmountedComponent[warningKey]) {\n          return;\n        }\n        error(\"Can't call %s on a component that is not yet mounted. \" + 'This is a no-op, but it might indicate a bug in your application. ' + 'Instead, assign to `this.state` directly or define a `state = {};` ' + 'class property with the desired state in the %s component.', callerName, componentName);\n        didWarnStateUpdateForUnmountedComponent[warningKey] = true;\n      }\n    }\n    /**\n     * This is the abstract API for an update queue.\n     */\n\n    var ReactNoopUpdateQueue = {\n      /**\n       * Checks whether or not this composite component is mounted.\n       * @param {ReactClass} publicInstance The instance we want to test.\n       * @return {boolean} True if mounted, false otherwise.\n       * @protected\n       * @final\n       */\n      isMounted: function isMounted(publicInstance) {\n        return false;\n      },\n      /**\n       * Forces an update. This should only be invoked when it is known with\n       * certainty that we are **not** in a DOM transaction.\n       *\n       * You may want to call this when you know that some deeper aspect of the\n       * component's state has changed but `setState` was not called.\n       *\n       * This will not invoke `shouldComponentUpdate`, but it will invoke\n       * `componentWillUpdate` and `componentDidUpdate`.\n       *\n       * @param {ReactClass} publicInstance The instance that should rerender.\n       * @param {?function} callback Called after component is updated.\n       * @param {?string} callerName name of the calling function in the public API.\n       * @internal\n       */\n      enqueueForceUpdate: function enqueueForceUpdate(publicInstance, callback, callerName) {\n        warnNoop(publicInstance, 'forceUpdate');\n      },\n      /**\n       * Replaces all of the state. Always use this or `setState` to mutate state.\n       * You should treat `this.state` as immutable.\n       *\n       * There is no guarantee that `this.state` will be immediately updated, so\n       * accessing `this.state` after calling this method may return the old value.\n       *\n       * @param {ReactClass} publicInstance The instance that should rerender.\n       * @param {object} completeState Next state.\n       * @param {?function} callback Called after component is updated.\n       * @param {?string} callerName name of the calling function in the public API.\n       * @internal\n       */\n      enqueueReplaceState: function enqueueReplaceState(publicInstance, completeState, callback, callerName) {\n        warnNoop(publicInstance, 'replaceState');\n      },\n      /**\n       * Sets a subset of the state. This only exists because _pendingState is\n       * internal. This provides a merging strategy that is not available to deep\n       * properties which is confusing. TODO: Expose pendingState or don't use it\n       * during the merge.\n       *\n       * @param {ReactClass} publicInstance The instance that should rerender.\n       * @param {object} partialState Next partial state to be merged with state.\n       * @param {?function} callback Called after component is updated.\n       * @param {?string} Name of the calling function in the public API.\n       * @internal\n       */\n      enqueueSetState: function enqueueSetState(publicInstance, partialState, callback, callerName) {\n        warnNoop(publicInstance, 'setState');\n      }\n    };\n    var emptyObject = {};\n    {\n      Object.freeze(emptyObject);\n    }\n    /**\n     * Base class helpers for the updating state of a component.\n     */\n\n    function Component(props, context, updater) {\n      this.props = props;\n      this.context = context; // If a component has string refs, we will assign a different object later.\n\n      this.refs = emptyObject; // We initialize the default updater but the real one gets injected by the\n      // renderer.\n\n      this.updater = updater || ReactNoopUpdateQueue;\n    }\n    Component.prototype.isReactComponent = {};\n    /**\n     * Sets a subset of the state. Always use this to mutate\n     * state. You should treat `this.state` as immutable.\n     *\n     * There is no guarantee that `this.state` will be immediately updated, so\n     * accessing `this.state` after calling this method may return the old value.\n     *\n     * There is no guarantee that calls to `setState` will run synchronously,\n     * as they may eventually be batched together.  You can provide an optional\n     * callback that will be executed when the call to setState is actually\n     * completed.\n     *\n     * When a function is provided to setState, it will be called at some point in\n     * the future (not synchronously). It will be called with the up to date\n     * component arguments (state, props, context). These values can be different\n     * from this.* because your function may be called after receiveProps but before\n     * shouldComponentUpdate, and this new state, props, and context will not yet be\n     * assigned to this.\n     *\n     * @param {object|function} partialState Next partial state or function to\n     *        produce next partial state to be merged with current state.\n     * @param {?function} callback Called after state is updated.\n     * @final\n     * @protected\n     */\n\n    Component.prototype.setState = function (partialState, callback) {\n      if (!(typeof partialState === 'object' || typeof partialState === 'function' || partialState == null)) {\n        {\n          throw Error(\"setState(...): takes an object of state variables to update or a function which returns an object of state variables.\");\n        }\n      }\n      this.updater.enqueueSetState(this, partialState, callback, 'setState');\n    };\n    /**\n     * Forces an update. This should only be invoked when it is known with\n     * certainty that we are **not** in a DOM transaction.\n     *\n     * You may want to call this when you know that some deeper aspect of the\n     * component's state has changed but `setState` was not called.\n     *\n     * This will not invoke `shouldComponentUpdate`, but it will invoke\n     * `componentWillUpdate` and `componentDidUpdate`.\n     *\n     * @param {?function} callback Called after update is complete.\n     * @final\n     * @protected\n     */\n\n    Component.prototype.forceUpdate = function (callback) {\n      this.updater.enqueueForceUpdate(this, callback, 'forceUpdate');\n    };\n    /**\n     * Deprecated APIs. These APIs used to exist on classic React classes but since\n     * we would like to deprecate them, we're not going to move them over to this\n     * modern base class. Instead, we define a getter that warns if it's accessed.\n     */\n\n    {\n      var deprecatedAPIs = {\n        isMounted: ['isMounted', 'Instead, make sure to clean up subscriptions and pending requests in ' + 'componentWillUnmount to prevent memory leaks.'],\n        replaceState: ['replaceState', 'Refactor your code to use setState instead (see ' + 'https://github.com/facebook/react/issues/3236).']\n      };\n      var defineDeprecationWarning = function defineDeprecationWarning(methodName, info) {\n        Object.defineProperty(Component.prototype, methodName, {\n          get: function get() {\n            warn('%s(...) is deprecated in plain JavaScript React classes. %s', info[0], info[1]);\n            return undefined;\n          }\n        });\n      };\n      for (var fnName in deprecatedAPIs) {\n        if (deprecatedAPIs.hasOwnProperty(fnName)) {\n          defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);\n        }\n      }\n    }\n    function ComponentDummy() {}\n    ComponentDummy.prototype = Component.prototype;\n    /**\n     * Convenience component with default shallow equality check for sCU.\n     */\n\n    function PureComponent(props, context, updater) {\n      this.props = props;\n      this.context = context; // If a component has string refs, we will assign a different object later.\n\n      this.refs = emptyObject;\n      this.updater = updater || ReactNoopUpdateQueue;\n    }\n    var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();\n    pureComponentPrototype.constructor = PureComponent; // Avoid an extra prototype jump for these methods.\n\n    _assign(pureComponentPrototype, Component.prototype);\n    pureComponentPrototype.isPureReactComponent = true;\n\n    // an immutable object with a single mutable value\n    function createRef() {\n      var refObject = {\n        current: null\n      };\n      {\n        Object.seal(refObject);\n      }\n      return refObject;\n    }\n    var hasOwnProperty = Object.prototype.hasOwnProperty;\n    var RESERVED_PROPS = {\n      key: true,\n      ref: true,\n      __self: true,\n      __source: true\n    };\n    var specialPropKeyWarningShown, specialPropRefWarningShown, didWarnAboutStringRefs;\n    {\n      didWarnAboutStringRefs = {};\n    }\n    function hasValidRef(config) {\n      {\n        if (hasOwnProperty.call(config, 'ref')) {\n          var getter = Object.getOwnPropertyDescriptor(config, 'ref').get;\n          if (getter && getter.isReactWarning) {\n            return false;\n          }\n        }\n      }\n      return config.ref !== undefined;\n    }\n    function hasValidKey(config) {\n      {\n        if (hasOwnProperty.call(config, 'key')) {\n          var getter = Object.getOwnPropertyDescriptor(config, 'key').get;\n          if (getter && getter.isReactWarning) {\n            return false;\n          }\n        }\n      }\n      return config.key !== undefined;\n    }\n    function defineKeyPropWarningGetter(props, displayName) {\n      var warnAboutAccessingKey = function warnAboutAccessingKey() {\n        {\n          if (!specialPropKeyWarningShown) {\n            specialPropKeyWarningShown = true;\n            error('%s: `key` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);\n          }\n        }\n      };\n      warnAboutAccessingKey.isReactWarning = true;\n      Object.defineProperty(props, 'key', {\n        get: warnAboutAccessingKey,\n        configurable: true\n      });\n    }\n    function defineRefPropWarningGetter(props, displayName) {\n      var warnAboutAccessingRef = function warnAboutAccessingRef() {\n        {\n          if (!specialPropRefWarningShown) {\n            specialPropRefWarningShown = true;\n            error('%s: `ref` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);\n          }\n        }\n      };\n      warnAboutAccessingRef.isReactWarning = true;\n      Object.defineProperty(props, 'ref', {\n        get: warnAboutAccessingRef,\n        configurable: true\n      });\n    }\n    function warnIfStringRefCannotBeAutoConverted(config) {\n      {\n        if (typeof config.ref === 'string' && ReactCurrentOwner.current && config.__self && ReactCurrentOwner.current.stateNode !== config.__self) {\n          var componentName = getComponentName(ReactCurrentOwner.current.type);\n          if (!didWarnAboutStringRefs[componentName]) {\n            error('Component \"%s\" contains the string ref \"%s\". ' + 'Support for string refs will be removed in a future major release. ' + 'This case cannot be automatically converted to an arrow function. ' + 'We ask you to manually fix this case by using useRef() or createRef() instead. ' + 'Learn more about using refs safely here: ' + 'https://fb.me/react-strict-mode-string-ref', getComponentName(ReactCurrentOwner.current.type), config.ref);\n            didWarnAboutStringRefs[componentName] = true;\n          }\n        }\n      }\n    }\n    /**\n     * Factory method to create a new React element. This no longer adheres to\n     * the class pattern, so do not use new to call it. Also, instanceof check\n     * will not work. Instead test $$typeof field against Symbol.for('react.element') to check\n     * if something is a React Element.\n     *\n     * @param {*} type\n     * @param {*} props\n     * @param {*} key\n     * @param {string|object} ref\n     * @param {*} owner\n     * @param {*} self A *temporary* helper to detect places where `this` is\n     * different from the `owner` when React.createElement is called, so that we\n     * can warn. We want to get rid of owner and replace string `ref`s with arrow\n     * functions, and as long as `this` and owner are the same, there will be no\n     * change in behavior.\n     * @param {*} source An annotation object (added by a transpiler or otherwise)\n     * indicating filename, line number, and/or other information.\n     * @internal\n     */\n\n    var ReactElement = function ReactElement(type, key, ref, self, source, owner, props) {\n      var element = {\n        // This tag allows us to uniquely identify this as a React Element\n        $$typeof: REACT_ELEMENT_TYPE,\n        // Built-in properties that belong on the element\n        type: type,\n        key: key,\n        ref: ref,\n        props: props,\n        // Record the component responsible for creating this element.\n        _owner: owner\n      };\n      {\n        // The validation flag is currently mutative. We put it on\n        // an external backing store so that we can freeze the whole object.\n        // This can be replaced with a WeakMap once they are implemented in\n        // commonly used development environments.\n        element._store = {}; // To make comparing ReactElements easier for testing purposes, we make\n        // the validation flag non-enumerable (where possible, which should\n        // include every environment we run tests in), so the test framework\n        // ignores it.\n\n        Object.defineProperty(element._store, 'validated', {\n          configurable: false,\n          enumerable: false,\n          writable: true,\n          value: false\n        }); // self and source are DEV only properties.\n\n        Object.defineProperty(element, '_self', {\n          configurable: false,\n          enumerable: false,\n          writable: false,\n          value: self\n        }); // Two elements created in two different places should be considered\n        // equal for testing purposes and therefore we hide it from enumeration.\n\n        Object.defineProperty(element, '_source', {\n          configurable: false,\n          enumerable: false,\n          writable: false,\n          value: source\n        });\n        if (Object.freeze) {\n          Object.freeze(element.props);\n          Object.freeze(element);\n        }\n      }\n      return element;\n    };\n    /**\n     * Create and return a new ReactElement of the given type.\n     * See https://reactjs.org/docs/react-api.html#createelement\n     */\n\n    function createElement(type, config, children) {\n      var propName; // Reserved names are extracted\n\n      var props = {};\n      var key = null;\n      var ref = null;\n      var self = null;\n      var source = null;\n      if (config != null) {\n        if (hasValidRef(config)) {\n          ref = config.ref;\n          {\n            warnIfStringRefCannotBeAutoConverted(config);\n          }\n        }\n        if (hasValidKey(config)) {\n          key = '' + config.key;\n        }\n        self = config.__self === undefined ? null : config.__self;\n        source = config.__source === undefined ? null : config.__source; // Remaining properties are added to a new props object\n\n        for (propName in config) {\n          if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {\n            props[propName] = config[propName];\n          }\n        }\n      } // Children can be more than one argument, and those are transferred onto\n      // the newly allocated props object.\n\n      var childrenLength = arguments.length - 2;\n      if (childrenLength === 1) {\n        props.children = children;\n      } else if (childrenLength > 1) {\n        var childArray = Array(childrenLength);\n        for (var i = 0; i < childrenLength; i++) {\n          childArray[i] = arguments[i + 2];\n        }\n        {\n          if (Object.freeze) {\n            Object.freeze(childArray);\n          }\n        }\n        props.children = childArray;\n      } // Resolve default props\n\n      if (type && type.defaultProps) {\n        var defaultProps = type.defaultProps;\n        for (propName in defaultProps) {\n          if (props[propName] === undefined) {\n            props[propName] = defaultProps[propName];\n          }\n        }\n      }\n      {\n        if (key || ref) {\n          var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;\n          if (key) {\n            defineKeyPropWarningGetter(props, displayName);\n          }\n          if (ref) {\n            defineRefPropWarningGetter(props, displayName);\n          }\n        }\n      }\n      return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);\n    }\n    function cloneAndReplaceKey(oldElement, newKey) {\n      var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);\n      return newElement;\n    }\n    /**\n     * Clone and return a new ReactElement using element as the starting point.\n     * See https://reactjs.org/docs/react-api.html#cloneelement\n     */\n\n    function cloneElement(element, config, children) {\n      if (!!(element === null || element === undefined)) {\n        {\n          throw Error(\"React.cloneElement(...): The argument must be a React element, but you passed \" + element + \".\");\n        }\n      }\n      var propName; // Original props are copied\n\n      var props = _assign({}, element.props); // Reserved names are extracted\n\n      var key = element.key;\n      var ref = element.ref; // Self is preserved since the owner is preserved.\n\n      var self = element._self; // Source is preserved since cloneElement is unlikely to be targeted by a\n      // transpiler, and the original source is probably a better indicator of the\n      // true owner.\n\n      var source = element._source; // Owner will be preserved, unless ref is overridden\n\n      var owner = element._owner;\n      if (config != null) {\n        if (hasValidRef(config)) {\n          // Silently steal the ref from the parent.\n          ref = config.ref;\n          owner = ReactCurrentOwner.current;\n        }\n        if (hasValidKey(config)) {\n          key = '' + config.key;\n        } // Remaining properties override existing props\n\n        var defaultProps;\n        if (element.type && element.type.defaultProps) {\n          defaultProps = element.type.defaultProps;\n        }\n        for (propName in config) {\n          if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {\n            if (config[propName] === undefined && defaultProps !== undefined) {\n              // Resolve default props\n              props[propName] = defaultProps[propName];\n            } else {\n              props[propName] = config[propName];\n            }\n          }\n        }\n      } // Children can be more than one argument, and those are transferred onto\n      // the newly allocated props object.\n\n      var childrenLength = arguments.length - 2;\n      if (childrenLength === 1) {\n        props.children = children;\n      } else if (childrenLength > 1) {\n        var childArray = Array(childrenLength);\n        for (var i = 0; i < childrenLength; i++) {\n          childArray[i] = arguments[i + 2];\n        }\n        props.children = childArray;\n      }\n      return ReactElement(element.type, key, ref, self, source, owner, props);\n    }\n    /**\n     * Verifies the object is a ReactElement.\n     * See https://reactjs.org/docs/react-api.html#isvalidelement\n     * @param {?object} object\n     * @return {boolean} True if `object` is a ReactElement.\n     * @final\n     */\n\n    function isValidElement(object) {\n      return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;\n    }\n    var SEPARATOR = '.';\n    var SUBSEPARATOR = ':';\n    /**\n     * Escape and wrap key so it is safe to use as a reactid\n     *\n     * @param {string} key to be escaped.\n     * @return {string} the escaped key.\n     */\n\n    function escape(key) {\n      var escapeRegex = /[=:]/g;\n      var escaperLookup = {\n        '=': '=0',\n        ':': '=2'\n      };\n      var escapedString = ('' + key).replace(escapeRegex, function (match) {\n        return escaperLookup[match];\n      });\n      return '$' + escapedString;\n    }\n    /**\n     * TODO: Test that a single child and an array with one item have the same key\n     * pattern.\n     */\n\n    var didWarnAboutMaps = false;\n    var userProvidedKeyEscapeRegex = /\\/+/g;\n    function escapeUserProvidedKey(text) {\n      return ('' + text).replace(userProvidedKeyEscapeRegex, '$&/');\n    }\n    var POOL_SIZE = 10;\n    var traverseContextPool = [];\n    function getPooledTraverseContext(mapResult, keyPrefix, mapFunction, mapContext) {\n      if (traverseContextPool.length) {\n        var traverseContext = traverseContextPool.pop();\n        traverseContext.result = mapResult;\n        traverseContext.keyPrefix = keyPrefix;\n        traverseContext.func = mapFunction;\n        traverseContext.context = mapContext;\n        traverseContext.count = 0;\n        return traverseContext;\n      } else {\n        return {\n          result: mapResult,\n          keyPrefix: keyPrefix,\n          func: mapFunction,\n          context: mapContext,\n          count: 0\n        };\n      }\n    }\n    function releaseTraverseContext(traverseContext) {\n      traverseContext.result = null;\n      traverseContext.keyPrefix = null;\n      traverseContext.func = null;\n      traverseContext.context = null;\n      traverseContext.count = 0;\n      if (traverseContextPool.length < POOL_SIZE) {\n        traverseContextPool.push(traverseContext);\n      }\n    }\n    /**\n     * @param {?*} children Children tree container.\n     * @param {!string} nameSoFar Name of the key path so far.\n     * @param {!function} callback Callback to invoke with each child found.\n     * @param {?*} traverseContext Used to pass information throughout the traversal\n     * process.\n     * @return {!number} The number of children in this subtree.\n     */\n\n    function traverseAllChildrenImpl(children, nameSoFar, callback, traverseContext) {\n      var type = typeof children;\n      if (type === 'undefined' || type === 'boolean') {\n        // All of the above are perceived as null.\n        children = null;\n      }\n      var invokeCallback = false;\n      if (children === null) {\n        invokeCallback = true;\n      } else {\n        switch (type) {\n          case 'string':\n          case 'number':\n            invokeCallback = true;\n            break;\n          case 'object':\n            switch (children.$$typeof) {\n              case REACT_ELEMENT_TYPE:\n              case REACT_PORTAL_TYPE:\n                invokeCallback = true;\n            }\n        }\n      }\n      if (invokeCallback) {\n        callback(traverseContext, children,\n        // If it's the only child, treat the name as if it was wrapped in an array\n        // so that it's consistent if the number of children grows.\n        nameSoFar === '' ? SEPARATOR + getComponentKey(children, 0) : nameSoFar);\n        return 1;\n      }\n      var child;\n      var nextName;\n      var subtreeCount = 0; // Count of children found in the current subtree.\n\n      var nextNamePrefix = nameSoFar === '' ? SEPARATOR : nameSoFar + SUBSEPARATOR;\n      if (Array.isArray(children)) {\n        for (var i = 0; i < children.length; i++) {\n          child = children[i];\n          nextName = nextNamePrefix + getComponentKey(child, i);\n          subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);\n        }\n      } else {\n        var iteratorFn = getIteratorFn(children);\n        if (typeof iteratorFn === 'function') {\n          {\n            // Warn about using Maps as children\n            if (iteratorFn === children.entries) {\n              if (!didWarnAboutMaps) {\n                warn('Using Maps as children is deprecated and will be removed in ' + 'a future major release. Consider converting children to ' + 'an array of keyed ReactElements instead.');\n              }\n              didWarnAboutMaps = true;\n            }\n          }\n          var iterator = iteratorFn.call(children);\n          var step;\n          var ii = 0;\n          while (!(step = iterator.next()).done) {\n            child = step.value;\n            nextName = nextNamePrefix + getComponentKey(child, ii++);\n            subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);\n          }\n        } else if (type === 'object') {\n          var addendum = '';\n          {\n            addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + ReactDebugCurrentFrame.getStackAddendum();\n          }\n          var childrenString = '' + children;\n          {\n            {\n              throw Error(\"Objects are not valid as a React child (found: \" + (childrenString === '[object Object]' ? 'object with keys {' + Object.keys(children).join(', ') + '}' : childrenString) + \").\" + addendum);\n            }\n          }\n        }\n      }\n      return subtreeCount;\n    }\n    /**\n     * Traverses children that are typically specified as `props.children`, but\n     * might also be specified through attributes:\n     *\n     * - `traverseAllChildren(this.props.children, ...)`\n     * - `traverseAllChildren(this.props.leftPanelChildren, ...)`\n     *\n     * The `traverseContext` is an optional argument that is passed through the\n     * entire traversal. It can be used to store accumulations or anything else that\n     * the callback might find relevant.\n     *\n     * @param {?*} children Children tree object.\n     * @param {!function} callback To invoke upon traversing each child.\n     * @param {?*} traverseContext Context for traversal.\n     * @return {!number} The number of children in this subtree.\n     */\n\n    function traverseAllChildren(children, callback, traverseContext) {\n      if (children == null) {\n        return 0;\n      }\n      return traverseAllChildrenImpl(children, '', callback, traverseContext);\n    }\n    /**\n     * Generate a key string that identifies a component within a set.\n     *\n     * @param {*} component A component that could contain a manual key.\n     * @param {number} index Index that is used if a manual key is not provided.\n     * @return {string}\n     */\n\n    function getComponentKey(component, index) {\n      // Do some typechecking here since we call this blindly. We want to ensure\n      // that we don't block potential future ES APIs.\n      if (typeof component === 'object' && component !== null && component.key != null) {\n        // Explicit key\n        return escape(component.key);\n      } // Implicit key determined by the index in the set\n\n      return index.toString(36);\n    }\n    function forEachSingleChild(bookKeeping, child, name) {\n      var func = bookKeeping.func,\n        context = bookKeeping.context;\n      func.call(context, child, bookKeeping.count++);\n    }\n    /**\n     * Iterates through children that are typically specified as `props.children`.\n     *\n     * See https://reactjs.org/docs/react-api.html#reactchildrenforeach\n     *\n     * The provided forEachFunc(child, index) will be called for each\n     * leaf child.\n     *\n     * @param {?*} children Children tree container.\n     * @param {function(*, int)} forEachFunc\n     * @param {*} forEachContext Context for forEachContext.\n     */\n\n    function forEachChildren(children, forEachFunc, forEachContext) {\n      if (children == null) {\n        return children;\n      }\n      var traverseContext = getPooledTraverseContext(null, null, forEachFunc, forEachContext);\n      traverseAllChildren(children, forEachSingleChild, traverseContext);\n      releaseTraverseContext(traverseContext);\n    }\n    function mapSingleChildIntoContext(bookKeeping, child, childKey) {\n      var result = bookKeeping.result,\n        keyPrefix = bookKeeping.keyPrefix,\n        func = bookKeeping.func,\n        context = bookKeeping.context;\n      var mappedChild = func.call(context, child, bookKeeping.count++);\n      if (Array.isArray(mappedChild)) {\n        mapIntoWithKeyPrefixInternal(mappedChild, result, childKey, function (c) {\n          return c;\n        });\n      } else if (mappedChild != null) {\n        if (isValidElement(mappedChild)) {\n          mappedChild = cloneAndReplaceKey(mappedChild,\n          // Keep both the (mapped) and old keys if they differ, just as\n          // traverseAllChildren used to do for objects as children\n          keyPrefix + (mappedChild.key && (!child || child.key !== mappedChild.key) ? escapeUserProvidedKey(mappedChild.key) + '/' : '') + childKey);\n        }\n        result.push(mappedChild);\n      }\n    }\n    function mapIntoWithKeyPrefixInternal(children, array, prefix, func, context) {\n      var escapedPrefix = '';\n      if (prefix != null) {\n        escapedPrefix = escapeUserProvidedKey(prefix) + '/';\n      }\n      var traverseContext = getPooledTraverseContext(array, escapedPrefix, func, context);\n      traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);\n      releaseTraverseContext(traverseContext);\n    }\n    /**\n     * Maps children that are typically specified as `props.children`.\n     *\n     * See https://reactjs.org/docs/react-api.html#reactchildrenmap\n     *\n     * The provided mapFunction(child, key, index) will be called for each\n     * leaf child.\n     *\n     * @param {?*} children Children tree container.\n     * @param {function(*, int)} func The map function.\n     * @param {*} context Context for mapFunction.\n     * @return {object} Object containing the ordered map of results.\n     */\n\n    function mapChildren(children, func, context) {\n      if (children == null) {\n        return children;\n      }\n      var result = [];\n      mapIntoWithKeyPrefixInternal(children, result, null, func, context);\n      return result;\n    }\n    /**\n     * Count the number of children that are typically specified as\n     * `props.children`.\n     *\n     * See https://reactjs.org/docs/react-api.html#reactchildrencount\n     *\n     * @param {?*} children Children tree container.\n     * @return {number} The number of children.\n     */\n\n    function countChildren(children) {\n      return traverseAllChildren(children, function () {\n        return null;\n      }, null);\n    }\n    /**\n     * Flatten a children object (typically specified as `props.children`) and\n     * return an array with appropriately re-keyed children.\n     *\n     * See https://reactjs.org/docs/react-api.html#reactchildrentoarray\n     */\n\n    function toArray(children) {\n      var result = [];\n      mapIntoWithKeyPrefixInternal(children, result, null, function (child) {\n        return child;\n      });\n      return result;\n    }\n    /**\n     * Returns the first child in a collection of children and verifies that there\n     * is only one child in the collection.\n     *\n     * See https://reactjs.org/docs/react-api.html#reactchildrenonly\n     *\n     * The current implementation of this function assumes that a single child gets\n     * passed without a wrapper, but the purpose of this helper function is to\n     * abstract away the particular structure of children.\n     *\n     * @param {?object} children Child collection structure.\n     * @return {ReactElement} The first and only `ReactElement` contained in the\n     * structure.\n     */\n\n    function onlyChild(children) {\n      if (!isValidElement(children)) {\n        {\n          throw Error(\"React.Children.only expected to receive a single React element child.\");\n        }\n      }\n      return children;\n    }\n    function createContext(defaultValue, calculateChangedBits) {\n      if (calculateChangedBits === undefined) {\n        calculateChangedBits = null;\n      } else {\n        {\n          if (calculateChangedBits !== null && typeof calculateChangedBits !== 'function') {\n            error('createContext: Expected the optional second argument to be a ' + 'function. Instead received: %s', calculateChangedBits);\n          }\n        }\n      }\n      var context = {\n        $$typeof: REACT_CONTEXT_TYPE,\n        _calculateChangedBits: calculateChangedBits,\n        // As a workaround to support multiple concurrent renderers, we categorize\n        // some renderers as primary and others as secondary. We only expect\n        // there to be two concurrent renderers at most: React Native (primary) and\n        // Fabric (secondary); React DOM (primary) and React ART (secondary).\n        // Secondary renderers store their context values on separate fields.\n        _currentValue: defaultValue,\n        _currentValue2: defaultValue,\n        // Used to track how many concurrent renderers this context currently\n        // supports within in a single renderer. Such as parallel server rendering.\n        _threadCount: 0,\n        // These are circular\n        Provider: null,\n        Consumer: null\n      };\n      context.Provider = {\n        $$typeof: REACT_PROVIDER_TYPE,\n        _context: context\n      };\n      var hasWarnedAboutUsingNestedContextConsumers = false;\n      var hasWarnedAboutUsingConsumerProvider = false;\n      {\n        // A separate object, but proxies back to the original context object for\n        // backwards compatibility. It has a different $$typeof, so we can properly\n        // warn for the incorrect usage of Context as a Consumer.\n        var Consumer = {\n          $$typeof: REACT_CONTEXT_TYPE,\n          _context: context,\n          _calculateChangedBits: context._calculateChangedBits\n        }; // $FlowFixMe: Flow complains about not setting a value, which is intentional here\n\n        Object.defineProperties(Consumer, {\n          Provider: {\n            get: function get() {\n              if (!hasWarnedAboutUsingConsumerProvider) {\n                hasWarnedAboutUsingConsumerProvider = true;\n                error('Rendering <Context.Consumer.Provider> is not supported and will be removed in ' + 'a future major release. Did you mean to render <Context.Provider> instead?');\n              }\n              return context.Provider;\n            },\n            set: function set(_Provider) {\n              context.Provider = _Provider;\n            }\n          },\n          _currentValue: {\n            get: function get() {\n              return context._currentValue;\n            },\n            set: function set(_currentValue) {\n              context._currentValue = _currentValue;\n            }\n          },\n          _currentValue2: {\n            get: function get() {\n              return context._currentValue2;\n            },\n            set: function set(_currentValue2) {\n              context._currentValue2 = _currentValue2;\n            }\n          },\n          _threadCount: {\n            get: function get() {\n              return context._threadCount;\n            },\n            set: function set(_threadCount) {\n              context._threadCount = _threadCount;\n            }\n          },\n          Consumer: {\n            get: function get() {\n              if (!hasWarnedAboutUsingNestedContextConsumers) {\n                hasWarnedAboutUsingNestedContextConsumers = true;\n                error('Rendering <Context.Consumer.Consumer> is not supported and will be removed in ' + 'a future major release. Did you mean to render <Context.Consumer> instead?');\n              }\n              return context.Consumer;\n            }\n          }\n        }); // $FlowFixMe: Flow complains about missing properties because it doesn't understand defineProperty\n\n        context.Consumer = Consumer;\n      }\n      {\n        context._currentRenderer = null;\n        context._currentRenderer2 = null;\n      }\n      return context;\n    }\n    function lazy(ctor) {\n      var lazyType = {\n        $$typeof: REACT_LAZY_TYPE,\n        _ctor: ctor,\n        // React uses these fields to store the result.\n        _status: -1,\n        _result: null\n      };\n      {\n        // In production, this would just set it on the object.\n        var defaultProps;\n        var propTypes;\n        Object.defineProperties(lazyType, {\n          defaultProps: {\n            configurable: true,\n            get: function get() {\n              return defaultProps;\n            },\n            set: function set(newDefaultProps) {\n              error('React.lazy(...): It is not supported to assign `defaultProps` to ' + 'a lazy component import. Either specify them where the component ' + 'is defined, or create a wrapping component around it.');\n              defaultProps = newDefaultProps; // Match production behavior more closely:\n\n              Object.defineProperty(lazyType, 'defaultProps', {\n                enumerable: true\n              });\n            }\n          },\n          propTypes: {\n            configurable: true,\n            get: function get() {\n              return propTypes;\n            },\n            set: function set(newPropTypes) {\n              error('React.lazy(...): It is not supported to assign `propTypes` to ' + 'a lazy component import. Either specify them where the component ' + 'is defined, or create a wrapping component around it.');\n              propTypes = newPropTypes; // Match production behavior more closely:\n\n              Object.defineProperty(lazyType, 'propTypes', {\n                enumerable: true\n              });\n            }\n          }\n        });\n      }\n      return lazyType;\n    }\n    function forwardRef(render) {\n      {\n        if (render != null && render.$$typeof === REACT_MEMO_TYPE) {\n          error('forwardRef requires a render function but received a `memo` ' + 'component. Instead of forwardRef(memo(...)), use ' + 'memo(forwardRef(...)).');\n        } else if (typeof render !== 'function') {\n          error('forwardRef requires a render function but was given %s.', render === null ? 'null' : typeof render);\n        } else {\n          if (render.length !== 0 && render.length !== 2) {\n            error('forwardRef render functions accept exactly two parameters: props and ref. %s', render.length === 1 ? 'Did you forget to use the ref parameter?' : 'Any additional parameter will be undefined.');\n          }\n        }\n        if (render != null) {\n          if (render.defaultProps != null || render.propTypes != null) {\n            error('forwardRef render functions do not support propTypes or defaultProps. ' + 'Did you accidentally pass a React component?');\n          }\n        }\n      }\n      return {\n        $$typeof: REACT_FORWARD_REF_TYPE,\n        render: render\n      };\n    }\n    function isValidElementType(type) {\n      return typeof type === 'string' || typeof type === 'function' ||\n      // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.\n      type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === 'object' && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);\n    }\n    function memo(type, compare) {\n      {\n        if (!isValidElementType(type)) {\n          error('memo: The first argument must be a component. Instead ' + 'received: %s', type === null ? 'null' : typeof type);\n        }\n      }\n      return {\n        $$typeof: REACT_MEMO_TYPE,\n        type: type,\n        compare: compare === undefined ? null : compare\n      };\n    }\n    function resolveDispatcher() {\n      var dispatcher = ReactCurrentDispatcher.current;\n      if (!(dispatcher !== null)) {\n        {\n          throw Error(\"Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\\n1. You might have mismatching versions of React and the renderer (such as React DOM)\\n2. You might be breaking the Rules of Hooks\\n3. You might have more than one copy of React in the same app\\nSee https://fb.me/react-invalid-hook-call for tips about how to debug and fix this problem.\");\n        }\n      }\n      return dispatcher;\n    }\n    function useContext(Context, unstable_observedBits) {\n      var dispatcher = resolveDispatcher();\n      {\n        if (unstable_observedBits !== undefined) {\n          error('useContext() second argument is reserved for future ' + 'use in React. Passing it is not supported. ' + 'You passed: %s.%s', unstable_observedBits, typeof unstable_observedBits === 'number' && Array.isArray(arguments[2]) ? '\\n\\nDid you call array.map(useContext)? ' + 'Calling Hooks inside a loop is not supported. ' + 'Learn more at https://fb.me/rules-of-hooks' : '');\n        } // TODO: add a more generic warning for invalid values.\n\n        if (Context._context !== undefined) {\n          var realContext = Context._context; // Don't deduplicate because this legitimately causes bugs\n          // and nobody should be using this in existing code.\n\n          if (realContext.Consumer === Context) {\n            error('Calling useContext(Context.Consumer) is not supported, may cause bugs, and will be ' + 'removed in a future major release. Did you mean to call useContext(Context) instead?');\n          } else if (realContext.Provider === Context) {\n            error('Calling useContext(Context.Provider) is not supported. ' + 'Did you mean to call useContext(Context) instead?');\n          }\n        }\n      }\n      return dispatcher.useContext(Context, unstable_observedBits);\n    }\n    function useState(initialState) {\n      var dispatcher = resolveDispatcher();\n      return dispatcher.useState(initialState);\n    }\n    function useReducer(reducer, initialArg, init) {\n      var dispatcher = resolveDispatcher();\n      return dispatcher.useReducer(reducer, initialArg, init);\n    }\n    function useRef(initialValue) {\n      var dispatcher = resolveDispatcher();\n      return dispatcher.useRef(initialValue);\n    }\n    function useEffect(create, deps) {\n      var dispatcher = resolveDispatcher();\n      return dispatcher.useEffect(create, deps);\n    }\n    function useLayoutEffect(create, deps) {\n      var dispatcher = resolveDispatcher();\n      return dispatcher.useLayoutEffect(create, deps);\n    }\n    function useCallback(callback, deps) {\n      var dispatcher = resolveDispatcher();\n      return dispatcher.useCallback(callback, deps);\n    }\n    function useMemo(create, deps) {\n      var dispatcher = resolveDispatcher();\n      return dispatcher.useMemo(create, deps);\n    }\n    function useImperativeHandle(ref, create, deps) {\n      var dispatcher = resolveDispatcher();\n      return dispatcher.useImperativeHandle(ref, create, deps);\n    }\n    function useDebugValue(value, formatterFn) {\n      {\n        var dispatcher = resolveDispatcher();\n        return dispatcher.useDebugValue(value, formatterFn);\n      }\n    }\n    var propTypesMisspellWarningShown;\n    {\n      propTypesMisspellWarningShown = false;\n    }\n    function getDeclarationErrorAddendum() {\n      if (ReactCurrentOwner.current) {\n        var name = getComponentName(ReactCurrentOwner.current.type);\n        if (name) {\n          return '\\n\\nCheck the render method of `' + name + '`.';\n        }\n      }\n      return '';\n    }\n    function getSourceInfoErrorAddendum(source) {\n      if (source !== undefined) {\n        var fileName = source.fileName.replace(/^.*[\\\\\\/]/, '');\n        var lineNumber = source.lineNumber;\n        return '\\n\\nCheck your code at ' + fileName + ':' + lineNumber + '.';\n      }\n      return '';\n    }\n    function getSourceInfoErrorAddendumForProps(elementProps) {\n      if (elementProps !== null && elementProps !== undefined) {\n        return getSourceInfoErrorAddendum(elementProps.__source);\n      }\n      return '';\n    }\n    /**\n     * Warn if there's no key explicitly set on dynamic arrays of children or\n     * object keys are not valid. This allows us to keep track of children between\n     * updates.\n     */\n\n    var ownerHasKeyUseWarning = {};\n    function getCurrentComponentErrorInfo(parentType) {\n      var info = getDeclarationErrorAddendum();\n      if (!info) {\n        var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;\n        if (parentName) {\n          info = \"\\n\\nCheck the top-level render call using <\" + parentName + \">.\";\n        }\n      }\n      return info;\n    }\n    /**\n     * Warn if the element doesn't have an explicit key assigned to it.\n     * This element is in an array. The array could grow and shrink or be\n     * reordered. All children that haven't already been validated are required to\n     * have a \"key\" property assigned to it. Error statuses are cached so a warning\n     * will only be shown once.\n     *\n     * @internal\n     * @param {ReactElement} element Element that requires a key.\n     * @param {*} parentType element's parent's type.\n     */\n\n    function validateExplicitKey(element, parentType) {\n      if (!element._store || element._store.validated || element.key != null) {\n        return;\n      }\n      element._store.validated = true;\n      var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);\n      if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {\n        return;\n      }\n      ownerHasKeyUseWarning[currentComponentErrorInfo] = true; // Usually the current owner is the offender, but if it accepts children as a\n      // property, it may be the creator of the child that's responsible for\n      // assigning it a key.\n\n      var childOwner = '';\n      if (element && element._owner && element._owner !== ReactCurrentOwner.current) {\n        // Give the component that originally created this child.\n        childOwner = \" It was passed a child from \" + getComponentName(element._owner.type) + \".\";\n      }\n      setCurrentlyValidatingElement(element);\n      {\n        error('Each child in a list should have a unique \"key\" prop.' + '%s%s See https://fb.me/react-warning-keys for more information.', currentComponentErrorInfo, childOwner);\n      }\n      setCurrentlyValidatingElement(null);\n    }\n    /**\n     * Ensure that every element either is passed in a static location, in an\n     * array with an explicit keys property defined, or in an object literal\n     * with valid key property.\n     *\n     * @internal\n     * @param {ReactNode} node Statically passed child of any type.\n     * @param {*} parentType node's parent's type.\n     */\n\n    function validateChildKeys(node, parentType) {\n      if (typeof node !== 'object') {\n        return;\n      }\n      if (Array.isArray(node)) {\n        for (var i = 0; i < node.length; i++) {\n          var child = node[i];\n          if (isValidElement(child)) {\n            validateExplicitKey(child, parentType);\n          }\n        }\n      } else if (isValidElement(node)) {\n        // This element was passed in a valid location.\n        if (node._store) {\n          node._store.validated = true;\n        }\n      } else if (node) {\n        var iteratorFn = getIteratorFn(node);\n        if (typeof iteratorFn === 'function') {\n          // Entry iterators used to provide implicit keys,\n          // but now we print a separate warning for them later.\n          if (iteratorFn !== node.entries) {\n            var iterator = iteratorFn.call(node);\n            var step;\n            while (!(step = iterator.next()).done) {\n              if (isValidElement(step.value)) {\n                validateExplicitKey(step.value, parentType);\n              }\n            }\n          }\n        }\n      }\n    }\n    /**\n     * Given an element, validate that its props follow the propTypes definition,\n     * provided by the type.\n     *\n     * @param {ReactElement} element\n     */\n\n    function validatePropTypes(element) {\n      {\n        var type = element.type;\n        if (type === null || type === undefined || typeof type === 'string') {\n          return;\n        }\n        var name = getComponentName(type);\n        var propTypes;\n        if (typeof type === 'function') {\n          propTypes = type.propTypes;\n        } else if (typeof type === 'object' && (type.$$typeof === REACT_FORWARD_REF_TYPE ||\n        // Note: Memo only checks outer props here.\n        // Inner props are checked in the reconciler.\n        type.$$typeof === REACT_MEMO_TYPE)) {\n          propTypes = type.propTypes;\n        } else {\n          return;\n        }\n        if (propTypes) {\n          setCurrentlyValidatingElement(element);\n          checkPropTypes(propTypes, element.props, 'prop', name, ReactDebugCurrentFrame.getStackAddendum);\n          setCurrentlyValidatingElement(null);\n        } else if (type.PropTypes !== undefined && !propTypesMisspellWarningShown) {\n          propTypesMisspellWarningShown = true;\n          error('Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?', name || 'Unknown');\n        }\n        if (typeof type.getDefaultProps === 'function' && !type.getDefaultProps.isReactClassApproved) {\n          error('getDefaultProps is only used on classic React.createClass ' + 'definitions. Use a static property named `defaultProps` instead.');\n        }\n      }\n    }\n    /**\n     * Given a fragment, validate that it can only be provided with fragment props\n     * @param {ReactElement} fragment\n     */\n\n    function validateFragmentProps(fragment) {\n      {\n        setCurrentlyValidatingElement(fragment);\n        var keys = Object.keys(fragment.props);\n        for (var i = 0; i < keys.length; i++) {\n          var key = keys[i];\n          if (key !== 'children' && key !== 'key') {\n            error('Invalid prop `%s` supplied to `React.Fragment`. ' + 'React.Fragment can only have `key` and `children` props.', key);\n            break;\n          }\n        }\n        if (fragment.ref !== null) {\n          error('Invalid attribute `ref` supplied to `React.Fragment`.');\n        }\n        setCurrentlyValidatingElement(null);\n      }\n    }\n    function createElementWithValidation(type, props, children) {\n      var validType = isValidElementType(type); // We warn in this case but don't throw. We expect the element creation to\n      // succeed and there will likely be errors in render.\n\n      if (!validType) {\n        var info = '';\n        if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {\n          info += ' You likely forgot to export your component from the file ' + \"it's defined in, or you might have mixed up default and named imports.\";\n        }\n        var sourceInfo = getSourceInfoErrorAddendumForProps(props);\n        if (sourceInfo) {\n          info += sourceInfo;\n        } else {\n          info += getDeclarationErrorAddendum();\n        }\n        var typeString;\n        if (type === null) {\n          typeString = 'null';\n        } else if (Array.isArray(type)) {\n          typeString = 'array';\n        } else if (type !== undefined && type.$$typeof === REACT_ELEMENT_TYPE) {\n          typeString = \"<\" + (getComponentName(type.type) || 'Unknown') + \" />\";\n          info = ' Did you accidentally export a JSX literal instead of a component?';\n        } else {\n          typeString = typeof type;\n        }\n        {\n          error('React.createElement: type is invalid -- expected a string (for ' + 'built-in components) or a class/function (for composite ' + 'components) but got: %s.%s', typeString, info);\n        }\n      }\n      var element = createElement.apply(this, arguments); // The result can be nullish if a mock or a custom function is used.\n      // TODO: Drop this when these are no longer allowed as the type argument.\n\n      if (element == null) {\n        return element;\n      } // Skip key warning if the type isn't valid since our key validation logic\n      // doesn't expect a non-string/function type and can throw confusing errors.\n      // We don't want exception behavior to differ between dev and prod.\n      // (Rendering will throw with a helpful message and as soon as the type is\n      // fixed, the key warnings will appear.)\n\n      if (validType) {\n        for (var i = 2; i < arguments.length; i++) {\n          validateChildKeys(arguments[i], type);\n        }\n      }\n      if (type === REACT_FRAGMENT_TYPE) {\n        validateFragmentProps(element);\n      } else {\n        validatePropTypes(element);\n      }\n      return element;\n    }\n    var didWarnAboutDeprecatedCreateFactory = false;\n    function createFactoryWithValidation(type) {\n      var validatedFactory = createElementWithValidation.bind(null, type);\n      validatedFactory.type = type;\n      {\n        if (!didWarnAboutDeprecatedCreateFactory) {\n          didWarnAboutDeprecatedCreateFactory = true;\n          warn('React.createFactory() is deprecated and will be removed in ' + 'a future major release. Consider using JSX ' + 'or use React.createElement() directly instead.');\n        } // Legacy hook: remove it\n\n        Object.defineProperty(validatedFactory, 'type', {\n          enumerable: false,\n          get: function get() {\n            warn('Factory.type is deprecated. Access the class directly ' + 'before passing it to createFactory.');\n            Object.defineProperty(this, 'type', {\n              value: type\n            });\n            return type;\n          }\n        });\n      }\n      return validatedFactory;\n    }\n    function cloneElementWithValidation(element, props, children) {\n      var newElement = cloneElement.apply(this, arguments);\n      for (var i = 2; i < arguments.length; i++) {\n        validateChildKeys(arguments[i], newElement.type);\n      }\n      validatePropTypes(newElement);\n      return newElement;\n    }\n    {\n      try {\n        var frozenObject = Object.freeze({});\n        var testMap = new Map([[frozenObject, null]]);\n        var testSet = new Set([frozenObject]); // This is necessary for Rollup to not consider these unused.\n        // https://github.com/rollup/rollup/issues/1771\n        // TODO: we can remove these if Rollup fixes the bug.\n\n        testMap.set(0, 0);\n        testSet.add(0);\n      } catch (e) {}\n    }\n    var createElement$1 = createElementWithValidation;\n    var cloneElement$1 = cloneElementWithValidation;\n    var createFactory = createFactoryWithValidation;\n    var Children = {\n      map: mapChildren,\n      forEach: forEachChildren,\n      count: countChildren,\n      toArray: toArray,\n      only: onlyChild\n    };\n    exports.Children = Children;\n    exports.Component = Component;\n    exports.Fragment = REACT_FRAGMENT_TYPE;\n    exports.Profiler = REACT_PROFILER_TYPE;\n    exports.PureComponent = PureComponent;\n    exports.StrictMode = REACT_STRICT_MODE_TYPE;\n    exports.Suspense = REACT_SUSPENSE_TYPE;\n    exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = ReactSharedInternals;\n    exports.cloneElement = cloneElement$1;\n    exports.createContext = createContext;\n    exports.createElement = createElement$1;\n    exports.createFactory = createFactory;\n    exports.createRef = createRef;\n    exports.forwardRef = forwardRef;\n    exports.isValidElement = isValidElement;\n    exports.lazy = lazy;\n    exports.memo = memo;\n    exports.useCallback = useCallback;\n    exports.useContext = useContext;\n    exports.useDebugValue = useDebugValue;\n    exports.useEffect = useEffect;\n    exports.useImperativeHandle = useImperativeHandle;\n    exports.useLayoutEffect = useLayoutEffect;\n    exports.useMemo = useMemo;\n    exports.useReducer = useReducer;\n    exports.useRef = useRef;\n    exports.useState = useState;\n    exports.version = ReactVersion;\n  })();\n}\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./node_modules/react/cjs/react.development.js?\n}");

/***/ },

/***/ "./node_modules/react/index.js"
/*!*************************************!*\
  !*** ./node_modules/react/index.js ***!
  \*************************************/
(module, __unused_webpack_exports, __webpack_require__) {

"use strict";
eval("{\n\nif (false) // removed by dead control flow\n{} else {\n  module.exports = __webpack_require__(/*! ./cjs/react.development.js */ \"./node_modules/react/cjs/react.development.js\");\n}\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./node_modules/react/index.js?\n}");

/***/ }

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		if (!(moduleId in __webpack_modules__)) {
/******/ 			delete __webpack_module_cache__[moduleId];
/******/ 			var e = new Error("Cannot find module '" + moduleId + "'");
/******/ 			e.code = 'MODULE_NOT_FOUND';
/******/ 			throw e;
/******/ 		}
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module is referenced by other modules so it can't be inlined
/******/ 	var __webpack_exports__ = __webpack_require__("./index.ts");
/******/ 	pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad = __webpack_exports__;
/******/ 	
/******/ })()
;
if (window.ComponentFramework && window.ComponentFramework.registerControl) {
	ComponentFramework.registerControl('Maqsad.EmailEditorControl', pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad.EmailEditorControl);
} else {
	var Maqsad = Maqsad || {};
	Maqsad.EmailEditorControl = pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad.EmailEditorControl;
	pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad = undefined;
}