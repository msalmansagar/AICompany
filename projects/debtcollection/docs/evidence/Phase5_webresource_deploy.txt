=== Deploying the Collection Workspace web resource ===

  Organisation: org5869857f.crm4.dynamics.com

─── The build artefact ───
  PASS  A single self-contained artefact exists — 344 KB
  PASS  It references no external script or stylesheet — nothing to resolve as a sibling under /WebResources
  PASS  It uses no absolute asset path — an absolute /assets path would 404 under a web-resource URL

─── Upload ───
  PASS  Existing web resource updated — 0bd4fee5-97b3-f111-aaac-000d3abd8313

─── Publish ───
  PASS  Published — only this web resource — PublishXml accepted

─── Verification ───
  PASS  The web resource reads back from the organisation — qdb_dcp_workspace.html (DCP — Collection Workspace)
  PASS  It is stored as HTML — type=1
  PASS  The stored content matches the artefact byte for byte — stored 343 KB, built 343 KB
  PASS  The design tokens survived the round trip
  PASS  The workspace host element survived

  Open it at:
    https://org5869857f.crm4.dynamics.com/main.aspx?pagetype=webresource&webresourceName=qdb_dcp_workspace.html

  NOT proven by this script: that the workspace loads inside Dynamics, that
  getGlobalContext answers, or that it can read data as the signed-in user.
  Those need a browser with a CRM session and are reported as pending.

=== 10/10 checks passed ===
