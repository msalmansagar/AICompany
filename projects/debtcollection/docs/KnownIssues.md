# DCP — Known Issues (Phase 0)

**As of 2026-09-17.** Severity: 🔴 blocks a target-architecture requirement · 🟠 wrong today but a
contained fix · 🟡 limitation / unknown to track. Status values: Open · Held (needs user decision) ·
TBD — Requires QDB Confirmation · Accepted (by design for now).

| ID | Issue | Sev | Evidence | Impact | Workaround / plan | Owner | Status |
|---|---|---|---|---|---|---|---|
| KI-01 | `msst_dcpcollectioncase` has `IsValidForQueue = false`; `AddToQueueRequest` fails ("This object type cannot be added to a queue"). The FR-097 queue move has **never** worked. | 🔴 | Live smoke 14/15; `StopContactQueueMover` error isolation surfaced it 2026-09-16; `grep IsValidForQueue crm/scripts` = nothing | Deceased/stop-contact cases never reach the Deceased & Insurance queue | Set `IsValidForQueue: true` in `qdb_collectioncase` definition at creation (MP §59); add a queue-move assertion to smoke. On the old entity = live metadata PATCH, held. | crm-onprem | **RESOLVED for the canonical schema 2026-09-17** — `qdb_collectioncase` was created with `IsValidForQueue = true` and verified live (`verify-qdb-schema.mjs`). The `msst_` entity is unchanged and still carries the defect; it is retired with that schema, not patched. |
| KI-02 | Web API version `9.2` hard-coded in application source: `apps/api/src/app.ts` ×2, `plugins/org-router.ts` ×2, literal `/api/data/v9.2/$metadata` in `routes/health.ts`. On-prem 9.1 exposes v9.1. | 🔴 | grep; `buildODataUrl` already parameterises it | Service cannot run against on-prem without a source change — violates CP §2 | `DV_API_VERSION` per org in config; browser side from `getGlobalContext().getVersion()` | backend | Open (Phase 1) |
| KI-03 | Provisioning / registration / smoke scripts authenticate only against `login.microsoftonline.com` (`crm/scripts/lib/crm-client.mjs:43`, `.default` scope). | 🔴 | code; DFE on-prem README records the same limitation | No DCP tooling can run against on-prem; violates MP §83 (artefacts for both targets) | Tooling auth adapter (Entra / AD FS) + solution.zip export for PRT/solution-import path | devops | Open (Phase 1) |
| KI-04 | Shared sandbox org5869857f holds only undeletable machine-made rows: 13 customers / 15 cases / 10 snapshots / 60 audit rows (`SMOKE …`, `DIAG …`). `ImmutabilityGuard` blocks Delete, sysadmin included; every smoke run adds more. | 🟠 | Live query 2026-09-16 | Org pollution; retirement of `msst_` entities blocked by the guard; EDP/CWFD/DFE share the org | Disable guard steps → delete → re-enable (weakens a control on a shared org). Longer term: smoke against an isolated org or self-cleaning test data. | user decision | **Closed for the `qdb_` schema 2026-09-17** — the authorised cleanup removed every `SMOKE-*` row from the canonical tables and `clean-qdb-smoke-data.mjs` makes it repeatable, so the canonical tables no longer accumulate residue. The original `msst_` rows (13 customers / 15 cases / 10 snapshots / 60 audit rows) were **not** in scope and remain; the legacy guard was never disabled. |
| KI-05 | `AdfsAdapter` has never been exercised against a real AD FS 2019 endpoint (COND-DCP-008). | 🟠 | `packages/auth-adapters/README.md`; tests use a mock OIDC issuer | On-prem service auth is "compatible by design" only; AD FS may need `resource=` or certificate S2S | Prove against QDB AD FS as the first on-prem task | backend | TBD — Requires QDB Confirmation (AD FS access) |
| KI-06 | API data routes (`/customers/:qid`, `/identity-exceptions`) need a *user* bearer token that cannot be minted headlessly; only the service layer is proven live. | 🟡 | 2026-09-16 boot notes | HTTP path unproven end-to-end | Routes are slated for retirement under MP §60 (React → Xrm.WebApi); prove remaining routes with an interactive token | backend | Accepted |
| KI-07 | Form Engine on-prem: designer loads blank (5 assets 404); on-prem plugin predates the 2026-07-28 rule fix; DFE code is not on `main`. | 🟠 | DFE memory + `onprem-deploy/2026-09-16/README.md` | `IFormEngine` reuse (MP §45) has no proven on-prem runtime | Close the DFE on-prem kit; until then DCP forms = "Compatible by Design — Runtime Test Pending" | DFE owner | Open (outside DCP) |
| KI-08 | EDP (Rule Engine) and DFE both document an on-prem path (Process Actions instead of Custom API, same plugin code) but **neither has been runtime-validated on an on-prem org**. Whether Custom API exists on QDB's on-prem 9.1 build is itself unconfirmed. | 🟡 | `enterprise-decision-platform/deploy/onprem/ONPREM.md` ("Not yet validated"); DFE `PLUGIN-REGISTRATION.md` §3b ("may be unavailable") | Engine facades are compatible by design only | DCP adopts the same dual surface (Custom API cloud / Process Action on-prem); validate once an on-prem org is available | architect | TBD — Requires QDB Confirmation |
| KI-09 | Smart Assignment: no artefact, contract or org evidence in this repository or under `D:\QDB` (no folder, no mention). | 🟡 | `find`/`grep` 2026-09-17 | `IAssignmentEngine` cannot be designed beyond an interface | `qdb_assignmentconfiguration` carries a `qdb_smartassignmentref` placeholder | QDB | TBD — Requires QDB Confirmation |
| KI-10 | Supplied Excel report renders the `1-30` bucket as the date `2026-01-30` (both workbooks, all sheets). | 🟡 | exceljs read: bucket values `{2026-01-30, 31-60, …}` | A naive import would create an eleventh bucket | MIS adapter normalises to canonical `1-30`; mock seed does the same; contract test asserts exactly 10 codes | backend | Accepted |
| KI-11 | Exposure segmentation conflict: MP §41 lists Exposure From/To on the strategy; BRD FR-034 forbids segmenting by exposure; HL portfolio analysis shows exposure carries no signal *in that population* (balance QAR 680–920k in every bucket). | 🟡 | `phase-2-ba.md` FR-034; `facts-and-analysis.md`; `HousingLoanDataAnalysis.md` F9 | Rule definitions could diverge from the BRD | **Architecture position (F9, 2026-09-17):** exposure is **retained** as an optional configurable Strategy/Assignment criterion — configured off for HL, available for BFD, never removed from the model. HL statistics must not become cross-platform policy. **This does not amend FR-034**, which remains in force until QDB approves the amendment — see KI-32 row (a). | ba | **Proposed BRD Amendment — Awaiting QDB Approval** (tracked under KI-32) |
| KI-12 | `projects/debtcollection/` is **untracked** on the checked-out branch `feat/dfe-six-point-batch`; history lives on `feat/dcp-phase1-foundation` @ `95dcb04d` (origin). | 🟠 | `git status` 2026-09-16 | Any DCP commit made here would land on the DFE branch | Merge `feat/dcp-phase1-foundation` to main (user's call) or check the branch out | user decision | Held |
| KI-13 | `crm/plugins/Qdb.DebtCollection.Plugins/key.snk` (path renamed in Phase 1) is gitignored — the strong-name key exists only on disk (`PublicKeyToken=f70ee8facd683d55`). | 🟠 | `.gitignore`; copied by hand during the worktree consolidation | Losing it = a new assembly identity that can never re-register over the org's assembly | Back up out-of-band; decide reuse vs QDB-owned key for `Qdb.*` (SchemaMigration §1.5) | user | Held |
| KI-14 | No solution package, no `solution.xml`, no CI/CD, no artefact for either target; schema exists only as Web-API scripts. | 🔴 | repo | MP §83 unmet; on-prem import impossible today | Phase 1: solution export + two package pipelines | devops | Open (Phase 1) |
| KI-15 | The 22-screen prototype is HTML + hard-coded `mock-data.js`, zero network calls; no `apps/web` exists. | 🟡 | grep fetch/Xrm/XMLHttpRequest = 0 | Must never be recorded as implemented UI (MP §74, §94) | Tracker status "Mock" | frontend | Accepted |
| KI-16 | `Exemption Amount` is **negative** in the report (e.g. −472,313.64); sign convention unknown. | 🟡 | Detailed sheet, 458 rows | Wrong sign in the canonical model would misstate arrears after exemption | Store as supplied; do not derive until confirmed | QDB MIS | TBD — Requires QDB Confirmation |
| KI-17 | `Account Status` values are only `7` and `8`; meaning unknown. | 🟡 | Detailed sheet, 4,357 rows | Cannot map to NPL/active/closed semantics | Keep as `accountStatusCode` string; no business rule on it yet | QDB MIS | TBD — Requires QDB Confirmation |
| KI-18 | Breakdown bucket-customer total (3,905) ≠ distinct customers (3,777); 4,357 accounts. A customer with accounts in two buckets is counted in both. | 🟡 | Breakdown sum row; Detailed distinct counts | Dashboard totals must be labelled "bucket customers", not "customers"; unit of record is the account | Canonical model is per facility; mock reproduces the double count | backend | Accepted |
| KI-19 | Existing HL/BFD facility entity logical names unknown; 3C Merging workbook shows loan-balance columns on `account` but no facility entity. | 🟡 | `3C Merging Entities Schema.xlsx` | `qdb_facilityid` lookup target cannot be fixed; only `qdb_facilitynumber` is certain | Lookup added per org at deployment | QDB | TBD — Requires QDB Confirmation |
| KI-20 | Existing Fax → SMS/WhatsApp trigger mechanism unknown. | 🟡 | MP §32, CP §40 | Communication Service cannot be completed for Phase 7 | Not invented; documented as TBD | QDB | TBD — Requires QDB Confirmation |
| KI-21 | Rows in the C# `StatusTransitionMatrix` / `ActivitySubjectComposer` hold option-set integers that will not match `qdb_` publisher-prefixed values. | 🟠 | option values are publisher-bound | Plugins would validate against wrong codes after migration | **RESOLVED 2026-09-17.** `StatusTransitionMatrix` now carries the provisioned `qdb` values (case 100000600–616, activity 100000640–645, PTP 100000080–085), verified against the organisation by `verify-qdb-schema.mjs`; `ActivitySubjectComposer` no longer maps option-set codes at all — it reads the activity type record, so there is nothing left to drift. | crm-onprem | **Resolved** |
| KI-22 | Every "live-verified" DCP result comes from the cloud sandbox; no on-prem org has ever been used for DCP. | 🟡 | session history | Tracker on-prem columns = "Runtime Test Pending" across the board (CP §1 — access constraint, not architecture) | Obtain an on-prem 9.1 org for DCP | QDB | TBD — Requires QDB Confirmation |

## Addendum 2026-09-17 — existing BFD schema on the sandbox (`ExistingQdbSchemaAssessment.md`)

| ID | Severity | Issue | Evidence | Impact | Workaround / decision | Status |
|---|---|---|---|---|---|---|
| KI-23 | High | An **existing DA collections module** (`qdb_da_case`, `qdb_da_configuration`, `qdb_collections_task`, site visit, staff, reasons) exists in the BFD schema; DCP's `qdb_collectioncase` overlaps it functionally | read-only metadata sweep of org5869857f; 0 rows on this sandbox, 7 `qdb_collection_reason` rows. The sandbox is a schema copy — row counts say nothing about production use at BFD | Duplicate collections models if both run; live status, production data, dependent processes and user base at BFD all unknown | **`qdb_collectioncase` is a PROPOSED successor only — pending QDB confirmation and a coexistence/migration assessment** (`ExistingQdbSchemaAssessment.md` §3). **No retirement, freezing, migration, replacement or disabling is approved**, and DA entities must not be extended by DCP. Six QDB questions must be answered first (§3.3): live? production data? which teams? dependent integrations/processes/plugins? historical data required? coexistence required? | `TBD — Requires QDB Confirmation` |
| KI-24 | Medium | Proposed `qdb_integrationlog` near-collided with `qdb_integrationlogs`; **both now withdrawn — QDB confirmed reuse of existing `qdb_crmlogs`** | sandbox metadata + QDB gate decision | Confusing duplicate technical logs | **Final:** both withdrawn; DCP reuses the existing **`qdb_crmlogs`** (QDB gate decision). Mapping, gaps and proposed extensions in `QdbCrmLogsReuseAssessment.md`; extension permission `TBD — Requires QDB Confirmation` | Resolved by QDB decision |
| KI-25 | Medium | `account` (BFD) has **no** stop-contact / deceased / vulnerability / language columns; `contact` (HL) unknown | sandbox metadata (577 `qdb_` account columns inspected by keyword) | CP §7 flags are additions on BFD, not reuse | Add `qdb_` flags on account; HL `TBD` | Open |
| KI-26 | Low | The sandbox `organizations` query returns nothing for the service principal | read-only call | Org friendly name not confirmable programmatically | Identify by schema evidence (ALD/exporter/account-centric = BFD) | Info |

## Addendum 2026-09-17 — data quality in the supplied Housing Loan report (`HousingLoanDataAnalysis.md` F11)

| ID | Severity | Issue | Evidence | Impact | Workaround / decision | Status |
|---|---|---|---|---|---|---|
| KI-27 | Medium | *Arrear Days* is stamped ~16 days after the balance date (constant +16 vs *First Arrear Date*); 8 accounts deviate by other offsets | 4,349/4,357 rows | Snapshot as-of and idempotency key ambiguity | `dpdAsOfDate` added; question to MIS | Open |
| KI-28 | Low | 6 seven-digit ID numbers, 1 blank row, 49 blank mobiles | Detailed sheet | Identity exceptions ≈ 0.2 % | Normaliser + `qdb_identityexception` reasons. **F4 decision:** the short IDs are **preserved as source identifiers and must not be rejected on length alone**; the validation rule is configurable and `TBD — Requires QDB Confirmation` | Open |
| KI-29 | Low | *Arrear %* and *Last Arrear Amount* are derived columns (instalment coverage, last instalment), not facts | 4,280/4,309 match the formula | Misread as arrears ÷ balance | Renamed `instalmentCoverageRatio`; marked derived | Resolved in design |
| KI-30 | Info | Excel converts every `1-30` bucket label to the date `2026-01-30` | 1,670 rows | Mock/normaliser must map it back | Handled in the adapter | Resolved in design |

## Addendum 2026-09-17 — F1–F11 review decisions

| ID | Severity | Issue | Evidence | Impact | Workaround / decision | Status |
|---|---|---|---|---|---|---|
| KI-31 | 🟡 | **One mobile number is shared by 46 customers** (a further 35 numbers are shared by 2–3, consistent with family use). | `HousingLoanDataAnalysis.md` F11; Detailed sheet, 4,357 rows | A number shared at that scale is almost certainly a branch, placeholder or data-entry default. Bulk SMS/WhatsApp to it would contact 46 unrelated customers. | **Recorded as a data-quality observation requiring QDB review — the system must NOT auto-correct, merge or suppress it.** The MIS adapter passes it through unchanged. **Mobile number is NEVER used as a customer identity key** (F4/F11 decision); it is a *recipient* attribute only, validated at send time by the Communication Service. Escalate the 46-customer number to QDB for investigation. | `TBD — Requires QDB Confirmation` |
| KI-32 | 🟠 | **The F1–F11 decisions conflict with four approved BRD positions.** Architecture analysis does **not** amend an approved BRD, so each conflict below keeps the original requirement and the proposed replacement side by side until QDB formally approves. See the amendment table beneath this register. | `phase-2-ba.md` FR-026 / FR-034 / FR-095 / FR-096 / FR-132; `facts-and-analysis.md` Q-09; F1/F6/F9 decisions 2026-09-17 | The BRD and the Phase 0 target architecture disagree **in writing**. Building from either alone would produce the wrong system. Phase 1 must not silently follow the architecture over the approved requirement. | Both texts remain traceable in the amendment table; all four flagged in the Project Tracker; **none resolved unilaterally**. | **Proposed BRD Amendment — Awaiting QDB Approval** |
| KI-33 | 🟡 | Superseded wording in the **first issue** of `HousingLoanDataAnalysis.md`: it asserted ">2000 = recovery, not a dunning target", "stop-contact must default from QCB DEAD" and "exposure criteria stay unused" as conclusions. | §2a of that document | Readers of the original text would implement policy that the user has explicitly left configurable. | Corrected in place by §2a (*Status of these findings after the F1–F11 review*); the three statements are now evidence and recommendation. No downstream document repeats them as decisions. | Resolved in design |

| KI-34 | 🟠 | **Risk of creating duplicate generic configuration infrastructure.** DCP proposes `qdb_platformconfiguration` and `qdb_platformmapping`, but the sandbox already carries QDB mapping/configuration entities: `qdb_attributesmapping`, `qdb_attributemappingconfiguration`, `qdb_parentattributemapping`, `qdb_form_submission_mapping`, `qdb_reportentitymapping`, `qdb_taskapprovalappfieldmapping`, `qdb_autonumberconfig`. The Phase 0 first pass judged them "different purpose" on inspection alone. | `ExistingQdbSchemaAssessment.md` §2, §3A | Building a second generic mapping layer beside QDB's own is exactly what Master Prompt §102 forbids, and it would fragment configuration ownership across the estate. | **Mandatory Phase 1 pre-condition:** the two entities **must not be created** until a documented reuse assessment (`ExistingQdbSchemaAssessment.md` §3A) classifies every entity above as *Reuse as-is · Reuse with extension · Cannot satisfy (with reason) · TBD*. Only a documented "cannot satisfy" for the whole set justifies creation. Needs QDB input on ownership. | `TBD — Requires QDB Confirmation` |

### KI-32 amendment table — original BRD text vs proposed replacement

Both columns stay in the register until QDB approves. **Status for every row:
`Proposed BRD Amendment — Awaiting QDB Approval`.**

| # | Approved BRD text (unchanged, still in force) | Proposed replacement (Phase 0 architecture) | Driver |
|---|---|---|---|
| a | **FR-034** — *"The strategy engine SHALL NOT segment by exposure (outstanding balance). DPD and arrears amount are the only permitted segmentation dimensions."* | Exposure is **retained as an optional, configurable** Strategy/Assignment criterion: disabled by default for HL (where the data shows it does not discriminate), available for BFD SME/Corporate where it may be material. Re-cast FR-034 as an **HL configuration default**, not an architectural prohibition. | F9 |
| b | **FR-026 / FR-132** — segregate the **>2000 DPD** population visually on case list and dashboards. | The >2000 value becomes **configuration**, not a constant. Acceptable as a *default view definition*; the requirement must read as a configurable segmentation, so HL and BFD can differ. | F1 |
| c | **FR-095 / FR-096** — an officer flags the customer deceased and sets `stopContact = true`, which suppresses automated communications. | Retained for the **manual** path, **plus** a new clause: a *confirmed deceased indicator from any source* (including MIS/QCB) must be **capable** of triggering a rule-driven, server-side **Contact Hold / Special Handling** evaluation before any automated *or* manual send. Whether **QCB DEAD alone** establishes the hold stays business/compliance policy. | F6 |
| d | **Q-09** (open BRD question) — what treatment applies above 2000 DPD; is automated contact permitted? | **Remains open.** The Phase 0 portfolio analysis is evidence toward an answer, not the answer. The threshold must not be encoded in application logic in the meantime. | F1 |

**Cross-platform principle (binding, applies to every issue above).** Housing Loan evidence may establish
HL default configuration, HL strategy recommendations, data-model requirements, required configurability
and test scenarios. It must **not** constrain BFD. The same Collection architecture and Rule Engine must
allow BFD to use materially different criteria and thresholds.

## Addendum 2026-09-17 (Phase 1) — consequences surfaced by implementation

| ID | Severity | Issue | Evidence | Impact | Workaround / decision | Status |
|---|---|---|---|---|---|---|
| KI-35 | **High** | **Consolidating actions, communications and PTP onto one `qdb_collectionactivity` removes the ability to separate "may log a call" from "may send a WhatsApp" by entity privilege** — precisely the split ADR-DCP-01 created ("split on privilege, not tidiness"). The Master Prompt §23/§26 mandates the consolidation, so the entity decision stands, but the security consequence was not carried into the Phase 0 security model. | `qdb-role-defs.mjs` `PRIVILEGE_GRANULARITY_NOTE`; ADR-DCP-01 rationale | Send rights can no longer be expressed as CRM entity privileges on the activity | Send authorisation moves to the **Communication Service**, enforced server-side against `fax` / `email` privileges plus the Contact Hold ruleset — never by activity privileges. **Architecturally resolved 2026-09-17**: the control point moved rather than disappearing. SecurityModel §5a and CommunicationArchitecture §2a now state that the Communication Service authorises every send server-side against privilege on the native `fax`/`email` activity plus the Contact Hold ruleset, and that Create on `qdb_collectionactivity` permits recording work, never contacting a customer. ADR-08 carries the same consequence. What remains is narrower: QDB confirmation that privilege on `fax`/`email` is an acceptable control point in their environment. | **Architecturally resolved — awaiting QDB confirmation of the control point** |
| KI-36 | Medium | Legacy DCP roles granted read on `msst_dcpcustomer` / `msst_dcploanfacility`. Those masters are now `contact` / `account` and the QDB-managed facility entities, which DCP does not own and must not grant privileges on. | `qdb-role-defs.mjs` | Collection officers need read on QDB-managed masters via an existing QDB role | Which existing QDB role confers that read is `TBD — Requires QDB Confirmation`; DCP roles deliberately grant nothing on them | Open |
| KI-37 | Medium | Customer PII moved off a DCP-owned entity, so the DCP field-security profile can no longer secure it. `QDB_PII_COLUMNS` now covers DCP-owned columns only. | `qdb-role-defs.mjs` | FR-014 / FR-114 masking on contact/account depends on QDB's own field security | Field security on contact/account columns is `TBD — Requires QDB Confirmation` | Open |
| KI-38 | Medium | `qdb_` option-set integer values cannot reuse the `msst_` base (463270000) — that range belongs to a different publisher, and a wrong base cannot be corrected in place afterwards. | `qdb-option-set-defs.mjs` `resolveQdbOptionValueBase()` | Provisioning throws unless `QDB_OPTION_VALUE_PREFIX` is set | **RESOLVED 2026-09-17** — QDB confirmed publisher `qdb`, OptionValuePrefix `10000`. All 30 canonical choices were provisioned in the 100000000–100000581 band and read back from the organisation. The fail-fast is retained so a different organisation cannot be provisioned by accident. | **Resolved** |
| KI-39 | Low | `qdb_casenumber` is a plain string, not `AutoNumberFormat`: QDB already has `qdb_autonumberconfig`, and `AutoNumberFormat` availability on CE 9.1 on-premises is not guaranteed. | Phase 1 §4 assessment §3.1 | Numbering mechanism undecided | Reuse the existing QDB auto-numbering plugin — owning plugin and HL deployment `TBD` | Open |

## Raised during Phase 1 execution (2026-09-17)

| ID | Sev | Issue | Evidence | Impact | Workaround / plan | Status |
|---|---|---|---|---|---|---|
| KI-40 | 🟠 | **The immutability guard on a completed activity only fired when `statecode` was among the changed columns.** The step was registered with `filteringattributes=statecode`, so a completed activity could still be edited field by field; Delete was blocked, Update was not. The same narrow filter is on the live `msst_` registration, where the defect has been present since the original registration. | `smoke-qdb-plugins.mjs` run 1: "Completed activity is immutable — update was allowed", Delete refused in the same run | A completed collection activity — a compliance record — was editable after completion | **Fixed for the canonical schema 2026-09-17**: the step now fires on every column (`filterAttributes: ''`), re-verified by the smoke test (13/13). `ensureStep` was given filter reconciliation so a widened filter actually reaches an organisation already registered. **FIXED ON THE LIVE `msst_` REGISTRATION 2026-09-17, on the gate decision.** Registration-only, two steps, one column each: `filteringattributes` cleared on `ImmutabilityGuardPlugin: Update of msst_dcpcollectionaction` and `… of msst_dcpcommunication`. No code change, no assembly re-deployed, no other component touched — the before/after capture in `docs/evidence/` shows 2 changed, 28 unchanged, 0 added, 0 removed. 🔴 **Clearing the filter alone did nothing: the pipeline caches the registration, and the guard kept accepting the update until the step was disabled and re-enabled.** Verified live on both steps: a completed activity and a completed communication now refuse an ordinary field update, Delete stays blocked, and open records and the completion transition still work. | **Closed** |
| KI-41 | 🟡 | `plugintype.friendlyname` is unique across the whole organisation, so a second assembly cannot register classes with the same short names. Registering `Qdb.DebtCollection.Plugins` beside `Msst.DebtCollection.Plugins` failed with 412 "A record with matching key values already exists" on the first plugin type. | Registration run 2026-09-17; manual probe with a distinct friendly name returned 204 | Any organisation running both assemblies during a migration hits this | `ensurePluginType` now sets `friendlyname` to the fully-qualified type name, which is unique by construction | Resolved |
| KI-42 | 🟠 | The `qdb_` smoke test leaves undeletable rows behind, for the same reason KI-04 describes on the `msst_` schema: it must exercise `ImmutabilityGuard`, and the guard then refuses to let the test clean up after itself. | `smoke-qdb-plugins.mjs` residue report | Sandbox pollution in the new canonical tables — currently 3 cases, 2 snapshots, 2 completed activities, all named `SMOKE-…` | The script now deletes everything the guard permits and prints what it could not remove, so the residue is known rather than discovered. **Closed 2026-09-17.** Under the authorised scope the seven identified records were removed: `clean-qdb-smoke-data.mjs` matched them on the `SMOKE-` prefix, confirmed nothing outside the smoke set referenced them, disabled only the three `qdb_` Delete guards, deleted, restored the guards and verified the restored registration. The verification smoke run that followed was cleaned the same way, so the canonical tables hold **zero** smoke rows. The legacy `msst_` guard was never disabled. | **Closed** |
| KI-43 | 🟡 | **`AuditLogWriter` has no home in the canonical schema.** The Master Prompt requires native audit, so there is no `qdb_` audit table for it to write to, and it has no step in the `qdb_` registration. It is retained in the assembly, with its 20 tests, because the `msst_` deployment still runs it. | `qdb-plugin-steps.mjs` header; `AuditLogWriter.cs` class comment | Dead code in the canonical assembly until the `msst_` deployment is retired | **Classified at the gate 2026-09-17: Legacy — pending `msst_` retirement.** It is not migrated to `qdb_`, no replacement audit entity is created, and the canonical schema keeps native audit for business history with `qdb_crmlogs` for technical logging. The class and its 20 tests stay while the `msst_` deployment runs them. Verified: no `qdb_` artefact references `AuditLogWriter` except the comment in `qdb-plugin-steps.mjs` explaining its absence. | **Legacy — deferred to `msst_` retirement** |
| KI-44 | 🟠 | **`StopContactQueueMover` has no trigger in the canonical schema.** It fires on a contact-hold flag flipping on a customer master; DCP owns no customer master, and which column on `contact`/`account` carries the hold is not confirmed. | `qdb-plugin-steps.mjs` header; `ContactHoldSettings` | FR-097 suppression is not enforced by a plugin on the canonical schema today | The plugin is retargeted and configuration-driven: register the step against the confirmed entity with `holdAttribute=<logical name>` in the step's unsecure configuration and it works with no code change. Until then the hold decision stays server-side in the Collection services per ADR-DCP-11. **Gate decision 2026-09-17: do not invent or add a Contact/Account column, do not create a customer-master field, and do not infer a hold from QCB DEAD alone.** FR-097 runtime enforcement is recorded as **Implementation ready — authoritative Contact Hold source configuration pending QDB confirmation**, and this does not block unrelated Phase 2 work. | **Open — QDB configuration dependency; not a Phase 2 blocker** |
| KI-45 | 🟡 | Whether the consolidated `qdb_collectionactivity` needs a channel column — a communication activity could not record which channel carried it. | Provisioned attribute list; `ActivitySubjectComposer` retarget | Would have decided whether communications live on a DCP table or on native ones | **CONFIRMED AND CLOSED 2026-09-17.** Communications are native records — SMS and WhatsApp on `fax`, Email on `email`, Warning Letters through the approved QDB document capability — and DCP creates **no** communication entity of its own. The consolidated activity needs no channel column because it is not the communication record. The officer-facing **unified Communication History** is a read/aggregation model over those sources, correlated to the Collection Case: `CommunicationArchitecture.md` §7, `APIContracts.md` §4.1, `SecurityModel.md` §5b, `ReactArchitecture.md` §5.1a, tests TC-215–TC-222. Remaining detail is implementation-level only: the `fax` fields distinguishing SMS from WhatsApp, the DCP-origin marker if `regardingobjectid` cannot carry attribution, the Warning Letter document source, and available delivery-status fields. | **Closed — unified communication architecture confirmed** |

## Raised during Phase 2 (2026-09-18)

| ID | Sev | Issue | Evidence | Impact | Workaround / plan | Status |
|---|---|---|---|---|---|---|
| KI-46 | 🟠 | **The approved case matrix had no cure path from most working states.** Settled was reachable only from PTP Active, Restructured, Under Legal Action and Deceased/Insurance Review, so a case that was In Progress, Assigned, Escalated or under legal review when MIS reported a cure could never reach Closed. | `StatusTransitionMatrix.cs` before Phase 2; `caseLifecycle.test.ts` "universal targets" | "Cure closes the current delinquency episode" was not representable for the majority of open cases | **Amended, code and plugin together:** Settled joined Deceased/Insurance Review as a universal transition target from every non-terminal state (same precedent as decision 3, 2026-09-16). The TypeScript mirror is parity-tested against the C# source; the Phase 2 smoke proved New → Settled live. Closure (Settled → Closed) stays a configured rule, not the sync. Recorded in the statuscode section of `FieldDictionary-Transaction.md`. | **CLOSED 2026-09-18 (Phase 3).** Approved at the Phase 2 gate with the architectural clarification *"MIS determines financial cure; DCP manages the resulting Collection lifecycle transition"*. Recorded as **ADR-DCP-12**; no artificial intermediate state was introduced and no other transition was widened. Live evidence: Phase 2 smoke records a cure from a working state (`Settled` + cure date + `Cured`); Phase 1 smoke still proves a genuine refusal (`New → In Progress`). |
| KI-47 | 🟡 | **`qdb_delinquencysnapshot` has no source-system column.** The case carries `qdb_facilitysourcesystem`; the snapshot's source-identity block is `qdb_customerbusinessid` + `qdb_facilitynumber` only. Phase 2 requires the snapshot to retain the source system. | `FieldDictionary-Transaction.md` snapshot block; `DelinquencySnapshotRepository.ts` | A snapshot's source system is carried inside the composed key when the composition includes it, and is otherwise implied by the organisation it lives in — recoverable, but not a first-class column | **Not added** — schema is not invented during implementation. Proposed for approval: `qdb_facilitysourcesystem` string(50) on `qdb_delinquencysnapshot`, unmanaged addition, identical on both platforms, no migration impact (new rows only; existing rows null). The canonical `DelinquencySnapshot` type already carries `facility.sourceSystem`, so the repository change is one line once approved. | **CLOSED 2026-09-18 (Phase 3).** Approved and implemented end to end: `qdb_facilitysourcesystem`, String(50), added to `qdb_delinquencysnapshot` (canonical column count 243 → **244**, verified 244/244 live). Not the sole source of the source system — the snapshot carries it as a first-class column independently of the idempotency key. Entity Dictionary, field dictionary, bindings, domain model, repository, sync service, tests, provisioning and verification all updated; Phase 1 (13/13) and Phase 2 (20/20) regression re-run green; Change Log entry recorded. Identical on both platforms, unmanaged addition, no migration impact (new rows only; existing rows null — no historical row was back-filled). Cloud runtime validated by the Phase 3 smoke. |
| KI-48 | 🟡 | **The Rule Engine eligibility operation contract is provisional.** `RuleEngineEligibilityEvaluator` calls the operation named in configuration and expects `Outcome` (from the approved set), `Reason`, `RulesetVersion`. No DCP-facing Custom API exists on the Rule Engine yet. | `eligibilityEvaluators.ts`; `APIContracts.md` §6 `qdb_dcp_EvaluateEligibility` | Until the operation exists, sync runs only with a deterministic evaluator (tests, sandbox demo) — never with thresholds in code | Define the operation on the Rule Engine (Phase 3 candidate); the evaluator fails closed on any response outside the contract | **Open — Rule Engine operations still to be created (unchanged in substance; the DCP side is now complete).** Phase 3 implemented `RuleEngineClient` against three operations — `qdb_dcp_EvaluateEligibility`, `qdb_dcp_SelectStrategy`, `qdb_dcp_EvaluateContactHold` — named by `qdb_platformconfiguration.qdb_featureflags.ruleEngineOperations` with no default. Every failure mode fails closed and is logged to `qdb_crmlogs` (`rule_engine_unusable`): operation not configured, ruleset code not configured, operation absent from the organisation, response outside the contract. Proved live: a non-existent Custom API was refused rather than defaulted. **What remains is QDB’s: create the operations and the rulesets.** See ADR-DCP-13, `APIContracts.md` §6. |
| KI-49 | 🟡 | **Case numbers are provisional.** `qdb_casenumber` is composed as `<sourceSystem>-<facilityNumber>-E<episode>` — unique per facility and episode by construction — because QDB's auto-number mechanism (`crmi_autonumberingsetup`) has no row for `qdb_collectioncase`. | `collectionCase.ts` `composeProvisionalCaseNumber`; KI-39 | Case numbers are readable identity, not an organisational sequence | Configure the existing QDB auto-number mechanism for the table when QDB confirms the format; the repository takes the number as an input, so the switch is local | **Open — format still `TBD — Requires QDB Confirmation`; the DCP side is complete.** Investigated live 2026-09-18: `crmi_autonumberingsetup` holds **0 rows**, `qdb_autonumberconfig` holds **0 rows**, and no `qdb_` table carries an `AutoNumberFormat` — so no QDB numbering convention is observable and none was invented. `qdb_casenumber` now has exactly two configured sources (**ADR-DCP-15**): `Provisional` (DCP composes `<sourceSystem>-<facilityNumber>-E<episode>`) or `PlatformConfigured` (DCP omits the column and QDB’s existing mechanism fills it). Uniqueness is enforced by the `qdb_casenumber_uk` alternate key — proved live ("Entity Key Case Number violated"). Switching is a configuration change, not a code change. No second auto-number engine was built and QDB’s mechanism was not modified. |
| KI-50 | 🟡 | The legacy API routes and services (`CustomerService`, `IdentityExceptionService`, `/customers/:qid`, `/identity-exceptions`) still bind to `msst_` entity sets. Phase 2 added the `qdb_` repositories beside them rather than migrating them, because the routes are slated for retirement under MP §60 (React → Xrm.WebApi). | `packages/types/constants.ts` `ENTITY_SETS`; `apps/api/src/routes` | Two generations of service code coexist until the routes are retired | Retire with the `msst_` deployment; their tests stay green and unweakened meanwhile | **Open — pending route retirement** |

## Raised during Phase 3 (2026-09-18)

| ID | Sev | Issue | Evidence | Impact | Workaround / plan | Status |
|---|---|---|---|---|---|---|
| KI-51 | 🟡 | **Who matches the strategy criteria has not been confirmed by QDB.** `qdb_collectionstrategy` carries criteria columns (customer type, product, DPD from/to, arrears, exposure, risk, NPL, broken-PTP count, legal and restructure status). Phase 3 treats them as **data the ruleset reads**, not as a matcher DCP evaluates: the ruleset returns a strategy *code* and `StrategyService` resolves the record, checks activation and effective dates, and refuses a priority tie. | `packages/domain/src/strategy.ts`; ADR-DCP-13 | If QDB expects DCP to evaluate the criteria itself, the matching semantics — precedence, overlap, tie-breaking — would move into application source, which is what ADR-DCP-11 and the Phase 3 authorisation forbid | Confirm with QDB that the strategy ruleset reads the criteria columns. Until then the columns are provisioned, readable and unevaluated by DCP. **No matching logic has been written**, so either answer is reachable without deleting anything. | **Open — TBD, Requires QDB Confirmation** |
| KI-52 | 🟡 | **A Dataverse lookup must be `$select`ed by its `_<column>_value` form; selecting the storage column is accepted and silently returns nothing for it.** `StrategyRepository` selected `qdb_strategyid`, so every strategy action came back looking unparented and each strategy resolved with zero actions. Unit tests passed throughout: the in-memory fake answered with whichever column the code asked for. | Phase 3 live smoke, first run — "Strategy actions read and linked to their strategy — 0 action(s)" | A whole class of read defect is invisible to tests that use a fake org — the same class as the Phase 2 navigation-property discovery | **Fixed** in `StrategyRepository` and audited: no repository now selects a bare lookup name, and the dead bare bindings were removed so the mistake cannot be repeated by autocomplete. The durable mitigation is the standing rule — **a column's read name, write name and storage name are read from the organisation, never inferred** — and the per-phase live smoke that would catch it again. | **CLOSED as a defect 2026-09-18; the finding is now a standing requirement.** Fixed in `StrategyRepository`, audited across every Collection repository, dead bare bindings removed, proved live. What it demonstrated — **an in-memory or fake adapter can validate the same incorrect assumption as the production code** — is recorded as a permanent testing rule in `TestingStrategy.md` §1D and is **mandatory from Phase 4 onward**: platform-specific query behaviour (lookups, `_<column>_value`, navigation properties, `$select`, `$expand`, `$filter`, `$orderby`, paging, `@odata.nextLink`, alternate keys, formatted values, FetchXML paging cookies) must not rely solely on mocked tests where the Cloud sandbox can provide real evidence. Unit tests are not weakened — the fast suite proves the logic, the live suite proves our belief about the platform. |

## Raised during Phase 4 (2026-09-18)

| ID | Sev | Issue | Evidence | Impact | Workaround / plan | Status |
|---|---|---|---|---|---|---|
| KI-53 | 🔴 | **No QDB MIS API contract exists in evidence.** This repository, the wider AICompany repository and `D:/QDB/Projects/` were all inspected. There is no endpoint, request or response schema, authentication, authorisation, paging mechanism, continuation semantics, change feed, retry contract, rate limit, source timestamp, observation id or record id. The only MIS evidence anywhere is **two spreadsheet report exports**. | `docs/MISContractEvidence.md`; `ApiMisDelinquencyService` | **MIS transport cannot be built, and must not be claimed.** The interface, normalization, adapter boundary, paging abstraction and mock provider are complete and tested; the production transport is not, and no runtime result involving real MIS exists | `ApiMisDelinquencyService` fails closed on every method, naming what QDB must supply, and refuses even when endpoints are configured because a configured URL is not a confirmed contract. Phase 4 reports **"MIS Integration Architecture & Processing Pipeline Complete — Production MIS Transport Contract Pending QDB"** and never "MIS API Integration Complete" | **Open — TBD, Actual MIS Contract Required** |
| KI-54 | 🟡 | **Final MIS observation identity is unsolved, because the feed carries no observation id and no source timestamp.** DCP replay protection works — a repeated run creates no duplicate case, episode or snapshot — but it rests on identity DCP composes, not on identity MIS asserts. | `misNormalization.ts`; `orchestration-and-replay.test.ts` "what replay protection does NOT yet prove" | A genuine intra-day correction and a duplicate delivery are **indistinguishable**. The snapshot key collapses them, which is safe but is not the same as knowing they were the same observation | Keep the snapshot key composition as deployment configuration with no default. Revisit when MIS supplies an observation identifier or source timestamp. **Do not freeze the production idempotency composition until then** | **Open — TBD, Requires QDB/MIS Confirmation** |
| KI-55 | 🟡 | **The `1-30` arrear bucket arrives as a date from the spreadsheet export**, affecting 1,670 of 4,359 rows — 38 % of the population. `1-30` is a valid month-day pair so the export coerced it to 30 January; nothing else in the taxonomy coerces, because there is no month 31, 61 or 91. | `docs/MISContractEvidence.md` §3; `readArrearBucket` | A normalizer expecting a string loses the **largest bucket in the book**, silently and without error | Handled **at the normalization boundary only** — `readArrearBucket` recovers the code from the month/day pair. Nothing downstream of normalization knows this happened, and the canonical model exposes only the normalized bucket. **This workaround must be revalidated when a real MIS API arrives**: it may be an artefact of spreadsheet export rather than of MIS itself, and a future developer must not delete it as apparently unnecessary before that check is made | **Open — revalidate against the real MIS contract** |

## Raised during Phase 5 (2026-09-18)

| ID | Sev | Issue | Evidence | Impact | Workaround / plan | Status |
|---|---|---|---|---|---|---|
| KI-56 | 🟠 | **The React workspace has never been run inside Dynamics.** The artefact is built, deployed, published and verified byte for byte, and its adapter and query modules are proven against the organisation from node. None of that proves the page loads in the host, that `getGlobalContext` answers, that assets resolve under `/WebResources/`, or that reads succeed as the signed-in user rather than as the service principal. | `docs/evidence/Phase5_webresource_deploy.txt` states what it does not prove; `smoke-qdb-phase5.mts` authenticates as the application user | The Phase 5 workspace is **Development Complete — Cloud Runtime Test Pending**. It must not be described as runtime validated | Needs an authenticated interactive CRM session. The exact checks are listed in the Phase 5 completion report. Authentication is not bypassed, no password is stored and no security control is weakened to get around it | **Closed 2026-09-19** — Phase 6 manual runtime validation ran the workspace inside `main.aspx` on `org5869857f` as the signed-in user: the page loaded, the host context answered, reads returned the user's own data and writes succeeded on their session |
| KI-57 | 🟡 | **An activity's lookups carry a relationship suffix that the bare attribute name does not.** Binding `qdb_collectioncaseid@odata.bind` on `qdb_collectionactivity` is rejected — the navigation property is `qdb_collectioncaseid_qdb_collectionactivity`, because `regardingobjectid` also targets the case. Reading is unaffected: `_qdb_collectioncaseid_value` is correct for `$select` and `$filter`. | Phase 5 query smoke, first run — "An undeclared property 'qdb_collectioncaseid' which only has property annotations in the payload but no property value was found" | A write that binds an activity to its case fails outright rather than silently, so the blast radius is small — but it is the same family as KI-52: a name that looks right and is not | The suffixed names were already recorded in `qdbBindings.NAVIGATION` and are what the service layer uses. The smoke script now uses them too, with the reason at the code. **A lookup's read name, write name and storage name are three different things, and all three are read from the organisation rather than inferred.** | **Closed 2026-09-18 — corrected in the script; the rule it illustrates is the standing one from KI-52** |
| KI-58 | 🟡 | **An empty `$select` is rejected by Dataverse.** A count asks for no columns, and `buildOptions` was sending `$select=` blank: *"'select' and 'expand' cannot be both null or empty"*. Every unit test passed, because a fake client accepts whatever option string it is handed. | Phase 5 query smoke, first run; `platform.test.ts` "omits $select entirely when no column is wanted" | Every KPI count on My Day, Dashboards and Delinquency Intake would have failed at runtime and shown an em dash — the safe failure, but for the wrong reason | Fixed: `buildOptions` omits `$select` when no column is wanted. Two regression tests added. **This is the third defect in this engagement that only a live read could find**, after KI-52 and the Phase 4 `Prefer` header — which is why `smoke-qdb-phase5.mts` exists rather than a larger mocked suite | **Closed 2026-09-18** |
| KI-59 | 🟡 | **`qdb_crmlogs` carries no correlation column.** The correlation id, batch id and counters are written into `description` as a JSON diagnostic block, so a case's technical trail is found by a `contains(description, …)` search rather than by a lookup or an indexed column. | `renderDiagnosticBlock` in `packages/domain/src/logging.ts`; the Case Workspace audit tab | A `contains` over a memo column does not use an index. On a large log this will be slow, and it cannot be narrowed by anything cheaper | Accepted for now — the trail is per case and the query is bounded and paged like every other list. **Not resolved by inventing a column**: `qdb_crmlogs` is an existing QDB table shared with other engagements, and adding to it is outside the authorised scope. If the cost becomes real, the request goes to QDB with measurements | **Open — accepted, revisit with measurements** |
| KI-60 | 🟡 | **The Phases sheet of the project tracker was one row out of alignment.** Phase 4's completion text had been written onto the Phase 5 row and Phase 5's start text onto the Phase 6 row, which left Phase 6 reading "In Progress" when it is not authorised. | `DebtCollection_Project_Tracker.xlsx`, Phases sheet, rows 5–7 before 2026-09-18 | The tracker is the engagement's status record, and it showed an unauthorised phase as started | Corrected: each row now carries the phase it names, with dates taken from `Phase4_timing.md` and `Phase5_timing.md` rather than reconstructed. The defect was mine, in the Phase 4 close-out, and it is recorded rather than quietly fixed | **Closed 2026-09-18** |

## Raised during Phase 5 — loading the real arrear report (2026-09-19)

| ID | Sev | Issue | Evidence | Impact | Workaround / plan | Status |
|---|---|---|---|---|---|---|
| KI-61 | 🔴 | **The KI-55 bucket recovery silently lost 38 % of the book when given a date from a real spreadsheet parser.** `readArrearBucket` recovered `1-30` by reading `getUTCMonth()`/`getUTCDate()`. Two parser artefacts defeat that, and the live Housing Loan report presents both: SheetJS materialises an Excel serial as **local** midnight, so on a machine in Qatar (+03:00) 30 January is `2026-01-29T21:00Z` — the 29th in UTC; and its serial arithmetic lands **eight seconds short**, at `23:59:52` on the 29th, so the 29th is what *both* frames read. The recovery returned `unknown` for all 1,670 coerced rows. | Loading `HousingLoanArrearReport.xlsx` for the first time: `0 rows recovered to 1-30`, `1,670 unrecognised (date 2026-01-29)`. Every unit test passed throughout — each one built its date with `Date.UTC` at exact midnight, meeting neither artefact | **The largest bucket in the portfolio — 1,670 of 4,358 facilities, 38 % — would have been ingested with no bucket at all.** This is precisely the failure KI-55 exists to prevent, reintroduced by the fix for it | **Fixed.** The value is now rounded to the nearest midnight in both the UTC and the local frame, and the first candidate that parses to a valid bucket code wins. Rounding rather than truncating absorbs the drift; trying both frames absorbs the timezone. It cannot invent a bucket: a month-day pair has a month ≤ 12 and a day ≤ 31, so `1-30` is the only code in the taxonomy a date can ever produce. Four regression tests: a locally-constructed date, the literal value SheetJS returns, a ±12-hour sweep of producing offsets, and a genuine non-bucket date at several offsets. The suite is green under `TZ=UTC` as well as Asia/Qatar | **Closed 2026-09-19** |
| KI-62 | 🟡 | **`qdb_firstarreardate` is the only `DateOnly` column on `qdb_delinquencysnapshot`**; every other date on the snapshot and on the case is `DateAndTime`/`UserLocal`. A timestamp is rejected outright rather than truncated. | First full-shape snapshot create: *"Cannot convert the literal '2026-06-30T00:00:00Z' to the expected type 'Edm.Date'"* | Any writer that treats every date column alike fails on this one column, and only on writes — reads are unaffected | Read from `DateTimeAttributeMetadata.Format`/`DateTimeBehavior` rather than assumed, and the loader sends `YYYY-MM-DD` for it. Recorded here because it is the same family as KI-52 and KI-57: **a column's read form, write form and storage form are three different things, and all three are read from the organisation** | **Closed 2026-09-19** |

## Raised during Phase 6 (2026-09-19)

| ID | Sev | Issue | Evidence | Impact | Workaround / plan | Status |
|---|---|---|---|---|---|---|
| KI-65 | 🟡 | **The ordering inside the activity Open-state group is not in evidence.** `FieldDictionary-Transaction.md` groups the six activity statuses by `statecode` — Open, In Progress, Awaiting Approval and Returned are all the Open state — but no appendix gives an activity transition matrix the way B.1 and B.2 give one for the case and the promise. Whether Awaiting Approval may be entered without In Progress first, or Returned may complete directly, is QDB policy. | `StatusTransitionMatrix.ActivityAllowed`; no appendix exists in the repository | A stricter matrix guessed here would block legitimate work and surface to an officer as a save that fails for no visible reason | The matrix is **deliberately permissive inside the Open group and strict at the boundary**: the four Open-state statuses are mutually reachable, each can reach Completed or Cancelled, and both terminal statuses go nowhere — which is the minimal matrix consistent with the documented `statecode` grouping and with `ImmutabilityGuard`, already registered and already freezing completed activities. Tightening it later removes transitions rather than adding them, which is the safer direction | **Open — QDB confirmation** |
| KI-66 | 🟠 | **No activity outcome catalogue exists.** `qdb_activityoutcome` holds zero rows, and it is not a table of labels: `qdb_requiresfollowup`, `qdb_followupdays`, `qdb_requiresnotes` and `qdb_escalationrequired` mean the outcome record **drives** follow-up scheduling and whether notes are mandatory. | Live count on `org5869857f`, 2026-09-19 | Outcome-driven behaviour cannot be configured for production until QDB supplies the catalogue. It does not block the mechanism, which reads whatever is configured | Seven synthetic outcomes seeded by `seed-phase6-configuration.mjs`, marked `P6-`, reversible and idempotent, shaped only to exercise the four behaviours — four require follow-up, three require notes, two ask for escalation. **They are not QDB policy and must never be treated as such.** The day counts are deliberately round numbers so they cannot be mistaken for negotiated terms | **Open — QDB confirmation; isolated, blocks nothing else** |
| KI-67 | 🟠 | **Concurrency is proven from node, not from the browser.** `spike-concurrency.mjs` established the platform's behaviour with a bearer token and full control of the request headers (16/16). The workspace will use the session cookie from a same-origin `fetch`. The OData semantics are identical; the authentication is not. | `docs/evidence/Phase6_concurrency_spike.txt`; ADR-DCP-18 | Until the browser write smoke runs, concurrency-controlled writes are **designed and unproven** — the same status the read path had before Phase 5's smoke found three defects in it | The browser write smoke is an explicit early gate **before any Phase 6 form is built**, per the authorisation. It must exercise create, read, update, complete and the PTP transition, and must verify the ETag round trip and the 412 | **Closed 2026-09-19** — the write smoke proved the OData semantics, and Phase 6 manual runtime validation then exercised the same path **in a browser on the session cookie**: step 7 produced a real two-window conflict, the typed refusal, a working reload and two consecutive saves. Authentication is no longer the untested half |
| KI-68 | 🟡 | **`If-Match: *` asserts existence, not freshness.** A write carrying it updates whatever version is there. It reads like a safety mechanism and is not one. | `spike-concurrency.mjs` check 4 — HTTP 204 against a row whose version had moved | Using `*` as a concurrency guard would produce silent last-write-wins while appearing to be guarded — worse than no guard, because it looks safe | Recorded in ADR-DCP-18 §5. The adapter sends the caller's actual expected version or nothing at all; `*` is never used | **Closed 2026-09-19 — recorded so it is not rediscovered** |
| KI-69 | 🟠 | **A lookup's navigation property is not derivable from its attribute name, and differs per entity.** `qdb_collectioncaseid` binds through `qdb_collectioncaseid_qdb_collectionactivity` on the activity — suffixed, because `regardingobjectid` also targets the case so the relationship name must be unique — and through the bare `qdb_collectioncaseid` on the snapshot, where nothing competes. Same attribute, two different write names, decided by what else happens to point at the same table. | Phase 6 browser write smoke, first runs: *"An undeclared property 'qdb_outcomeid' which only has property annotations in the payload but no property value"*; metadata sweep of `ManyToOneRelationships` | A write that guesses the name fails outright — loudly, so the blast radius is small — but it is the **third** appearance of this family after KI-52 (read form) and KI-57 (write form), and guessing had already cost debugging twice | `NAVIGATION_PROPERTIES` and `NAVIGATION_REGISTRY` added to `apps/web/src/data/schema.ts`, read from `ReferencingEntityNavigationPropertyName` and never inferred. `bindLookup()` is the only way the workspace composes an `@odata.bind`. `verify-view-columns.mts` now checks all eight against live metadata alongside the columns — **181 names verified**. The polymorphic Customer lookup has one navigation property per target, which is what lets HL bind a contact and BFD an account from one codebase | **Closed 2026-09-19 — registry plus live verification** |
| KI-70 | 🟡 | **A Dataverse PATCH answers `204 No Content` with no ETag**, so a caller doing concurrency-controlled writes is left holding the version it read — already stale — and its next write fails for a reason that looks like a conflict but is a missing token. | Phase 6 browser write smoke, first run: *"was updated but returned no new version"* | Every second consecutive save on a form would have failed with a misleading "this record changed" message, which is worse than a plain error because it sends the user to reload something that has not moved | `Prefer: return=representation` is sent on every write, making the response a `200` carrying the updated record **and** its new version in one round trip. Joined with `odata.include-annotations`, never substituted — `Prefer` is a list, and replacing it is the Phase 4 defect that silently dropped annotations from every paged read | **Closed 2026-09-19** |
| KI-71 | 🟠 | **No schema mechanism links an activity to the strategy action that produced it.** `qdb_collectionactivity` carries no lookup to `qdb_strategyaction`, and it has no alternate keys. Its only generic reference columns, `qdb_relatedrecordtype` and `qdb_relatedrecordid`, are documented in `FieldDictionary-Transaction.md` as holding `fax`/`email` when an activity mirrors a send, and are read-only and process-populated — so they cannot be repurposed without overloading a documented column. | Metadata read of `qdb_collectionactivity` (`ManyToOneRelationships`, `Keys`) on `org5869857f`, 2026-09-19; `FieldDictionary-Transaction.md` | The Action Plan can show what a strategy **plans** and what has actually been **done**, correlated by activity type, but it **cannot prove** that a given activity was created *because of* a given planned action. Manual and strategy-generated work are therefore not distinguishable today. Phase 8, which materialises planned actions automatically, will need the distinction | `loadActionPlan` correlates on the activity **type id** — not the label, which is editable configuration — and the limitation is stated in the function's own documentation rather than hidden behind a plausible-looking column. **Nothing infers provenance.** Adding a lookup would be a schema change and needs QDB approval | **Open — resolve before Phase 8; blocks nothing in Phase 6** |
| KI-72 | 🟡 | **No PTP policy limits are in evidence.** Maximum promise amount, maximum promise horizon, the number of promises a case may carry at once, a tolerance amount, a grace period and any automatic broken threshold are all QDB policy, and none appears in the requirements, the field dictionaries or the organisation's configuration. | Sweep of `FieldDictionary-Transaction.md` Appendix B.2 and the seeded Phase 6 configuration, 2026-09-19 | A limit invented here would be enforced on real customers and would look like an agreed rule. The opposite risk — accepting an implausible promise — is visible to a supervisor and reversible | `activityOperations.ts` imposes **none**, and `activityOperations.test.ts` asserts the absences deliberately (*"imposes no maximum amount, because none is in evidence"*, *"imposes no maximum promise period"*), so a future change that adds a limit has to delete a test that says why it is not there. The mechanism to enforce limits exists; the values do not | **Open — QDB confirmation** |
| KI-73 | 🔴 | **The smoke cleaner reported success while leaving every record it could not see.** `clean-qdb-smoke-data.mjs` matched one marker column per table: activities by `qdb_activitynumber`, cases by `qdb_facilitynumber`, contacts by `governmentid`, and accounts and activity outcomes not at all. An activity created through the **service layer** carries the marker only in its `subject`, because the domain never writes `qdb_activitynumber` — the server composes it. So the cleaner printed *"None. There is no smoke residue to remove."* and exited green while six rows remained. | First run of `smoke-domain-operations.mts`, 2026-09-19 — cleanup reported nothing to remove; the script's own residue count then found 4 activities and 2 cases | The worst failure mode a cleanup routine has: silent, and confidently reported as success. Left unfixed it would have accumulated SMOKE- rows in the sandbox on every Phase 6 run and eventually contaminated the DEMO- scenario visually, while every smoke went on passing | `markerField` now accepts **several** columns and any match is enough — activities by `qdb_activitynumber` **or** `subject`, cases by `qdb_facilitynumber` **or** `qdb_casenumber`, contacts by `governmentid` **or** `lastname` — and `accounts` and `qdb_activityoutcomes` were added, the latter matched on the `SMOKE-` prefix so the seven `P6-` outcomes survive. The residue assertion lives in the smoke itself, not in the cleaner, so the cleaner cannot mark its own homework. Verified by the rerun: **13 records identified and removed, 0 residue, guards restored and re-verified** | **Closed 2026-09-19 — found by the live smoke, fixed and re-proved** |
| KI-74 | 🔴 | **The Phase 6 configuration was seeded in a state no officer could have used.** `seed-phase6-configuration.mjs` wrote eleven activity types without `qdb_isactive`, and every one came out **inactive**; it wrote seven outcomes with **no owning activity type** at all. Both are correct-looking rows with the right columns, and both make a form empty: the type dropdown because it refuses retired configuration, and the outcome dropdown because `qdb_activityoutcome.qdb_activitytypeid` names the type an outcome belongs to and the list is filtered by it. | Live read of `org5869857f` while wiring the Phase 6 forms, 2026-09-19 — 12 types, all `qdb_isactive=false`; 7 outcomes, all `_qdb_activitytypeid_value = null` | Every seeded row existed and every automated test passed, because nothing asserted that the **catalogue a form reads** was non-empty. The feature would have reached manual runtime validation completely unusable, and the cause — a documented column default that is not what the platform actually wrote — would have looked like a defect in the forms | The seeder now writes `qdb_isactive` and `qdb_sequence` explicitly, and binds each outcome to its owning type through the metadata-verified navigation property `qdb_activitytypeid` (bare, no suffix — added to `NAVIGATION_REGISTRY`, so `verify-view-columns.mts` checks it: **205 names**). Outcomes are now seeded **per type** (`P6-CALL-CONTACTED`, not `P6-CONTACTED`), because an outcome belongs to one type. The seeder re-reads and asserts zero orphans rather than trusting a 204, and `smoke-domain-operations.mts` asserts the catalogue a form would read is non-empty, correctly filtered and varied across the four behaviours — **11 active types, 30 owned outcomes, 50/50 live** | **Closed 2026-09-19 — found by reading the organisation, not by a test** |
| KI-75 | 🔴 | **The workspace built its adapter with no write transport, so every write and every versioned read failed at runtime.** `App.tsx` constructed `new XrmCrmAdapter(xrm)` — the third constructor argument, the transport, was simply never passed, although `context.apiBase` was already computed two lines above. Logging an action failed with *"Cannot create a record idempotently: this adapter was built without a write transport"*, and opening an existing activity failed the same way through `retrieveVersioned`. | Reported by the user on the **first attempt** at Phase 6 manual runtime validation, step 1, 2026-09-19 | Every Phase 6 write path was dead on arrival: create, edit, complete, cancel, promise capture and every promise transition — roughly two thirds of the runtime validation script. Reads were unaffected, so every screen rendered and nothing looked wrong until a Save was pressed | The session is now built by an exported `createCrmSession()`, which composes `SameOriginWriteTransport(context.apiBase)` and passes it. **Why nothing caught it is the real lesson**: every one of the 241 tests built its own adapter and handed it a transport, so the single place that assembles the production object had no test at all, and a component test could not have found it — the failure was in the wiring, not the rendering. `session.test.ts` now drives the real factory and asserts a request actually leaves for create, versioned read and versioned update, with `credentials: same-origin` and both `Prefer` directives. Verified by reintroducing the exact line and watching all four fail with the user's own error text | **Closed 2026-09-19 — deployed and published; runtime re-test pending** |
| KI-76 | 🟡 | **Whether an officer may override a configured follow-up window is not in evidence.** `qdb_activityoutcome.qdb_followupdays` says when a follow-up falls; nothing says whether a collector may choose a different date, or whether a supervisor must approve one that differs. | Sweep of `FieldDictionary-Configuration.md` and the outcome column set, 2026-09-19 | Either reading is defensible and they differ materially: enforcing the window removes officer judgement, allowing the override means a configured schedule is advisory | The typed date is **preserved**, because that is the choice that loses no information — a supervisor can see an officer chose differently, where silently overwriting would discard a deliberate decision with no trace. The form shows the configured date as soon as an outcome is chosen, so an override is a visible act rather than an accident. Recorded here rather than enforced | **Open — QDB confirmation** |
| KI-77 | 🔴 | **A configured follow-up was derived invisibly at save time, and the adjacent button reported success without recording one.** `planCompleteActivity` derived the date correctly at every layer — proved live end to end — but only when the officer went through **Complete…**. The neighbouring **Save changes** wrote subject, date and notes, answered "Saved.", and touched neither outcome nor follow-up. Nothing on screen had ever shown that a follow-up was implied, so the two saves were indistinguishable to the user. | Phase 6 manual runtime validation step 3b, reported by the user 2026-09-19. The activity was found still **Open with no outcome** — the completion had never run | An officer would record work believing a follow-up had been scheduled when none had. The follow-up queue is how the next contact is remembered, so the lost item is silent and the customer is simply never called back | The rule moved into `deriveFollowUpDate()` in `@dcp/domain` — **one implementation**, used by `planCompleteActivity` to write and by the form to *preview*. Choosing an outcome now fills the follow-up field immediately with the date configuration derives, so it is visible before committing; changing to an outcome that needs none withdraws it; a date the officer typed is never overwritten (KI-76). The ambiguous button is relabelled **Save notes & details** and the dialog states in words that recording an outcome is done with **Complete…**. Live: the deployed `P6-CALL-CONTACTED` (3 days) derives, persists and reads back correctly, same calendar day in UTC and Asia/Qatar — **58/58** | **Closed 2026-09-19 — runtime re-test of step 3b pending** |
| KI-78 | 🟠 | **How SMS and WhatsApp are distinguished on `fax` cannot be established from evidence.** `qdb_whatsapptemplate` (String) is the only WhatsApp-specific column on the entity, which suggests "populated ⇒ WhatsApp, empty ⇒ SMS" — but that is an inference, and there are **zero fax rows** on `org5869857f` to confirm it against. | Phase 7 discovery, 2026-09-19: `fax` custom-attribute sweep (31 `qdb_` columns) and a row count of 0 | Guessing the discriminator would send a WhatsApp message as an SMS, or the reverse, at the customer's expense and outside the channel they consented to. The authorisation forbids inventing it | SMS proceeds on the evidence that exists; WhatsApp is **held** until QDB confirms. `qdb_communicationtemplate.qdb_externaltemplateref` is the natural carrier for the provider-registered template name, so the design is ready without the convention being assumed | **Closed 2026-09-20 — QDB confirmed the contract.** WhatsApp uses the same Fax entity and the same Custom Workflow Activity, with `faxnumber`, `qdb_message_body`, `qdb_sender` **plus** `qdb_language`, `qdb_whatsapptemplate` and `qdb_otp`, which are WhatsApp-only. SMS uses `faxnumber`, `qdb_message_body`, `qdb_sender` and nothing else. The discriminator is the presence of the WhatsApp-only fields, confirmed rather than inferred |
| KI-79 | 🔴 | **No authoritative Contact Hold source exists on the organisation.** `CommunicationArchitecture.md` §3.1 assumed `qdb_stopcontact`, `qdb_deceasedflag`, `qdb_dateofdeath`, `qdb_deceasedsource` and `qdb_specialhandling` on contact/account — **none of them exists**. `qdb_platformconfiguration.qdb_contactholdrulesetcode` is **null** on both rows, so no Rule Engine ruleset is nominated either. What does exist is the native Dynamics set (`donotemail`, `donotfax`, `donotphone`, `donotpostalmail`, `creditonhold`) and deceased facts on the **snapshot** (`qdb_isdeceasedperqcb`), not on the customer. | Phase 7 discovery, 2026-09-19: attribute sweep of `contact` and `account`; platform configuration read | Contact Hold is the control that stops a bank contacting a bereaved family or a customer under legal protection. Defaulting to *allow* because the source is missing would be the most damaging possible failure mode, and it would look like working software | **Fail closed.** The send gate refuses with `contact_hold_unverifiable` until QDB nominates the authoritative source. Native `donot*` flags are honoured as a **necessary** condition but are marketing preferences and are never presented as QDB's Collection hold policy. The snapshot deceased flags remain **MIS data attributes only** — not turned into Phase 9 behaviour **2026-09-20:** confirmed against the organisation — `qdb_contactholdrulesetcode` is null on both platform configuration rows. The policy for an unverifiable hold is now read from the configuration's own `qdb_featureflags` and **defaults to refuse**, including when the flag is absent, unreadable or on an inactive row. A recorded, reversible sandbox exception permits sending on `org5869857f` only (`crm/scripts/record-contact-hold-exception.mts`, `--revoke` to withdraw); every other organisation still fails closed | **Open — QDB confirmation; production sends still fail closed** |
| KI-80 | 🟡 | **The Arabic/English template model is not established.** `qdb_communicationtemplate.qdb_language` is a picklist; `fax.qdb_language` is a free-text **String**. Whether a bilingual template is one row per language or one row carrying both bodies, and what value `fax.qdb_language` expects, is not in evidence. | Phase 7 discovery, 2026-09-19: template and fax attribute metadata; 0 template rows | Choosing wrongly means either duplicate templates that drift apart, or a single row that cannot express per-language approval | Recorded rather than decided. Synthetic `P7-` templates will carry one language per row as the simpler reversible option, clearly marked as synthetic, and the choice is reversible either way | **Open — QDB confirmation** |
| KI-81 | 🟠 | **The authoritative Warning Letter mechanism is undecided.** The **Report Engine is installed** on `org5869857f` (`qdb_reportdefinition`, `qdb_reportlayout`, `qdb_reportversion`, `qdb_reportexportsetting`, `qdb_reportexecutionlog` and more) and is the obvious candidate — but its 13 definitions are **all samples and demos**, and none of the 56 SSRS reports has a name matching letter, warning, notice, demand or collection. `qdb_document_template` exists but is bound to term sheets. | Phase 7 discovery, 2026-09-19: `qdb_reportdefinition` listing, SSRS report catalogue sweep | A warning letter is legally significant correspondence. Picking a mechanism without QDB's decision risks producing a document QDB cannot stand behind, and how the generated document associates to case/customer/activity is part of the same decision | Isolated as its own work package (**WP8**) so it cannot block SMS, WhatsApp or Email, per §8 of the authorisation. Investigation only until QDB decides | **Closed 2026-09-20 — Deferred / Out of Scope by QDB decision.** Warning Letters are removed from Phase 7 entirely: no Report Engine configuration, no letter templates, no UI, no SSRS integration, no document generation, no entities or fields, and no further investigation time. This is a **scope decision, not an unresolved technical blocker** |
| KI-82 | 🟡 | **Whether `qdb_privsendsms` is the intended authorisation control point is unconfirmed.** It exists (0 rows) alongside a family of `qdb_priv_*` marker entities — QDB's established convention is a privilege-marker table whose access is granted through security roles. That makes it the organisation's existing "may this user send an SMS" control, but nothing states it is the one DCP should check. | Phase 7 discovery, 2026-09-19: `qdb_` entity sweep, 922 entities | Inventing a DCP-specific privilege would create a second authorisation model beside QDB's, and the two would drift | The design checks `qdb_privsendsms` rather than inventing a privilege, pending confirmation. CRM native RBAC on `fax`/`email` remains authoritative regardless | **Open — QDB confirmation** |
| KI-83 | 🔴 | **QDB's SMS/WhatsApp dispatch mechanism is not installed on `org5869857f`.** The `fax` schema QDB uses is fully present (31 `qdb_` columns, coherent sender/recipient/segment model), but there are **0 fax rows**, **0 workflows** with `fax` as primary entity, and the single plugin step on `fax` is Microsoft's own `LeadManagement.SendFax` (`Microsoft.Dynamics.LeadManagement.Plugins.PreOperationSendFax`, `Send` message). A sweep of **400 workflows and 200 plugin assemblies** found every SMS-related item to belong to Microsoft — Omnichannel, Field Service, Sales Insights. | Phase 7 discovery, 2026-09-19 | Phase 7 can prove that the **correct native record is created, bound and read back**. It **cannot** prove on this organisation that a message reaches a customer, because nothing is listening for the row. Describing Phase 7 as "communications working" would be the same category of claim as "runtime validated" without a runtime | Stated in every status line rather than blurred: **Record Creation Proven — Delivery Unproven on this organisation**. The live smoke asserts the persisted record, never a send. No DCP gateway is built to fill the gap, because that would replace QDB's mechanism rather than use it | **Revised 2026-09-20 — no longer an architectural unknown.** QDB confirms the SMS/WhatsApp Custom Workflow Activity **exists on-prem**; it is simply not installed in the Cloud development organisation. Approved architecture: Collection Workspace → Communication domain/service → Dynamics Fax activity → existing QDB Custom Workflow Activity → SMS/WhatsApp provider. DCP builds **no** dispatcher. In the Cloud sandbox the proof is native Fax record creation and read-back against the confirmed field contract; **external delivery is explicitly not Cloud-runtime-tested** and on-prem end-to-end validation remains a later deployment test | **Open — on-prem dispatcher validation pending deployment** |
| KI-84 | 🟠 | **No existing QDB or native mechanism can safely represent a resumable bulk-communication job.** Phase 7 §7 requires server-side bounded batching with a checkpoint, resumability across a browser close, per-recipient results and progress. Candidates inspected on `org5869857f`: `qdb_publish_job` (**277 live rows — the Dynamic Form Engine's**, carrying `qdb_form_code` and `qdb_languages_*`); `qdb_workflowtracker` (0 rows, closest in shape but no checkpoint, batch size, filter definition or per-recipient result, and unowned); `qdb_tcs_job_status`, `qdb_processstatus`, `qdb_wf_runtime_context` (empty shells); native `bulkoperation`/Quick Campaign (creates one activity per recipient but is bound to marketing `list` members and exposes no controllable continuation or per-recipient retry). | Phase 7 bulk discovery, 2026-09-20 | Repurposing `qdb_publish_job` would overload another product's live table — the mistake KI-71 already documents. Without **some** durable header, a bulk run cannot be resumed after a browser close, because the population definition is lost | **Safety does not depend on the entity.** Deterministic per-recipient ids — `activityId = uuidv5(bulkRunId + recipientId)` with `If-None-Match: *` — already guarantee no duplicates and no corruption on double-click, retry, interruption, a repeated customer in the population, or partial failure. What the entity adds is **durable resumability, progress and auditability**. A minimal `qdb_communicationrun` is **proposed for approval and NOT created**; single-channel work proceeds unblocked meanwhile | **Open — awaiting schema approval; blocks bulk resumability only** |
| KI-85 | 🟠 | **An activity's recipient party cannot be set in the create, nor in a following PATCH — only by POST to the activity's own party collection, and that POST must not ask for a representation.** Four shapes were tried against `org5869857f`: `to: [...]` inside the upsert create → **400**; `to: [...]` in a PATCH after the record exists → **400**; `POST /faxes(id)/fax_activity_parties` with `Prefer: return=representation` → **500 "Unable to cast object of type Microsoft.Crm.Metadata.EntityMetadata to Microsoft.Crm.Metadata.XrmMetadataEntityMetadata"**; the same POST with annotations only → **204**. | Phase 7 live bulk concurrency smoke, 2026-09-20 | The first two failures are visible immediately. The third is the dangerous one: the Fax row is created perfectly and only the party fails, so every send was recorded as `failed` while six correct records sat in the organisation — the run reported `0 sent + 0 refused + 6 failed` against a platform holding 6. A reconciliation that trusted the executor's own tally would have reported success | `CommunicationService.attachRecipient` POSTs to the collection-valued navigation property, whose name is read from metadata (`fax_activity_parties`, `email_activity_parties`) rather than derived — the KI-69 rule applied to relationships. `WriteTransport.post` sends `Prefer` **without** `return=representation`, because a collection append has nothing to return. Only for a record the call actually created: a repeat already carries its recipient, and a second party would give one message two recipients | **Closed 2026-09-20 — 20/20 live** |
| KI-86 | 🟠 | **A native activity row existing does not prove a communication was created.** A Fax or Email can be created perfectly while its recipient ActivityParty fails (KI-85), leaving a row nobody can receive. The danger is on the *next* attempt: the deterministic id already exists, the platform refuses the create, and an executor reading `created: false` as "already sent" would report a customer contacted who never was. | Phase 7, 2026-09-20 — consequence of KI-85, raised as a correctness rule | Silently not delivering a message to a real customer, behind a green "sent". The whole idempotency design rests on `created: false` being safe to treat as success, and this is the one case where it is not | **Three changes, all proven.** (1) `CommunicationService.send` returns `sent` only when the row **and** its recipient party exist; it reads the party collection before writing, adds the party when missing (`repaired`), and returns `incomplete` — recorded as a **retryable failure, never as sent** — when the structure cannot be completed. (2) Reconciliation counts `activityparty` rows with `participationtypemask eq 2`, not activity rows, so the platform's own count is of *complete* communications. (3) A run whose cursor has reached the end but still holds retryable failures is **Paused, not Completed**, and `BulkCommunicationService.retryFailures` repairs them through the same deterministic ids — never a second native activity. Ten named tests cover the window; the live smoke asserts one party per activity and none duplicated | **Closed 2026-09-20 — 32 automated + 23/23 live** |
| KI-87 | 🟡 | **A second copy of the entity-set mapping drifts, and the drift only shows against a live organisation.** The node harness kept its own hand-written `logical name → entity set` table. `activityparty` was added to the workspace, the harness had no entry, its `+ s` fallback produced `activitypartys`, and the live smoke failed **after creating rows** with `Resource not found for the segment 'activitypartys'`. The same shape had already produced `faxs` and `faxe`. | Phase 7 live bulk concurrency smoke, 2026-09-20 | A harness that cannot address an entity fails late, mid-run, having already written to the organisation — and the failure looks like a platform problem rather than a stale table | The harness now **derives** its map by inverting `XrmCrmAdapter.toLogicalName` over `ENTITY_SETS`, so there is one authority read in both directions and nothing to keep in step. `verify-view-columns.mts` additionally reads every set name back from `EntityDefinitions` and reports how many would be wrong by naive pluralisation (**5 of 18** on `org5869857f`) | **Closed 2026-09-20 — 46/46 live** |

| KI-88 | 🟡 | **The workspace resolved `qdb_platformconfiguration` without the key every service resolves by.** HL and BFD share one Dataverse, so **two active configuration rows is the designed shape** — one per `qdb_organizationcode`. `PlatformConfigurationService` has always resolved by that code and refuses when more than one active row claims it. The browser's Contact Hold resolver instead read *all* active rows and folded over them. | Phase 7, 2026-09-20 — raised by QDB before manual validation, after the sandbox exception activated both rows | Two failures, neither visible on the sandbox because both rows happen to carry the same flag: a decision recorded for **HL would have permitted sending on a BFD case**, and where two rows could both match, the branch taken and the message shown depended on the order the platform returned them in | `resolveContactHoldPolicy` now takes the case's organisation and filters `qdb_organizationcode eq <code> and qdb_isactive eq true` with `top: 2`. Zero rows and more than one both **fail closed** — more than one is refused rather than resolved by picking, because a deployment whose shape nobody confirmed is not one to send from. 14 regression tests, including a fake that honours the filter so a keyless resolver cannot pass, and both row orders asserted to give the same verdict. `crm/scripts/verify-configuration-determinism.mts` checks the invariant against the organisation | **Closed 2026-09-20 — 14 automated + 4/4 live** |