/**
 * The workspace root.
 *
 * Phase 5 builds this out view by view; at this point it exists so the scaffold, the build and the
 * web-resource packaging can be proven before twenty-one views are written on top of them.
 */
export function App() {
  return <div className="dcp-shell">Debt Collection Workspace</div>;
}
