<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>EDP.RuleRuntime.DevHarness</RootNamespace>
    <!-- LOCAL DEVELOPER TOOL ONLY. Not part of the shipped product; the product's
         runtime executes inside CRM/Dataverse (zero external infra invariant). This
         host merely exposes the SAME RuleRuntimeService for local browser testing. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\EDP.RuleRuntime\EDP.RuleRuntime.csproj" />
  </ItemGroup>

</Project>
