# Build artifacts / dependencies (regenerated)
node_modules/
dist/
bin/
obj/
runtime/pack/publish/
runtime/pack/*.dll
*.user

# Keep: the strong-name key (assembly identity, not a secret) is preserved for
# reproducible plugin signing — losing it changes the public key token.
!runtime/pack/edp.snk

# Noise / stale artifacts
*.log
BusinessRuleEngine_entities.zip
